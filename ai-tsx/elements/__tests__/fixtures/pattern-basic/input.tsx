import React from 'react';
import { Pattern } from '../../../pattern.js';
import { Node } from '../../../../../prompt-kit/elements/group.js';

export default (
  <Pattern id="PT_EXAMPLE">
    <Node is="Intent">Exported data shape.</Node>
    <Node is="Why">No runtime behavior.</Node>
  </Pattern>
);
