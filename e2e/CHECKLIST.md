# agent-inbox — чек-лист живой верификации

Единственный источник правды о том, что в продукте **реально работает**, а что нет. Живёт на диске,
поэтому любая сессия продолжает с места, а не с нуля.

## Как этим пользоваться

**Запуск цикла:**

```
/loop продолжай e2e-верификацию agent-inbox по e2e/CHECKLIST.md: возьми первый пункт со статусом ⬜ или ❌, прогони его команду, при красном — починить и перепрогнать, обновить статус и строку «Факт» в этом файле, закоммитить. Не спрашивай разрешения, не пиши отчёты между пунктами.
```

**Статусы:** ⬜ не прогонялся · 🟢 прогнан, зелёный · ❌ прогнан, красный · 🚧 в работе · ⛔ заблокирован (нужен оператор)

**Правила ведения (обязательные):**

1. Статус ставится ТОЛЬКО после реального прогона команды. Не «по коду видно», не «должно работать».
2. В строку «Факт» пишется вывод прогона (счёт тестов / точка отказа), а не пересказ.
3. Один пункт = один коммит. Нельзя закрывать пункт, не закоммитив.
4. ❌ обязан нести конкретную точку отказа (файл/узел/ассерт), иначе это не диагноз.

## Когда ОСТАНОВИТЬСЯ (жёсткие условия, чтобы не крутиться бесконечно)

Останавливаюсь и пишу оператору, если верно ЛЮБОЕ:

- **Всё зелёное** в разделах A–C (это финиш; D — необязательный хвост).
- **Один пункт красный 3 попытки подряд** — дальше это не отладка, а угадывание. Ставлю ⛔ с описанием, что пробовал, и иду к следующему пункту.
- **Нужно решение оператора** (удалить данные, тронуть чужой процесс, поменять контракт, постить в реальный MR сверх разрешённого) → ⛔ и вопрос.
- **Нет внешнего доступа** (нет `GITLAB_PERSONAL_TOKEN`, GitLab недостижим, opencode не поднимается) → ⛔, потому что живая проверка невозможна в принципе.
- **Нельзя починить, не сломав зелёное** → ⛔ с объяснением конфликта.

Запрещено: закрывать пункт без прогона; ослаблять ассерт, чтобы позеленело; помечать 🟢 при `skip`.

---

## A. Ядро флоу (реальный сервер, реальный MR) — блокирует всё остальное

Прогон: `npx playwright test --config e2e/inbox-serve/playwright.review-flow.config.ts <spec>`
Харнесс: `review-flow/_support.ts` — `makeStateDir({seedReview:false})` = чистая история одного MR
(режим сброса, только для теста), `bootReal(stateDir)` = настоящий сервер на :4174, `teardown`.

| #   | Пункт                                      | Команда                                 | Статус | Факт                                                       |
| --- | ------------------------------------------ | --------------------------------------- | ------ | ---------------------------------------------------------- |
| A1  | Сервер поднимается, борд отдаёт данные     | `review-flow/t1-startup.spec.ts`        | 🟢     | 1 passed (2.0s), 2026-07-27                                |
| A2  | Назначение роли создаёт живой instance     | `review-flow/t2-assign.spec.ts`         | 🟢     | 1 passed (50.7s), 2026-07-27                               |
| A3  | Живое ревью идёт по стадиям                | `review-flow/t3-t4-live-review.spec.ts` | 🟢     | 2 passed (11.7m), 2026-07-28 — см. диагноз ниже            |
| A4  | Детальный вид MR + артефакты               | `review-flow/t5-detail.spec.ts`         | 🟢     | 1 passed (2.6s), 2026-07-28                                |
| A5  | Полный проход одного MR от начала до конца | `review-flow/t9-full-flow.spec.ts`      | ⛔     | 2 passed / 3 failed / 1 skipped (17.4m) — см. диагноз ниже |

## B. То, что оператор видит первым (UI-регрессии из живого прогона 2026-07-24/27)

| #   | Пункт                                                            | Как проверяется                                                | Статус | Факт                                                                                                                                                                            |
| --- | ---------------------------------------------------------------- | -------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1  | Холодный старт: нет ложных нулей, видно «загружается» vs «пусто» | `review-flow/b1-cold-start.spec.ts`                            | 🟢     | 1 passed (4.7s), 2026-07-28 — новый тест, задержка `/api/board` на 1.5с                                                                                                         |
| B2  | Назначение отражается мгновенно (без ожидания поллинга)          | e2e: клик → карточка в лейне роли < 1s                         | 🟢     | ручная живая проверка 2026-07-28: карточка ушла в «Ждут меня»/AWAITING сразу после клика, без ожидания поллинга                                                                 |
| B3  | Роль не сбрасывается после рестарта сервера                      | `inbox-roles/__tests__/assignment-persistence.test.ts`         | 🟢     | 3/3, 2026-07-27 (6eacedb)                                                                                                                                                       |
| B4  | Стадии/бейджи меняются: план → обогащение → линзы → синтез       | `review-flow/b4-stage-badges.spec.ts`                          | 🟢     | 1 passed (9.7m), 2026-07-28 — «Проверка обогащения→Ревью→Синтез→Ожидает решения» через реальный /api/board                                                                      |
| B5  | Часы тикают и не показывают NaN                                  | e2e: два замера таймера с интервалом                           | 🟢     | ручная живая проверка 2026-07-28: 119:21:59→119:22:17→119:23:58, растёт корректно, NaN нет, переживает reload                                                                   |
| B6  | Dropdown назначения закрывается по клику вне                     | `review-flow/b6-dropdown-outside-click.spec.ts`                | 🟢     | было ❌ (реальный баг — не было outside-click хендлера), пофиксил в `UnassignedBlock.tsx`, 1 passed (5.5s) 2026-07-28                                                           |
| B7  | Отчёт без данных: понятное состояние, не голая ошибка            | `review-flow/b7-report-not-started.spec.ts`                    | 🟢     | было ❌, пофиксил `MrDetailPage.tsx` (различает HTTP 404 «ещё не начато» от реальной ошибки), 1 passed (6.6s) 2026-07-28                                                        |
| B8  | Панель кандидатов: выбор + «Постить выбранное» реально постит    | e2e + проверка комментария в MR                                | 🟢     | ручная живая проверка 2026-07-28: чекбокс→«Постить выбранное»→«✅ Запощено», audit.jsonl подтверждает `effect_applied`/`node_effect` (dry-run включён по умолчанию — безопасно) |
| B9  | Чат: моя реплика появляется сразу (оптимистично)                 | `review-flow/b9-chat-optimistic.spec.ts`                       | 🟢     | было ❌ (реального оптимистичного эха не было), пофиксил `ChatPanel.tsx`/`ChatThread.tsx`, 1 passed (10.3s) 2026-07-28                                                          |
| B10 | Выделение в артефакте → контекст → бейдж с якорем                | e2e: выделить → в контекст → бейдж указывает на тот же участок | ⬜     | —                                                                                                                                                                               |

### B7 — диагноз и фикс: голая ошибка → «ещё не начато» (закрыто)

Живая проверка на реальном сервере (`gennady inbox serve`, без `--mocks`, реальный GitLab): открыл
`/mr/vk-workspace%2Fsuperapp!645` (MR без роли, ревью никогда не запускалось) — экран показывает только
красную иконку + «Не удалось загрузить отчёт», без объяснения, что отчёта попросту ещё нет. Сетевой
запрос `GET /api/mr/.../report → 404 Not Found` — технически корректный ответ (отчёта действительно нет),
но UI не различал «404 потому что ещё не начато» и «404/500 потому что реально сломалось».

**Фикс** ([MrDetailPage.tsx](../services/agent-inbox/modules/inbox-dashboard/components/MrDetailPage.tsx)):
`_isReportNotStarted` распознаёт HTTP 404 из `fetchReport`'s catch и рендерит отдельный пустой стейт
(«Ревью ещё не начато — назначьте роль или активируйте её, чтобы начать», иконка часов, не тревоги) —
настоящие ошибки (сеть/500) продолжают идти через прежний `role="alert"`. Новый тест
`b7-report-not-started.spec.ts` проходит: 1 passed (6.6s). Регрессия проверена: B1/B6/t1 перепрогнаны
после ребилда дэшборда — всё зелёное.

## C. Постинг и эффекты (реальные записи в MR)

| #   | Пункт                                           | Как проверяется                                                | Статус | Факт                                                       |
| --- | ----------------------------------------------- | -------------------------------------------------------------- | ------ | ---------------------------------------------------------- |
| C1  | Позиция строкового комментария собирается верно | `inbox-roles/__tests__/effect-executor.reply-position.test.ts` | 🟢     | 3/3 + 8 живых комментов в `mail-core/wisever!86` (6501b15) |
| C2  | Ошибка эффекта видна в логе с причиной          | грепом по логу после сбоя: нет `error:{}`                      | 🟢     | вместе с C1                                                |
| C3  | Повторный проход не дублирует постинг           | `run-mode.test.ts` (идемпотентность)                           | 🟢     | 6/6, 2026-07-27 (72866cd)                                  |
| C4  | Шаг обогащения не вешает пайплайн               | `run-mode.test.ts` + gate feedback                             | 🟢     | 6/6, 2026-07-27 (72866cd)                                  |

## D. Хвост (не блокирует «можно отдать»)

| #   | Пункт                                                   | Статус | Факт                                                  |
| --- | ------------------------------------------------------- | ------ | ----------------------------------------------------- |
| D1  | Рестарт при orphan-opencode + устаревший pid            | ⬜     | воспроизведено вручную 2026-07-25                     |
| D2  | async git в worktree-ops (тяжёлый fetch вне event-loop) | ⬜     | context-builder уже async (6c322b5)                   |
| D3  | Изолированный тест выделение→контекст→чат без MR        | ⬜     | —                                                     |
| D4  | Визуал: читаемость, автор vs агент в чате               | ⬜     | —                                                     |
| D5  | Полный `npm run test` зелёный                           | ⬜     | был 0 fail на 5f9b23c; после правок не перепроверялся |

---

## Журнал прогонов

Одна строка на прогон: дата · пункт · результат · коммит.

- 2026-07-27 · A1 · 🟢 1 passed (2.0s) · —
- 2026-07-27 · A2 · 🟢 1 passed (50.7s) · —
- 2026-07-27 · B3 · 🟢 3/3 · 6eacedb
- 2026-07-27 · C3/C4 · 🟢 6/6 · 72866cd
- 2026-07-27 · A3 попытка 1 · ❌ beforeAll hook timeout 720000ms exceeded · —
- 2026-07-28 · A3 попытка 2 (после фикса дедлайна) · ❌ node_enrich fetch failed × 4 сессии подряд, RoleScheduler paused after 3 errors · —
- 2026-07-28 · A4 · 🟢 1 passed (2.6s) · —
- 2026-07-28 · A5 · ⛔ P4 node_enrich fetch failed (тот же блокер A3), P5-P7 каскад · —
- 2026-07-28 · A3 попытка 3 (websearch off, модель не менялась) · ❌ node_enrich fetch failed × 2 сессии, всё ещё ~300с · —
- 2026-07-28 · A3 попытка 4 (модель → deepseek-v4-flash) · ❌ почти дошёл (gate_review_synthesis на tick9), но уложился ровно в дедлайн теста · —
- 2026-07-28 · root cause · undici headersTimeout/bodyTimeout 300000мс дефолт, SDK's `req.timeout=false` не отключает его — 2 несогласованных таймаута · —
- 2026-07-28 · A3 попытка 5 (dispatcher fix + флеш-модель) · 🟢 2 passed (11.7m) · —
- 2026-07-28 · A5 перепрогон с фиксом A3 · ⛔ 2 passed/3 failed/1 skipped (17.4m) — P4-P6 на отдельном известном блокере TSK-131, P7 полностью прошёл · —
- 2026-07-28 · B2/B5/B7/B8 (ручная живая проверка, реальный сервер+GitLab, роль ревьювер) · B2🟢 B5🟢 B7❌ B8🟢 · —
- 2026-07-28 · B1 (новый тест, задержка /api/board) · 🟢 1 passed (4.7s) · —
- 2026-07-28 · B4 попытка 1 · ❌ мои баги теста: неверное поле `mrKey`, `test.setTimeout` в beforeAll не покрывал тело теста · —
- 2026-07-28 · B4 попытка 2 (поля исправлены на project/iid) · ❌ всё равно badge=n/a — поля тоже неверные · —
- 2026-07-28 · B4 попытка 3 (webUrl, подтверждено диагностическим дампом) · 🟢 1 passed (9.7m) · —
- 2026-07-28 · B6 попытка 1 (без tick() перед goto) · ❌ locator never resolved, 13m timeout — забыл F7 "unassigned needs one poll" из t9 · —
- 2026-07-28 · B6 попытка 2 (tick() добавлен) · ❌ реальный баг подтверждён: dropdown не закрывается на клик вне · —
- 2026-07-28 · B6 фикс + ребилд дэшборда · 🟢 1 passed (5.5s) · —
- 2026-07-28 · B7 фикс (различает 404 not-started от реальной ошибки) + ребилд · 🟢 1 passed (6.6s), регрессии B1/B6/t1 перепрогнаны зелёными · —
- 2026-07-28 · B9 фикс (оптимистичное эхо вопроса) попытка 1 · ❌ SocketError — тест не ждал завершения тёрна, teardown убил opencode посреди фонового createSession · —
- 2026-07-28 · B9 попытка 2 (тест ждёт завершения тёрна перед концом) · 🟢 1 passed (10.3s), t6-chat перепрогнан зелёным · —

### A3 — реальный root cause найден и исправлен (было ⛔, теперь 🟢)

Две ложные гипотезы по пути (websearch/webfetch, затем медленная модель) были опровергнуты
прогонами — оставлено в истории журнала прогонов ниже, не переписываю задним числом.

**Настоящая причина:** `@opencode-ai/sdk`'s `createOpencodeClient` пытается отключить таймаут запроса
через `req.timeout = false` — это НЕ реальное поле Fetch API и ни на что не влияет. Настоящий таймаут
(`headersTimeout`/`bodyTimeout`, undici-дефолт 300000мс) живёт на диспетчере уровнем ниже и никогда не
отключался. Любой ход агента дольше 5 минут убивался этим скрытым лимитом с голым `fetch failed` —
ДО того как наш собственный `_withTimeout` (`promptTimeout`, 3-10 мин по политике узла) успевал сработать.
Два несогласованных таймаута, младший побеждал молча.

**Фикс:** [opencode.real.ts](../services/agent-inbox/modules/inbox-opencode/opencode.real.ts) —
`OpenCodeRealOpts.dispatcher` (опциональный undici `Agent`); когда передан, оборачивает `fetch` с этим
диспетчером вместо SDK-дефолта. [bootstrap.ts](../services/agent-inbox/serve/bootstrap.ts) передаёт во все
3 места создания `OpenCodeReal` общий `new UndiciAgent({headersTimeout:0, bodyTimeout:0})` — единственный
источник таймаута теперь `_withTimeout`, не два конкурирующих. Опция сделана optional (не по умолчанию в
конструкторе), т.к. явный `dispatcher` в фиче-запросе `fetch()` полностью обходит
`setGlobalDispatcher`/`MockAgent` — а на нём стоит весь network-black-box тест-тир; это была моя
собственная регрессия при первой попытке фикса, поймана прогоном юнит-тестов (4 красных в
`opencode.real.blackbox.test.ts`) и исправлена тем же диффом.

Модель `node_enrich` также сделана конфигурируемой (`AGENT_INBOX_ENRICH_MODEL`, дефолт
`llm-proxy/deepseek-v4-flash` — обогащение не требует ревью-качества) и websearch/webfetch —
конфигурируемый (`AGENT_INBOX_ENRICH_NETWORK`), с динамическим промптом и гейтом на секцию
`## Веб-исследование`, когда включено — но ни то ни другое не было настоящей причиной сбоя.

**Итог:** `t3-t4-live-review.spec.ts` — 2 passed (11.7m), 2026-07-28, полный живой проход
prepare→enrich→lenses→synthesize→ask, без единого `fetch failed`.

### A5 — перепрогнан с фиксом A3: 2 passed / 3 failed / 1 skipped (⛔ новый, отдельный блокер)

Диспетчерный баг A3 подтверждён исправленным: **P7** (`t9 P7 action (own independent live drive)`,
это отдельный собственный живой прогон внутри того же файла) прошёл целиком — `node_review_fanout →
gate_review_filled → node_synthesize`, дошёл до постинга DRY-RUN действия, 0 `fetch failed`. **P3 sub-step
1** (борд грузится) тоже 🟢.

**P4/P5/P6 падают каскадом от одной причины, уже задокументированной в самом тесте (TSK-131), не
связанной с A3:** P4 явно рассчитывает, что P3 назначил роль РЕАЛЬНЫМ кликом в UI («no fresh
assignManual — the instance P3 assigned via the UI is still live in this same process» —
[t9-full-flow.spec.ts:154](../e2e/inbox-serve/review-flow/t9-full-flow.spec.ts)). Но кнопка «Assign»
для этого MR не рендерится — `RoleScheduler#_filterActionable`
([role-scheduler.ts:572-590](../services/agent-inbox/modules/inbox-roles/role-scheduler.ts)) отфильтровывает
его через approved-by-me/idle/stale-draft логику. Тест это УЖЕ знает и не падает молча на этом месте —
сам себя помечает `test.fixme()` с явным указанием на тикет («Ticket P3: an absent UI element is
escalated as a discovery, never routed around»). Из-за этого P4 просто крутит 60 пустых тиков
(`state=none node=n/a`, 5.4 мин) без инстанса и честно проваливается на своём ассерте; P5/P6 — чистый
каскад (нечего проверять на диске).

**Не чинил** — это не A3 и не угадывание: причина указана самим тестом с точным путём (`role-scheduler.ts`)
и номером тикета. Вопрос на решение оператора: реальный MR `hocuspocus/hocuspocus!18` сейчас
approved-by-me/idle/stale-draft на GitLab (тогда фильтр работает верно, и тестовую фикстуру нужно менять
на другой MR), или `_filterActionable` ошибочно исключает валидный MR (тогда это баг фильтра) — не
проверял живое состояние MR на GitLab самостоятельно, т.к. это решение по данным, не по коду.
