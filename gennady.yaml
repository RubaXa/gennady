verify:
  gates:
    - { id: type-check, argv: [npx, tsc, --noEmit], timeout: 5m }
    - {
        id: contracts,
        argv: [npx, tsx, cli/gennady.ts, lint, cli/, shared/, services/],
        timeout: 5m,
      }
