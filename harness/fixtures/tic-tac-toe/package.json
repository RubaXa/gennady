{
  "name": "tic-tac-toe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CLI tic-tac-toe — SDD Flow v2 eval fixture (scenario S1: ready specs + ticket).",
  "scripts": {
    "test": "node --test --import tsx test/*.test.ts",
    "coverage": "c8 --reporter=json --reporter=text-summary node --test --import tsx test/*.test.ts",
    "play": "node --import tsx src/cli.ts"
  },
  "devDependencies": {
    "c8": "^10.1.3",
    "tsx": "^4.19.2"
  }
}
