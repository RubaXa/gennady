# MR vk-workspace/superapp!510 — feat: Улучшение механизма автообновления и поддержка S3/CDN

> Стадия A · роль: reviewer · reply_needed · черновик, в GitLab НЕ постится.

**Вопрос 1 — @d.zhuk (suggestion про `resolvedS3Prefix`)**
> Идея разделять префикс по окружению верная, но в одном suggestion смешаны два вопроса: (а) разные значения prod/dev и (б) формат пути. По формату согласен с @d.sumbaev ниже — `desktop` отдельным сегментом небезопасен; давайте сойдёмся на его варианте. ⚠ В треде не видно, откуда берётся `isProduction` и какова реальная раскладка бакета в prod — подтвердите, что в проде префикс действительно без `vk-workspace/`.

**Вопрос 2 — @d.sumbaev (риск ветки `desktop`, вариант `vk-workspace/superapp-desktop`)**
> Согласен. `desktop` отдельным сегментом конфликтует с пространством веток, на dev это выстрелит. `vk-workspace/superapp-desktop` (один сегмент через дефис) коллизию убирает и ближе к проду. Поддерживаю.

**Вопрос 3 — @d.sumbaev («Заменил»)**
> Спасибо, вижу замену. С моей стороны ок, тред можно закрывать. ⚠ Перед резолвом проверьте, что новое значение подхвачено везде, где использовался старый префикс (нет хардкода в других местах).

**Вопрос 4 — @d.zhuk («почему не TS?»)**
> Технически TS тут сделать можно, ограничения нет. Но смысл переезда есть только если файл остаётся надолго — см. ответ ниже.

**Вопрос 5 — @d.sumbaev («можно, но нужно ли — всё равно убираем»)**
> Согласен: раз файл планируем выносить, переписывать на TS сейчас — лишняя работа, которая всё равно уедет. Оставляем, тред закрываем. ⚠ Если «убрать отсюда» пока только на словах — заведите TODO/задачу со ссылкой, чтобы файл не завис в этом виде.
