// @file: pipe in nested class
// @consumers: test
export class Outer {
  /**
   * @purpose Deep indented pipe.
   * @param x Input.
   * @returns Result.
  */
  m1(x: string): string { return x; }
}
