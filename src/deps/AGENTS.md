# AGENTS — deps (продолжение src/AGENTS.md)

Домен: ленивый импорт пакетов. Установка только в node_modules, без изменения package.json (`npm install --no-save`).

**Файлы:** npm-import.js (фасад), find-package-root.js, npm-install.js. Логгер из utils/logger.js.

Правила и контракты: см. корневой [AGENTS.md](../../AGENTS.md) (flow logging, JSDoc). Реализация — эта папка.

Вверх: [src/AGENTS.md](../AGENTS.md).
