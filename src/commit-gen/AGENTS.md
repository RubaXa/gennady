# AGENTS — commit-gen (продолжение src/AGENTS.md)

Домен: генерация commit-сообщений из staged изменений.

**Файлы:** commit-gen.js. Использует AiCore, getGitDiffInfo (git), prompts (commit message/changeset), utils (logger, style, xml).

**Точка входа из CLI:** cli/cmd/commit.js.

Вверх: [src/AGENTS.md](../AGENTS.md).
