# review-diff

```sh
# Генерация XML артифакта (по умолчанию --type=unified)
npx --yes gennady@next review-diff

# Добавляем в контекст
npx --yes gennady@next review-diff --type=around

# Генерация png-файла
npx --yes gennady@next review-diff --output=png
```

## Базовое поведение:

Базовые поведения работы с параметрами запуском, аналогично:

- cli/cmd/review/review-issues.cmd.ts
- cli/cmd/review/review-verify.cmd.ts

## Определения среза

## Структура XML-артифакта

TODO

## Структура png-файла
