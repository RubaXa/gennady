// @file: Template for the E2E fixture that intentionally omits @file after copying.
// @spec: CLI-E2E
// @consumers: FixtureConsumer

/** @purpose Function without @file: header. */
export function noHeader(value: number): number {
  return value;
}
