# Quality rules

Cross-cutting quality rules referenced from cascaded rule sets.

Examples: `naming.xml`, `security.xml`, `accessibility.xml`, `i18n.xml`.

Rules in this category cut across language and architecture — they apply universally to whatever is being built.
