# Architecture rules

Architectural patterns referenced from cascaded rule sets (project / domain / task).

Examples: `ports-adapters.xml`, `ddd.xml`, `event-sourcing.xml`, `hexagonal.xml`.

Rules in this category constrain HOW components compose and depend on each other, not the language or test framework.
