# 60 — Приёмка «v1 больше не нужен»

> Статус: ЧЕРНОВИК на основе решений оператора (1.1, 3.4, D-3, D-4, D-6, D-12, D-16, D-28, D-29, **D-31..D-33**, **D-38..D-46**, **D-48, D-49**). Ревизия после независимой верификации **V-61** (`_raw/V-61-TASK-BOARD.md` § Приёмка): добавлена колонка «задачи-исполнители», исправлено «10 скоупов» → 12, заведены критерии A11..A15, пересобран §4. **Вторая ревизия — после гейта адекватности** (`06-ADEQUACY-GAP.md` §5.3/§5.4, верификация **V-06-GAP**, решения оператора **D-38..D-46**): заведены критерии **A16..A23**, применены восемь правок существующих критериев (A1, A3 ×2, A6 ×2, A9, A13, §3). **Третья ревизия — после триажа #14** (`_raw/V14-PROPOSALS-TRIAGE.md`, `_raw/V14-2-AUTONOMOUS-EXECUTE.md`, решения **D-48, D-49**, Lead **L-16**): заведён критерий **A24** (гейт отложенного решения execute); `V14-1` внесена исполнителем `A8`, `V14-3` — исполнителем `A21`. **Четвёртая ревизия — решение оператора D-51**: умбрелла `V14-2` (`V14-2a..e`) перенесена из «вне волн» в релиз `2.0.0-draft.N` (Волны 3/4 доски `61`); критерий **A24** переформулирован как релизный (не блокер релиза); строка про `V14-2a..e` снята из §3. Уточняется по мере исполнения треков.
> Абсолютный путь к документам плана в worktree Lead: `/Users/k.lebedev/Developer/gennady/.claude/worktrees/nice-panini-8aa14e/ai/drafts/research/sdd-v1-to-v2-transfer/`. Идентификаторы задач — по мастер-таблице `61-TASK-BOARD.md` §1.

## 1. Что доказываем

RC (v2) становится единственным SDD: всё полезное из main перенесено или мотивированно отклонено, v1 удалён из дерева, потребители могут мигрировать. Версия релиза — `2.0.0-draft.<N>` (D-12); всё ниже 2 — v1.

## 2. Критерии приёмки (все обязательны)

| № | Критерий | Доказательство | Задачи-исполнители | Источник решения |
|---|---|---|---|---|
| A1 | **Самомиграция gennady**: собственные `tasks/` (127 v1-тикетов, **12 скоупов**) переведены `sdd-migrate` в v2-раскладку; `tasks/` удалён; `sdd-state` → `FLOW_VERSION=v2`. **Бар (правка 57, 06 §3.3): корневой `sdd-check --all .` не даёт ошибок, которых нет в baseline `GAP-B-1` (zero-new относительно среза `rc-baseline-1`)** — формулировка «без ошибок класса `MIGRATION_CRITICAL_CODES`» снята как недостижимая: этот класс красен **уже сегодня** (10 из 198 ошибок на `227c03a8`). **Остаток перечисляется по `code/file/severity`; общий count непригоден** (`evidence-audit:346`). Корневой прогон — **диагностика слоёв A→C**, сам `A1` покрывает только слой C. **Зелёный не достигается правкой самих v1-данных вне мигратора.** **До завершения самомиграции корень красный и `A1` недостижим — это ожидаемо (D-39)** | детерминированный грейд `migration-grade.ts` + `sdd-check --all` в CI **против baseline `GAP-B-1`** (предикат zero-new-error); бар несовместимости — на frozen golden-v1 (`E-22`); **покрытие BDD держит `B2-22`** (правка 60: исходная адресация на `A7` неверна — `A7` про журнал) | **E-14** (самомиграция), **E-20** (удаление `tasks/`), B2-16, B2-09, B2-10, B2-15, **B2-22** (fail-closed BDD), **GAP-B-1** (точка отсчёта), **E-22** (frozen golden-v1); предикат v2-имён — B2-16 (L-2: грандфазеринг не распространяется на `*.task.<ID>.md`) | D-3, 3.4, L-2, **D-38, D-39** |
| A2 | **Миграция реального проекта cloud-ios** (`fixture-mig-run`): PASS грейда; мигрированные тикеты исполнимы (`sdd-task` принимает: 3-колоночные §5, секция `SCOPE_TYPE`, маркер `PHASE_RECEIPTS:v1`) | E-07/E-06 трека 50 (**порядок по L-15: сначала бар red-first E-07, затем полнота мигратора E-06**); предпосылка (а) G4; фикстуры воспроизводимы из SHA через `EVAL_FIXTURES_ROOT` и не содержат файлов класса секретов (L-13) | **E-16** → **E-07** → **E-06**, `fixture-mig-run` | D-29, L-13, L-15, статус RC |
| A3 | **Полный цикл на не-Node стеке**: authoring → scaffold → execute на golang-фикстуре и swift-фикстуре (cloud-ios round-trip) проходит механически: тикет `[x] DONE`, `sdd-verify` receipt записан без `package.json`-шима, групповая квитанция аудита есть, R-COMPLETE зелёный | flow-eval G1 (E-* трека 50), `roundtrip-eval.sh` без `roundtrip-readiness-shim.package.json`; **покрытие BDD fail-closed держит `B2-22`** (правка 60: `.skip`/`.todo`/неактивный тест не закрывает сценарий — иначе «тикет `[x] DONE`» доказывает не то) | go-ветка: V-04a, V-05, V-06, **V-09**, E-11, **E-12**; swift-ветка: V-07, V-08, **V-11**, E-10 и **E-18**; общее: **B2-22** (fail-closed BDD). **Правка 58 + D-45: `E-13` из исполнителей удалён** — это диагностика, а не бар (следствие D-28/L-14); **бар G1 = `E-12` (go) + `E-18` (swift)**. Python в критерий не входит (правка 59a, **D-44**) | 2.2, D-14, D-15, D-16, **D-44, D-45** |
| A4 | **Все 17 вердиктов по issues akkrat** (док 20) реализованы или мотивированно отклонены; #9.4 и #24 закрыты минимальным набором SO-1/2/2b/7/11 (**5 блокеров релиза**, D-22) | тесты-замки из док 20/32; e2e-фикстуры G2 «дерево до → sync → снимок после» на копиях messenger и cloud-ios без потерь проектных файлов | SO-1, SO-2, SO-2b, SO-7, SO-11, LOCK-1..3, владельцы ISS-* (B2-02/03/04/05/06/12, V-12, V-13, V-14, T-B6-*); **E-04** (6 both-way кейсов, пишется **после** пятёрки SO) и **E-19** (снимки на копиях реальных потребителей — новая задача; без неё критерий сужается до E-04 + портов тестов SO-*) | D-6, D-22 |
| A5 | **v1 удалён из дерева RC**: нет `ai/directives/sdd/`, v1-скиллов, `ai/skills/sdd-execute/scripts/`, `verify.sh`; `sync`/`sync-skills` деплоят только v2; docs (`README`, `ai/skills/README.md`, guides) описывают только v2 | grep-гейт анти-v1 в `npm run check`; deployed-surface golden | **V-17** (зависит от V-15 + **V-16a**, не от заблокированной V-16b), SO-5, SO-6, SO-12 (guide), T-B6-07 (только триггеры в `description`, без обёрток — D-27), **REL-16** (docs-проход) | 1.1, 3.4, D-27 (в части имён скиллов) |
| A6 | **Универсальный verify**: node-пресет даёт байт-в-байт те же команды и receipts (D-17); стеки **anystack → golang → swift** (правка 59a + **D-44**: python вынесен за `2.0.0-draft.N` вместе с `V-10` и в этом критерии больше не перечисляется; возвращается после релиза); **оговорка (правка 59): npm-набор гейтов относится к самому репозиторию `gennady` и не сужает `A3`/`A6` до Node** (`R3:247`); `gennady verify --plan --json` read-only; долгие гейты требуют `when` (D-18); `[gate] `-строки всегда попадают в **отчёт** — **попадание в квитанцию решается отдельно, после сверки D-19 с инвариантом И-2** (`61-TASK-BOARD.md` §2.2 п.6; до решения этот подпункт не считается); для репозитория без `gennady.yaml`/`when` план и `gateEvidence` до и после V-12 совпадают побайтово (D-17); в `dist`/`exports`/help **нет** `gennady fix` без `RepairMutationBoundary` (D-13) | parity-тесты трека 30 (golden резолва + поведенческий + receipt), `services/stack` тесты перенесены и зелены; grep-замок на `gennady fix` в публикуемой поверхности | V-01..V-09, V-11..V-15, V-04a, **V-16a**, **V-19** (валидация `timeout > 10m` без `when`), V-02 (`services/stack`), **SO-13** (плагинные корни в sync — **только `plugins/{anystack,golang,swift}`**); отрицательный замок — V-16b/REL-2/REL-4. **`V-10` (python) из исполнителей исключён — пост-релиз по D-44**; **правка V-61b: python-часть `SO-13` (корень `plugins/python`) тоже вне релиза** — идёт после релиза вместе с `V-10`, ребро `SO-13 → V-10` снято, иначе релизный критерий стоял бы на пост-релизной предпосылке (`61 §2.2` п.13) | D-13..D-19, **D-44** |
| A7 | **Журнал**: `— re-run:` только до close, `## Blocker Trail`, `correction` в словаре, post-close детекция, Reopens по причинности, групповая квитанция учитывается в pickable | тесты `shared/sdd/execution-log`, `sdd-log`, `sdd-check`; golden `DA-lazy-asm` | B2-01, B2-03, B2-04, B2-06, B2-08, **B2-13** (pickable, вариант D3 выбран D-21), **B2-19** (секция `## Blocker Trail` — новая задача), B2-17 (spec-first предпосылка трека), E-05 | D-20, D-21, L-1..L-3 |
| A8 | **Правила (релизный объём, режим (A) по D-31)**: baseline + go-rules перенесены; реестр project-owned (временно, до слоёв); проверка контракта правила — один модуль, два входа; `<Rule><File>` реестра обязан существовать (`SDD_RULE_FILE_MISSING`, severity `warning` по L-3); механическая проверка «Swift-тикет не получает `typescript-rules`» — в детерминированной форме (33b §6), LLM-точка G1 в релиз не входит. **`RULES-D1..RULES-D6` (динамический справочник `sdd-rules`) вынесены за релиз** и пересматриваются по D-33. **По триажу #14 (D-48): `scope`/`module` authoring объявляют `AX_SCALE_PROPORTIONAL_DEPTH` и несут EXPRESS-ветку** — прямого критерия на directive-scale в доске нет, ближайший владелец — этот критерий (`V14-1`) | kit-аудит + `sdd-check --rules`; детерминированный тест на swift-фикстуре (см. A3/E-18); `deployed-surface`-golden правил (T-9); для `V14-1` — `grep -c AX_SCALE_PROPORTIONAL_DEPTH` на `scope.directive.xml`/`module.directive.xml` ≥1 в каждом | T-1, T-3, T-4, T-6, T-9, T-10, T-11, T-12, T-13, SO-1, SO-11, **T-15** (аксиомная половина L-11), **V14-1** (SCALE/EXPRESS в scope/module — ближайший владелец, прямого критерия на directive-scale нет); вне релиза — T-2, RULES-D1..D6 | **D-25, D-31, D-32, D-33** (D-24 замещён), L-5..L-8, L-11, L-3, **D-48** |
| A9 | **Релиз**: `exports` как в main (+`./stack`), `.npmignore`, publish-before-git, smoke/publish-contents тесты, CI workflow вокруг test-topology, `npm audit` без high, версия `2.0.0-draft.N`; **вопрос тест-конкурентности закрыт по итогу REL-15** — либо `=1`, либо доказанная безопасность `=6` с зафиксированным обоснованием (D-10/D-30) | `npm pack --dry-run` без тестов/фикстур; CI зелёный; отчёт REL-15 с воспроизведением. **Правка 61 (06 §2.1 M15): доказательство релиза воспроизведено на ОДНОМ immutable коммите (`REL-18`); `npm run format` на финальном прогоне — check-only; локальные `dist`/`coverage` и прежние receipts как evidence не используются; прогон выполняется ВНЕ restricted sandbox — `EPERM`-падения не считаются product failure и не смешиваются с ним** (`sdd-v2-rc-hardening-plan:312`) | REL-1..REL-6, REL-8..REL-11, REL-14, **REL-15** (причина IPC-краша) и зависящие **REL-7/REL-12/REL-13**, SO-5 (deployed-surface + tarball), **REL-17** (immutable срез), **REL-18** (evidence pack на одном чистом коммите), **SO-13** (плагинные корни в sync — релизная часть без `plugins/python`; python-корень идёт после релиза вместе с `V-10` по D-44, правка V-61b, `61 §2.2` п.13) | D-9, D-10, D-11, D-12, D-30, **D-38**, **D-44**, REL-1..18 |
| A10 | **Eval**: PASS = детерминированные гейты; `budget-exhausted` отдельный исход; минимальные сценарии G1–G4 существуют и воспроизводимы (2 pass) — **для G1 детерминированный охват: go (E-12) и swift (E-18); python в релиз не входит** | flow-eval suite + `.results`; агрегированный exit-код считается по гейтам, не по вердикту судьи | E-00, E-01, E-02, E-03, **E-17** (исход `budget-exhausted` + exit-код по гейтам), **E-21** (роль судьи — диагностика, L-14), E-09, **B2-22** (fail-closed BDD — правка 60 требовала внести её в доказательства `A3`, `A1` **и `A10`**; пропуск найден V-61b: без fail-closed покрытия «G1 воспроизводим 2 pass» доказывает не то) | D-28, L-14, 4.5 |
| A11 | **`## Blocker Trail` существует как секция**: `sdd-log resolved` пишет в неё с обратной ссылкой на раунд/фазу; анкор доступен `sdd-extract`; существующие `✅ RESOLVED` мигрированы; шаблон тикета обновлён | `sdd-log.cmd.test.ts` + миграционная фикстура; `sdd-extract` вытаскивает анкор | **B2-19** (единственный владелец; B2-01 лишь переносит существующий `scanBlockerTrail`) | **D-20** |
| A12 | **`budget-exhausted` — отдельный исход**, не `fail`: прогон с исчерпанным бюджетом не красит CI и не входит в статистику; агрегированный exit-код `sdd-flow-eval` считается по **детерминированным гейтам**, а не по вердикту судьи | both-way: «батч с исчерпанным бюджетом → exit 0 + отдельный исход», «батч с fail гейта → exit 1» | **E-17** (+ E-00 как основа, E-21 как роль судьи) | **D-28**, L-14 |
| A13 | **Групповые квитанции переключены WARN→ERROR** после того, как мигратор эмитит `PHASE_RECEIPTS:v1` (E-06) и пройдена самомиграция (E-14): `SDD_GROUP_AUDIT_MISSING`/`SDD_GROUP_REVIEW_MISSING` — класс error | `check.test.ts` (severity) + `sdd-check --all .` на самомигрированном корпусе; **плюс правка 62: на frozen golden-v1 фикстуре (`E-22`) переключение WARN→ERROR не даёт новых error относительно baseline `GAP-B-1`** | **B2-20** (гейт «после B2-16, E-14 и E-22»); предпосылки — B2-16, E-06, E-14, **E-22**, **GAP-B-1** | **D-4**, O-6, **D-38, D-39** |
| A14 | **Docs и `tasks/` приведены к v2**: `README`, `ai/skills/README.md`, guides описывают только v2; grep-замок анти-v1 расширен на docs; каталог `tasks/` удалён отдельным проверяемым шагом | grep-гейт = 0 v1-упоминаний в docs; `ls tasks/` отсутствует; `sdd-state` → `FLOW_VERSION=v2` | **REL-16** (docs), **E-20** (`tasks/`) | 1.1, 3.4, D-3 |
| A15 | **Найденная утечка в поставляемой поверхности устранена**, не только зафиксирована golden'ом: путь dev-home в `ai/directives/agent-inbox/golden-chat-output.example.md:176` вычищен (при необходимости — правило в `path-normalizer.ts`); tarball чист | deployed-surface golden (`UPDATE_SURFACE_GOLDEN=1`) + `npm pack --dry-run`; grep по dev-home путям = 0 | **SO-5** (правка файла в `ai/directives/agent-inbox/**` — устранение утечки в поставляемой поверхности, не работа над agent-inbox; D-2 не нарушается) | 1.1, 3.4, D-2 (граница) |
| A16 | **Идентичность решений замкнута**: неразмеченного normative `D-NNN` в новых артефактах нет; repo-wide коллизии акронимов и дубли canonical DL/REQ ловятся **механически**; cross-spec перенос без Decision Move Map — **fail closed**. Релизный объём сужен **D-41/D-42**: короткий ADR (репозиторная уникальность акронима + `**Acronym:**` override в META) + ошибка `sdd-check`/scaffold на коллизию + падение `sdd-migrate move` при внешних ссылках на переносимое решение (репозиторный поиск, перечень файлов в ошибке). **ID не мигрируются** | guard-тест «новый неразмеченный normative `D-NNN` = fail» + именованный allowlist из 6 legacy-мест; детерм. «две спеки с одним акронимом → error», «с `**Acronym:**` override → зелено»; детерм. «`move` решения, на которое ссылаются, → exit≠0 + список файлов» | релиз: **IDN-1**, **IDN-2**, **IDN-2a**; вне релиза (D-41): IDN-3, IDN-4, IDN-5, IDN-6 | **D-41, D-42**; 06 §2.1 M17, §2.4 G-1 |
| A17 | **Владение файлом честное (полный переход на `@spec`, D-40)**: новый v2-источник хранит **один resolvable `@spec`**; `planned`/`active`/`blocked`/`history` вычисляются инструментом; исторический тикет **не** становится current owner; два active writer-а — **fail closed**; v1 `@tasks` остаётся совместимым legacy-**входом**; resolver обязан различать **declared** и **observed** evidence-классы и **никогда не показывать `declared` как доказанное изменение** (`ownership-plan:57`, предупреждение R2 №9, `R2:394`). Мигратор механически заменяет `@tasks` на `@spec`; аксиома `AX_FILE_HEADER_TASK_TRACEABILITY`, шапка `templates.ts`, `tasks-append-only`, `reconcile.directive.hbs` и парсер `orient/core/extract-header.ts` приведены к `@spec` | `file-relations.test.ts` (declared/observed разведены); детерм. «два active writer-а → fail closed»; детерм. «orphan-`@spec` → finding»; `audit:sdd-templates` зелен на новой шапке; `orient --file`/`--history`/versioned `--json` | **FO-1, FO-2, FO-3, FO-3a, FO-4, FO-5, FO-6** (все в релиз по D-40) + **B2-12** (единый список расширений, уровень доказательства по языку); вне релиза — **FO-7** (кэш resolver-а, только по измерениям) | **D-40**; 06 §2.1 M8, §3.1 |
| A18 | **Приёмка стоит на четырёх изолированных слоях и одном immutable SHA**: слой **A** — frozen golden-v1 (`E-22`), слой **B** — fresh-v2, слой **C** — migration, слой **D** — adversarial (`E-23`); всё доказательство снимается на срезе **тег `rc-baseline-1` = `227c03a8`** (`REL-17`); worktree после гейтов **чист**; **остаточные warnings перечислены по коду и количеству**, а не общим счётчиком. Точка отсчёта «ноль новых ошибок» — версионированный baseline `GAP-B-1`; **любое изменение baseline — отдельное operator-approved решение** | `git rev-parse rc-baseline-1` = `227c03a8`; `git status --porcelain=v1 --untracked-files=all` пуст; предикат zero-new-error в обе стороны (включая негативный тест); evidence pack на одном чистом коммите; migration dry-run дважды (второй — no-op); **прогон вне restricted sandbox, `EPERM` ≠ product failure** | **REL-17**, **GAP-B-1**, **REL-18**, **E-22**, **E-23**, E-16 | **D-38, D-39**; 06 §2.1 M15/M16, §2.4 G-3/G-4/G-10 |
| A19 | **BDD-покрытие fail-closed**: `.skip`/`.todo`/неактивный тест **не закрывает** сценарий; unparseable строка → `unknown/unverified`; **ни один negative case не даёт false closure**. Переход к `unknown` вводится **через versioned baseline** существующего корпуса (140 строк), а не сплошным поднятием severity; отдельно проверяется **неудовлетворимость**: swift-тикет с корректной строкой покрытия не должен становиться незакрываемым | negative fixtures + фикстура на неудовлетворимость; adversarial-корпус `E-23` (malformed rows, inactive tests) | **B2-22**, **E-23** | 06 §2.1 M6, §2.4 G-5 |
| A20 | **Обещания не шире механизма**: `sdd-sync` описан как **tracker-status/rollup** во всех поверхностях (help, директива, `AUTHORING.md`); остальной `spec↔code↔task` помечен `agent-reviewed`; `proof`/`verified`/`100%` **не применяются** к проверкам presence/shape/linkage; **уровень сканера объявлен по языку** (`exact` для ts/tsx, `approximate` для остальных); зафиксировано «golden доказывает неизменность поведения, а не его корректность». **По D-43: ID требований обязательны только в подписях диаграмм требований** (Overview — без ID), чекер проверяет **существование** указанных ID, правило действует для новых и мигрируемых спек | grep-замок словаря assurance; grep-замок «`back-sync` не обещается как механизм»; детерм. «подпись диаграммы требований без существующего ID → finding» | **T-B6-28**, **T-B6-29**, **SO-14**, REL-16, **B2-12** (уровень по языку) | **D-43**; 06 §2.1 M13/M14, §2.3 №19, §3.5 |
| A21 | **Харнесс не производит ложных исходов**: `events` либо подключены, либо убраны из evidence и доков; `phase`/`mode` валидируются **fail-fast**, а не через fallback (опечатка `phase` даёт пустой промпт, опечатка `mode` — **другой валидный** промпт: прогон выглядит успешным, но измеряет не ту ветку); миграционный раннер запускается **из репозитория** дефолтом (три литерала `migration-eval.sh` + `session-metrics.py:19-21` устранены); `require-developer-repo.test.sh` включён в гейт; каждая `E-*`-приёмка называет `--keep`, бюджет и путь `.results/run-<ISO>/summary.json`. **Единая eval-спека (D-46)** заменяет `AGENT-BRIEF.ru.md`, корпус доков сокращён **19 → README + спека + RUNBOOK + ledger**, словарь правил качества **один** (`R1`, `MIGRATION`, `R-COMPLETE`). **По триажу #14 (предложение 7): спека несёт раздел «инъекция → ожидаемый исход» + один both-way причинный кейс** (детерминированные инъекции — часть приёмки; панель дешёвых агентов бар не образует, D-28/L-14) | детерм.: «опечатка `phase`/`mode` → exit≠0»; «раннер стартует из чистого клона без env-переменных»; шелл-тест виден гейту; **скрипт-верификатор спеки: каждая команда и путь проверены, `[UNVERIFIED]` = 0; `README` линкует все оставшиеся доки**; для `V14-3` — both-way `injection-golden.test.ts` (с инъекцией — код есть и exit≠0, без — кода нет и exit 0) | **GAP-E-1**, **GAP-E-4**, **GAP-E-5** (несёт поглощённый `GAP-E-3`), **V14-3** (раздел «инъекция → исход» + both-way кейс поверх `E-23`); **GAP-E-2** — SHOULD, не MUST: **пункт таблицы бюджетов по фазам этот критерий не блокирует** (записано правкой V-61b — модальность SHOULD раньше стояла только в строке задачи и в приёмке не отражалась). **Область `GAP-E-5` — все 19 доков `ai/flow-eval/**/*.md`** (правка V-61b: прежняя область покрывала 11 из 19) | **D-46**; 06 §2.5, §3.8, §3.9; **предложение #14 №7** |
| A22 | **Вход замкнут**: приложен read-only снимок открытых issues (`GAP-1`, **без записей в GitHub**); baseline плана дополнен по D-36 (`GAP-4`); у остаточных находок сводки есть владельцы — 8 инвариантов без вердикта (`P-01`), 25 тест-файлов вне `npm test` (`GAP-2`), `H_ASK_WITHOUT_CARD` (`GAP-3`) | сверка: каждый open issue снимка имеет вердикт или явное «вне скоупа»; топология видит все тест-файлы; `lint-axioms` без исключения под `H_ASK_WITHOUT_CARD`; baseline содержит sha `0.9.0-next.4` либо запись «публикации не будет» | **GAP-1**, **GAP-2**, **GAP-3**, **GAP-4**, **P-01** | **D-36**; 06 §1.1 (цели 2.1, 3.4, 5.3) |
| A23 | **В RC нет research-only темы без явного решения, и ни один пост-релизный id не втянут в релизный объём молча.** Список research-only ведётся в **§3** этого документа (`GAP-P-2`). Утверждение о состоянии релиза: **на момент релиза `2.0.0-draft.N` ни один id вне релиза не является исполнителем релизного объёма** — то есть не стоит в волнах −1…5 и не назван в графе «задачи-исполнители» без явной пометки «вне релиза» в той же ячейке. **Переформулировано правкой V-61b:** прежняя редакция объявляла доказательством буквальный grep «ни один id вне релиза не встречается в волнах и в исполнителях критериев», и этот grep на текущем `60` **красный** — 15 «вне волн» id встречаются здесь именно как помеченные исключения (`B2-21`, `FO-7`, `GAP-E-3`, `IDN-3..IDN-6`, `RULES-D1`, `RULES-D2`, `RULES-D6`, `SO-3`, `SO-8`, `T-8`, `V-10`, `V-16b`), например в `A8` («вне релиза — `T-2`, `RULES-D1..D6`») и `A16` («вне релиза (D-41): `IDN-3..IDN-6`»). Пометка — не нарушение инварианта, а его исполнение. **После триажа #14 множество «вне волн» ненадолго пополнилось `V14-2a..e`** (пост-релиз по D-49), но решением оператора **D-51** все пять перенесены обратно в релиз (Волны 3/4 доски `61`) — сегодня они не в «вне волн» и инвариант A23 их не касается | **проверяется на релизе, командой:** для каждого id из множества «вне волн» (`61 §3` «Вне волн» + `61 §5.3`) grep по `60 §2`/`61 §3` — **каждое** вхождение либо отсутствует, либо несёт в той же ячейке явную пометку «вне релиза»/«пост-релиз»/`ЗАБЛОКИРОВАН`; вхождений без пометки — **ноль**. Результат прогона — в `61 § Итог правок гейта адекватности`, п. «Сверка консистентности» | **GAP-P-2**, **`61 §5`** (машинный пересчёт счётчиков) | 06 §2.1 M16+; закрывает п.15 acceptance-списка первоисточника (`sdd-v2-rc-hardening-plan:334`) |
| A24 | **Релизный критерий (D-51; умбрелла `V14-2` — в релиз `2.0.0-draft.N`, Волны 3/4 доски `61`) — не блокер релиза, заведён для трассируемости.** Отложенное решение execute — машинно-проверяемый объект (триаж #14 предложение 2). Запись `[verdict: pending-operator]` в ticket-local Decision Log делает `sdd-check --all .` красным на границе завершения группы; пачка не закрывается, пока по каждой записи не выставлен вердикт **принять как есть** / **переделать** / **откатить** (словарь токенов закрыт **L-16**: `pending-operator` (по умолчанию) · `accepted` · `rework` · `rolled-back`; «add to the spec» отдельным токеном не вводится — следствие `accepted`). Hard stops (Approval #1/#2, красный обязательный гейт, внешняя авторитетность) под перенос **не** попадают; вопрос оператору посреди пачки без записанной карточки — `H_ASK_WITHOUT_CARD` | код `SDD_DEVIATION_VERDICT_MISSING` (ERROR при `[x] DONE`, WARN иначе); both-way на фикстуре (запись `pending-operator` → exit≠0, после `sdd-log deviation-verdict … accepted` → exit 0); групповой гейт `sdd-check --all <fixture>` не закрывает группу с неразобранной записью | **V14-2a**, **V14-2b**, **V14-2c**, **V14-2d**, **V14-2e** | **D-49**, **L-16** |

## 3. Что НЕ входит в приёмку (зафиксировано)

- agent-inbox из `sdd-v2-inbox-transplant` (D-2); messenger как обязательный снапшот G4 (D-29); python-rules до первого проекта (D-25); **python-пресет `V-10` целиком — за `2.0.0-draft.N` (D-44)**: порядок стеков исполняется как anystack → golang → swift, `A6` python не перечисляет; **вместе с `V-10` за релиз уходит и python-часть `SO-13`** — корень `plugins/python` в sync и портированные python-кейсы `sync-skills-core.test.ts` (правка V-61b: релизная `SO-13` покрывает только `plugins/{anystack,golang,swift}`, `61 §2.2` п.13); полная хэш-модель владения SO-3 (D-22, после релиза); `gennady fix` (D-13, ждёт O-2); слой правил `RULES-D1..RULES-D6` и `T-2` (указатель на `RULES-D2`, D-45; D-33 — пересмотр до постановки) вместе с зависящей от них **`T-8`**; LLM-точки G1/G2 как бар (D-28: судья — диагностика, L-14), в том числе **`E-13`** — диагностика, а не бар (D-45).
- **`FO-7`** — кэш resolver-а владения: решение принимается **только по измерениям** и **после релиза** (D-40).
- **`IDN-3`, `IDN-4`, `IDN-5`, `IDN-6`** — полная политика идентичности (repo-wide проверка, Decision Move Map, применение карты через `id-replace.ts`, consumer sweep) — после релиза (D-41). В релиз входит только `IDN-1` + `IDN-2` + `IDN-2a`.
- WARN→ERROR групповых квитанций **до** A1/A2 (D-4) — но после A1/A2 переключение обязательно и вынесено в отдельный критерий **A13**.
- **Формулировка X-9 (правка 63, 06 §3.10): v1 удаляется как ИНСТРУМЕНТАРИЙ; корпус данных v1 сохраняется как миграционный вход** — frozen-копия снимается `E-22` **до** удаления `tasks/` (`E-20`), иначе слой A приёмки теряет исходный корпус безвозвратно.
- **Research-only темы (правка 63 + `GAP-P-2`, проверяется `A23`).** Ни одна из них не имеет решения сегодня, ни одна не входит в `2.0.0-draft.N`, и ни один из этих id не должен встречаться в волнах или в исполнителях критериев:
  - **T3** — property-based testing и мутационное тестирование как бар качества: решения нет, ROI не доказан;
  - **T5** — transitive impact analysis (кто ломается при изменении спеки): механизма нет, только идея;
  - **T6** — типизация рёбер графа зависимостей: требует canonical machine-readable модели, а план стоит на стороне R1 (Markdown как SSOT) — выбор не доказан, а обоснован (06 §6 п.11);
  - **T7** — `sdd.req_id` / OpenTelemetry-трассировка требований до рантайма (R2 №13): решения нет;
  - **T9** — персистентные допущения (assumption ledger, переживающий сессию): решения нет.
- **Полезность самого SDD v2 не входит в приёмку и признана недоказанной** (06 §6 п.12): корпус не отвечает, насколько v2 снижает defect rate, rework, time-to-change и context cost; ни одна запись adversarial-матрицы не имеет уровня применимости `A0`. Приёмка доказывает, что **план исполнен**, а не что **цели верны**. Единственное известное лекарство против composition fallacy (06 §6 п.13) — preregistered pilot со stop-criterion, и он документный (`GAP-P-1`), а не измеряющий.

## 4. Открытые пункты приёмки

- Параллель задач в одном дереве (D-26, O-4) — критерий появится после eval-исследования (T-B6-04 + V-18 одним брифом).
- Поведение первого v2-sync над v1-деревом потребителя (D-23, O-3) — критерий формулируется при постановке SO-12.
- **Слой правил: D-24 замещён D-31 (целевое видение — динамический CLI-справочник; в релиз — режим (A)), D-32 (авто-распознавание legacy) и D-33 (весь блок пересматривается перед постановкой задач трека 33 / RULES-D*).** Критерий A8 зафиксирован для релизного режима (A); критерий для инструмента `sdd-rules` появится после пересмотра по D-33 (материалы — `33a-RULES-EXPLAINER.md`, `33b-RULES-VARIANTS.md`).
- **O-2 (`fix` — глагол или флаг):** A5 транзитивно зависела от V-16 → V-17; зависимость сужена до **V-16a** (read-only, входит в релиз по D-13), а `gennady fix` (V-16b) вынесен за релиз. Пока O-2 открыт, положительного критерия на `fix` нет — только отрицательный замок в A6.
- **D-19 vs И-2 (`[gate] `-строки в квитанции):** подпункт A6 заморожен до решения Lead/оператора (`61-TASK-BOARD.md` §2.2 п.6). До решения бриф V-14 не выдаётся.
- **O-6 (WARN→ERROR групповых квитанций):** решение D-4 принято, критерий A13 заведён; открыт только момент переключения — по факту прохождения A1/A2.
- **Релизность функционального регресса S2** (`resolvePackageDir`, SO-8) — требуется решение оператора; в критерии A9/A4 пока не входит.
- **Закрыто гейтом адекватности (D-38..D-46) — пунктов больше нет:** `O-10` (ID в подписях диаграмм → **D-43**, критерий `A20`), `O-11` (идентичность акронима → **D-41**, критерий `A16`), `O-11b` (перенос решений → **D-42**, `IDN-2`), `O-12` (шапка владения файла → **D-40**, критерий `A17`), `O-13` (python-пресет → **D-44**, правки `A6`/`A3`), `O-16` (срез RC и стартовое условие → **D-38**, критерий `A18`), `O-17` (совместимость с v1 → **D-39**, `B2-21` отменена, `A1` стоит на `GAP-B-1`). `O-14` и `O-15` сняты верификацией **V-06-GAP** как дубли уже принятых решений (D-28/`60 §3` и `61:635`/D-36).
- **Остаётся открытым (не решается этим документом):** **компрессия Decision Log при миграции** — замечание оператора, оформленное как `O-18` в `00-INDEX.md` §4: дизайн-вопрос **после релиза**, решения нет, критерия приёмки нет.
- **Честный остаток приёмки (06 §6, подтверждён V-06-GAP):** три группы утверждений не проверены исполнением и не могут быть проверены чтением — прогоны main-side («198/198» по тестам, `verify --plan --json`), живой LLM-контур (`R-COMPLETE` **ни разу** не давал pass на живом прогоне) и реальный масштаб дефекта `events` (нужны два прогона: с подключённым `readEvents` и без). До этих прогонов соответствующие подпункты `A6`/`A10` считаются **предсказанием**, а не доказательством.
