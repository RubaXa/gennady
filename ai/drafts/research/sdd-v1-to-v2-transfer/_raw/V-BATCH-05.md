ВЕРИФИКАЦИЯ — Пачка 5 «Висячие ссылки в директивах ловятся сборкой, а не глазами»

Верификатор: `plan-verifier` (свежие глаза, только чтение). Дерево `rc-w2`, ветка `lead/kit-lint`,
HEAD `61b86fb8`, база `f16d7f17` (= `origin/lead/release-package`, PR #28). Все команды перезапущены
мной, числа пересчитаны скриптами, не на глаз. Проверяемые документы: `R-BATCH-05-kit-lint.md` +
8 × `R-T-B6-*.md`; строки доски `61-TASK-BOARD.md:145-170`; трек `40-TRACK-DIRECTIVES-SKILLS.md` §4.1.

---

## A. Полнота

| Утверждение | Команда/строка | Вердикт |
|---|---|---|
| 61 файл, +936/−477 | `git diff --shortstat f16d7f17..61b86fb8` → `61 files changed, 936 insertions(+), 477 deletions(-)` | **ПОДТВЕРЖДЕНО** дословно |
| 8 коммитов, SHA и порядок | `git log --oneline f16d7f17..HEAD` — все 8 SHA и темы совпадают | **ПОДТВЕРЖДЕНО** |
| Пофайловые счётчики коммитов | `git show --stat` → 4, 4, 3, 11, 25, 15, 3, 20 — совпало со всеми 8 отчётами | **ПОДТВЕРЖДЕНО** |
| Каждый файл имеет строку в таблице | Сверка `--name-only` с §1 всех отчётов; группировки явные и покрывают пути | **ПОДТВЕРЖДЕНО** |

**Разбиение 61 файла:** **19** — сгенерированные директивы `ai/directives/sdd-v2/**` (+171/−316);
**42** — исходники (17 `.hbs`, 8 файлов библиотеки аксиомов из них 3 новых, 5 kit-скриптов, 7
kit-тестов из них 4 новых, 2 golden-теста инспектора, 1 снапшот `services/ai-kit`, `AGENTS.md`,
`ai/kit/AUTHORING.md`).

**Зоны пачек 1/4.** Grep по `cli/cmd/sync*`, `shared/sdd/check.ts`, `templates.ts`,
`ax-reopen-format.xml`, `audit/steps/*`, `task-ticket-structure.xml`, `tasks/**`, `specs/**` в
`git diff --name-only f16d7f17..61b86fb8` → **0 совпадений**. **ПОДТВЕРЖДЕНО.** Уточнение, которого
нет в отчётах: по коммитам затронуто **62** уникальных файла — лишний
`ai/directives/sdd-v2/audit/steps/STEP_1_MECHANICAL.xml` (изменён в `b6eb6f4b`, возвращён в
`61b86fb8`, net = 0). Формулировку сводки стоит уточнить до «net по диапазону». Неблокирующее.

---

## B. Существо по каждой задаче против строки доски

**T-B6-09 (`:146`) — kit-скрипты не зависят от cwd; приёмка «3 теста §0 из чужого cwd».**
Перезапущено мной из чужого cwd (`…/scratchpad/foreign`, бинарь из `rc-w2`): `build-directives.test.ts`
6/6, `delta-assembly.test.ts` 13/13, `skeleton-package-binding.guard.test.ts` 4/4, exit 0 каждый;
`cwd-independence.test.ts` 3/3. Файлы — ровно `lazy-assembly.ts`, `render.ts`, `step-budget-gate.ts`
из строки доски. Корень бага виден дословно: `DEFAULT_ASSEMBLY_MANIFEST_PATH =
'ai/kit/assembly-manifest.json'` → `join(KIT, …)`. **ПОДТВЕРЖДЕНО**, без расширения смысла.

**T-B6-08 (`:145`) — висячая ссылка роняет сборку, allowlist по L-10.**
- Гейт в конвейере: `build-directives.ts:166-175` — `lintUndefinedAxiomRefs(rendered, {allowlist})`,
  `console.error` + `process.exit(1)` ДО записи файлов. **ПОДТВЕРЖДЕНО** по коду.
- **Красный на синтетической висячей ссылке** — проверено без правки дерева, прямым вызовом
  экспортированной функции: вход `AX_REAL` (определён) + `AX_TOTALLY_MADE_UP` (нет) →
  `[{file:'sdd-v2/x.directive.xml', id:'AX_TOTALLY_MADE_UP'}]`; тот же вход без выдуманного id →
  `[]`. **ПОДТВЕРЖДЕНО.** **Зелёный на дереве:** `build:directives` внутри `audit:sdd-templates` —
  exit 0, строки `✗ … undefined axiom reference(s)` нет. **ПОДТВЕРЖДЕНО.**
- **Allowlist:** пересчёт по `KNOWN_DANGLING_AXIOM_REFS` → `42 пары / 20 уникальных id`.
  **ПОДТВЕРЖДЕНО.**
- **Совпадение с baseline §4.1 — частично.** Набор 20 id совпадает с таблицей §4.1 поэлементно
  (сверил все 20). Но «`files scanned: 55 … mentioned ids: 174` — совпадает до элемента» неверно: в
  §4.1 стоит `files scanned: 70 | mentioned ids: 184`. Мой прогон линта по 70 файлам на диске даёт
  `43 пары / 20 id` — ровно ручной подсчёт по §4.1. Причина: линт видит МОНОЛИТНЫЙ пре-delta рендер
  (55 файлов), §4.1 и диск — расщеплённые lazy-скелеты и шаги (70). Предмет тот же, атрибуция путей
  разная. **ЧАСТИЧНО ОПРОВЕРГНУТО**; правка формулировки в `R-T-B6-08.md` §1. Неблокирующее.

*Риск «allowlist легализует долг» — приемлемо, с одной дырой.* В плюс: L-10
(`02-LEAD-DECISIONS.md:16`) прямо санкционирует вариант (b); гейт роняет билд на ЛЮБОЙ новой ссылке
(проверено); записи поимённые. В минус: владелец назван у **10** id из 20 (`T-B6-25`, `T-B6-17`,
`T-B6-02`×4, `ISS-8/T-B6-12`, `T-B6-13`, `T-B6-24`, V14-2a), остальные **10** — `unassigned`, без
задачи и срока. Правило «список только сокращается» живёт **только комментарием**
(`lint-axioms.ts:186-193`, `AUTHORING.md` §7): `lint-axioms.test.ts` (21/21) проверяет
accept/reject, но НЕ проверяет, что размер не вырос. Итог по критерию «висячая ссылка = ошибка
сборки»: для новых — да, замок реален; 20 унаследованных — зафиксированный поимённый долг.
Правка (неблокирующая): кейс `assert.ok(KNOWN_DANGLING_AXIOM_REFS.size <= 42)` + владельцы на 10 id.

**T-B6-21 (`:165`) — `critic-protocol` перестаёт быть сиротой.**
`review-lifecycle.directive.hbs` STEP_2 теперь открывается «Load `critic-protocol.directive.xml`,
then dispatch one fresh reviewer with its full contents as the reviewer's own governing
instructions»; `READ_AND_USE_DIRECTIVE` намеренно НЕ использован (иначе рвётся существующий
охранный кейс про class-3). `delta-assembly.test.ts` 13/13. **ПОДТВЕРЖДЕНО.** Замечание: критерий
нового кейса — подстрочное вхождение имени файла в чужой рендер, т.е. любое случайное упоминание
удовлетворит инвариант; замок слабее, чем звучит. Неблокирующее.

**T-B6-22 (`:166`) — баг роутинга. Подтверждён по коду до/после и golden.**
ДО (`router.directive.hbs:127` @ `f16d7f17`): `WHEN intent = recover-from-code ->
READ_AND_USE_DIRECTIVE(".../discover-from-code.directive.xml")`; ПОСЛЕ — `.../recover-from-code.directive.xml`;
одна строка, идентично в `router.directive.xml:387`. Что это разные флоу — из собственного текста
`recover-from-code.directive.hbs:9-13` («one path, portal already live» против «WHOLE project,
portal absent»). `discover-from-code` остался достижим: единственная ссылка на него —
`root.directive.hbs:75`, не тронута (grep по шаблонам). Golden-тесты `parse-directive.test.ts:183`
и `debug.test.ts:163` поправлены точечно, по одной строке ожидаемого списка веток — вне списка
«Файлы» доски, но без этого коммит не проходит `pre-commit`. **ПОДТВЕРЖДЕНО.**

**T-B6-18 (`:162`) — 7 расхождений инлайн-аксиом.**
`axiom-home.test.ts` прочитан и прогнан (2/2); держит два инварианта: id не определён одновременно
инлайн и файлом библиотеки; id не инлайнен независимо в ≥2 шаблонах. Проверка механическая по
живому дереву, не по фикстуре. Мой независимый скан: inline-id в шаблонах — **57**, из них в ≥2
шаблонах — **0**; нарушений обоих инвариантов — **0**. **ПОДТВЕРЖДЕНО: закрыты все 7, остаток 0** —
с оговоркой, что инвариант узкий: 57 одиночных инлайнов он разрешает, а 319 файлов библиотеки, ни
разу не подключённых через `{{> }}`, не видит (предмет T-B6-02/11/20, не этой задачи).

**T-B6-26 (`:170`) — `AX_PROGRESSIVE_DISCLOSURE`.**
`git grep '<Axiom id="AX_PROGRESSIVE_DISCLOSURE"' f16d7f17 -- ai/kit/templates` → **0 совпадений**;
на базе партиал уже подключался в 8 шаблонах. Половина находки доски («7 инлайн-копий») была
устаревшей — исполнитель это указал явно, не скрыл. Вторая половина подтвердилась: 7 новых `{{> }}`,
партиал теперь в 15 шаблонах. `deps.test.ts` 28/28, `REQUIRED_CONDUCT` + `OPERATOR_FACING_OWNERS`
(14 директив) явные, как требовала приёмка. **ПОДТВЕРЖДЕНО.**

**T-B6-15 (`:157`) — D2 в `AGENTS.md`.**
Новый `## Directive markup — mandatory` стоит между `# AGENTS — Router` и `## Project description`,
т.е. первым содержательным разделом. Файлы — ровно `AGENTS.md` + `ai/kit/AUTHORING.md` из строки
доски плюс новый тест; `directive-markup-contract.test.ts` 5/5 (порядок заголовков, «not XML»,
`xmllint`, отсутствие `xmllint` в `package.json` и `scripts/**`). **ПОДТВЕРЖДЕНО**, строго по строке.

**T-B6-23 (`:167`) — трёхчленный триаж критика; вопрос «строго ли по строке доски».**
Строка называет `critic-protocol.directive.hbs` и `ax-confusion-bug.xml`; изменены ещё
`ax-isolation.xml` и `ax-uncertainty-is-signal.xml` — расширение **списка файлов**. Проверил
обоснование: grep по шаблонам — все три партиала подключает **только** `critic-protocol.directive.hbs`
(0 других потребителей); оба удалённых утверждения — дословно та же инверсия D3.4 («Confusion →
underspecification, not a question to ask» / «Uncertainty = underspecification»). Без их правки
директива осталась бы самопротиворечивой. **Смысл задачи не расширен**, расширен минимально
необходимый список файлов. `critic-confusion-triage.test.ts` 5/5; `AX_CONFUSION_BUG` активирован в
`STEP_2_JUDGE`. **ПОДТВЕРЖДЕНО с раскрытой оговоркой по объёму.**
*Находка верификатора:* та же инверсия жива в ЧЕТВЁРТОМ месте —
`ai/kit/axiom/boundary/ax-isolation-signal.xml:2`: «Confusion = underspecification. Fix the artifact
bundle.» Файл — сирота (0 подключений, id нигде не упоминается), поэтому агент его не читает и тест
его не проверяет; но при будущем подключении инверсия вернётся молча. Правка — отдельной задачей.

---

## C. Побочный фикс рендерера

**Формулировка задания неточна: `normalizeBrick()` НЕ исправлялся.** Единственное изменение
`ai/kit/render.ts` в диапазоне — новый экспорт `PROJECT_ROOT` (T-B6-09, +6 строк). Баг жив сейчас,
`render.ts:34`: `while (lines.length && lines[0].trim().startsWith('<!--')) lines = lines.slice(1);`
— снимается только ПЕРВАЯ строка многострочного комментария, строки 2..N утекают в тело рендера.
**ПОДТВЕРЖДЕНО** (баг существует), **ОПРОВЕРГНУТО** (что он «исправлен фиксом рендерера»).

**Изменение поведения генерации и его размер.** Внесено `b6eb6f4b` (T-B6-18): 5 файлов библиотеки
получили многострочные ведущие комментарии → утечка. Доказано на промежуточном коммите:
`git show b6eb6f4b:…/audit.directive.xml` содержит на строках 133 и 178 обрывки комментария
(«copy (T-B6-18) — the inline copy had evolved…», «… merged the three finding types…») ВНУТРИ тел
аксиомов. Снято `61b86fb8` — сплющиванием 8 комментариев в одну строку, не правкой рендерера.
Затронутые сгенерированные файлы: **8** (`agent-inbox/{code-lens,security-lens,synthesize,track-review}`,
`audit.directive.xml`, `audit/steps/STEP_1_MECHANICAL.xml`, `infra.directive.xml`,
`interface.directive.xml`), в этом коммите по ним **96 вставок / 136 удалений**.
**Net по диапазону — нуль утечки:** `grep -rn "T-B6-18\|converged to\|reconciled with"
ai/directives/` → 0 совпадений; `STEP_1_MECHANICAL.xml` net-неизменён; `check:directives-fresh`
exit 0. Мой скан: файлов библиотеки аксиомов с многострочным ведущим комментарием — **0**; 5 таких
комментариев остались только в `.hbs`-шаблонах верхнего уровня, к которым `normalizeBrick()` не
применяется (проверено: комментарий `code-lens.directive.hbs:1-5` попадает в `code-lens.directive.xml:1-5`
целиком, без утечки).

**Оценка: безвредно по итогу; требует отдельной задачи; откатывать нечего.** Скрытых изменений
смысла директив нет (net = 0, доказано grep'ом и freshness-гейтом). Но латентный баг — ловушка:
следующий автор двухстрочного комментария в кирпиче аксиомы молча испортит промпт, который читает
агент, и это не поймает ни один тест, ни `check:directives-fresh`, ни `audit:*` — доказано тем, что
`b6eb6f4b` прошёл весь `pre-commit` зелёным с утечкой в 9 файлах. Правка: снимать комментарий
целиком до `-->` в `normalizeBrick()` + кейс «в теле аксиома нет продолжения комментария».

---

## D. Смысловые изменения директив (`git diff … -- ai/directives ai/kit/templates` прочитан)

| Что изменилось по смыслу | Где | Задача |
|---|---|---|
| Интент `recover-from-code` ведёт в одноимённый файл вместо `discover-from-code` | `router` | T-B6-22 |
| STEP_2 велит загрузить `critic-protocol.directive.xml` как инструкции ревьюера | `review-lifecycle` | T-B6-21 |
| Критик обязан триажировать непонимание (`ARTIFACT_GAP`/`CONTEXT_MISSING`/`NON_BLOCKING_QUESTION`), только `ARTIFACT_GAP` → находка; 2 инвертированных утверждения удалены | `critic-protocol` | T-B6-23 |
| Добавлено правило «решение первым, обоснование ниже» | 7 директив (`scope, module, scaffold, execute, critic, audit, code-review`) | T-B6-26 |
| `AX_FINDING_ROUTING`: 3 новых типа находок И закрывающий абзац «spec edit ⇒ authoring flow + independent review + approval #1» — **единственное содержательное ДОБАВЛЕНИЕ смысла**: абзац жил только в неподключённом библиотечном файле, теперь читается агентом; раскрыто в `R-T-B6-18.md` §1 | `audit` | T-B6-18 |
| `AX_MECHANICAL_VIA_SDD_CHECK`: устаревшая библиотечная копия заменена актуальным инлайн-текстом — net по сгенерированному выводу = 0 | `audit` | T-B6-18 |
| `AX_NO_DUPLICATION`: 4 копии → партиал, `AX_TICKET_DEDUPLICATION` из вложенного стал соседним; лёгкое расширение — каждая из 4 директив теперь несёт ОБА применения (код-символы + reconciliation), раньше по одному; раскрыто | 4 × `agent-inbox` | T-B6-18 |
| `AX_OPERATOR_LANGUAGE` (2 копии agent-inbox) → новый id `AX_REVIEW_REPORT_LANGUAGE`, 3 ссылки переименованы; репозиторный `AX_OPERATOR_LANGUAGE` (10 потребителей) не тронут | `synthesize, track-review, security-lens` | T-B6-18 |
| `AX_SPEC_MANDATORY_DIAGRAM`: 2 копии слиты, ОБА примера сохранены; добавлена активация в шаге записи спеки (следствие «инлайн → партиал», гейт `audit:axioms`) | `infra, interface` | T-B6-18 |
| `AX_EVIDENCE_HYGIENE`: инлайн → общий партиал, специфика флоу сохранена прозой рядом (тексты сверил) | `recover-from-code` | T-B6-18 |
| «`ai/directives/**/*.xml` — не XML, `xmllint` запрещён» первым разделом + кросс-ссылка | `AGENTS.md`, `AUTHORING.md` §1 | T-B6-15 |
| Обратная проверка линта задокументирована | `AUTHORING.md` §7 | T-B6-08 |
| −257 строк: тела унаследованных аксиомов заменены строкой «Inherited from the loading directive (already in context): …» | 6 сгенерированных | побочный эффект T-B6-22 |

**Про −257 строк.** `delta-assembly.ts:23-27`: `ctx(n)` = пересечение по ВСЕМ входящим рёбрам от
`(ctx(parent) ∪ partials_ORIGINAL(parent))`. До фикса `recover-from-code` имел 0 входящих рёбер ⇒
`ctx = ∅` ⇒ пересечение у его детей (в т.ч. `scaffold`) было бедным; после фикса `ctx` непустой ⇒
вычитание законно шире. `discover-from-code` симметрично потерял одно из двух рёбер. Это штатный
пересчёт фикспоинта, подтверждённый `check:directives-fresh` (exit 0), а не потеря содержания:
каждый вычтенный партиал перечислен в строке «Inherited …» и по определению пересечения гарантированно
присутствует у любой загружающей директивы. **ПОДТВЕРЖДЕНО.**

**Итог D:** кроме перечисленных в задачах правок смысл директив не менялся. Единственная добавка,
формально выходящая за «перенести дом аксиомы» — закрывающий абзац `AX_FINDING_ROUTING` в
`audit.directive.xml`; она раскрыта исполнителем и согласуется с корпусом. Скрытых изменений нет.

---

## E. Регрессии — перезапущено мной, всё exit 0

| Команда | Мой результат | Заявлено | Вердикт |
|---|---|---|---|
| `npm test` | `# tests 3605 # pass 3595 # fail 0 # skipped 10` | 3595/0/10 | **ПОДТВЕРЖДЕНО** |
| `npm run check` | `✅ ALL PASS (5/5)` — type-check 4.5s · test:coverage 71.0s · lint 10.7s · format 2.1s · yagni 0.9s | 5/5 | **ПОДТВЕРЖДЕНО** |
| `npm run build` | `✓ built in 3.50s` | собрано | **ПОДТВЕРЖДЕНО** |
| `npm run gate:sdd-check-baseline` | `OK — no error outside the baseline (baseline 227c03a8, tag rc-baseline-1)` | то же | **ПОДТВЕРЖДЕНО** |
| `npm run audit:sdd-templates` | fresh + axioms (28 шаблонов) + contracts (28+33) + halts (33+33) + budgets — clean | clean | **ПОДТВЕРЖДЕНО** |
| `npm run check:directives-fresh` | `✓ ai/directives/** matches a fresh rebuild.` | то же | **ПОДТВЕРЖДЕНО** |
| Пофайлово: `lint-axioms` 21/21 · `axiom-home` 2/2 · `critic-confusion-triage` 5/5 · `directive-markup-contract` 5/5 · `deps` 28/28 · `cwd-independence` 3/3 · `delta-assembly` 13/13 | все exit 0 | те же числа | **ПОДТВЕРЖДЕНО** (7 из 7 дословно) |

---

## F. Конфликты и порядок мержа

**Пачка 1 (`lead/sync-no-loss`, PR #29, база `2acbe682`, 15 файлов).** `comm -12` пересечения имён →
**0 общих файлов** (пачка 1 — `cli/cmd/sync*`, `cli/cmd/sdd-check`, `shared/sdd/check.ts`,
`specs/cli/**`; пачка 5 — `ai/kit/**`, `ai/directives/**`, `ai/inspector/**`). **ПОДТВЕРЖДЕНО, мерж
в любом порядке.**

**Пачка 4 (`lead/specs-match-code`, ветка существует локально, дерево `rc-v6`, HEAD `585537ac`,
база — тот же `f16d7f17`, 38 файлов).** Пересечение — **3 файла**, из них сгенерированный только 1:
`ai/directives/sdd-v2/scaffold.directive.xml` (авто-мерж проходит, всё равно перегенерируется),
`ai/kit/axiom/audit/ax-mechanical-via-sdd-check.xml` — **КОНФЛИКТ**,
`ai/kit/templates/sdd-v2/audit.directive.hbs` — **КОНФЛИКТ**
(`git merge-tree --write-tree lead/kit-lint lead/specs-match-code` → два `CONFLICT (content)`).
**ОПРОВЕРГНУТО** утверждение сводки «пересечения файлов нет» — оно верно только про пачку 1.

Конфликт семантический: две пачки лечат один дефект противоположно. Пачка 4 (`B2-11`) оставила
инлайн-копию в `audit.directive.hbs` живой, добавила в библиотечный файл `NOTE (B2-11)` «not
included as a partial by any current .hbs … Kept byte-consistent with the live copy **by hand**» и
обновила ОБЕ копии, добавив `SDD_TASK_ID_GRAMMAR`, `BDD_NEGATIVE`, `BDD_TRACE`, `COVERAGE_POLICY`,
`PHASE_RECEIPT`, group receipts. Пачка 5 (`T-B6-18`) инлайн удалила и подключила библиотечный файл
партиалом.

**Порядок и способ мержа.** 1) PR #28 (`lead/release-package`) — база обеих, после мержа ребейзнуть
`lead/kit-lint` (diff сократится до 8 коммитов). 2) `lead/sync-no-loss` — независим, в любой момент.
3) **`lead/kit-lint` раньше `lead/specs-match-code`**; при мерже пачки 4 брать **структуру пачки 5**
(инлайн удалён, `{{> "axiom/audit/ax-mechanical-via-sdd-check"}}`) и **содержание пачки 4** (все
новые проверки) — переносить их в единственный библиотечный файл, удалив устаревший `NOTE (B2-11)`;
затем `build:directives` + `check:directives-fresh`. Опасность наивного резолва: содержание пачки 4
(6 новых механических проверок в тексте, который читает audit-агент) теряется молча — freshness-гейт
сверяет вывод с шаблонами, а не с чужой веткой. Обратный порядок допустим при том же резолве;
«оставить обе копии» нельзя — это вернёт дефект T-B6-18.

---

## Итог

**Блокирующее: нет.**

**Неблокирующее (5, по убыванию важности):**
1. **Конфликт с пачкой 4** (§F) по `ax-mechanical-via-sdd-check.xml` и `audit.directive.hbs` — не
   дефект работы, но требует названного способа резолва до мержа; формулировку сводки исправить на
   «с пачкой 1 пересечения нет; с пачкой 4 — 3 файла, 2 конфликта».
2. **Латентный `normalizeBrick()`** (§C) — отдельная задача: снимать комментарий целиком до `-->` +
   кейс на отсутствие продолжения комментария в теле аксиома. Класс порчи промпта не ловит ни один
   существующий гейт.
3. **`ax-isolation-signal.xml`** (§B, T-B6-23) — четвёртая, неисправленная копия инверсии D3.4 в
   файле-сироте; привести к `AX_CONFUSION_BUG` или удалить.
4. **«Allowlist только сокращается» не механизирован** (§B, T-B6-08) — добавить кейс на размер
   `KNOWN_DANGLING_AXIOM_REFS`; завести владельцев на 10 `unassigned` id из 20.
5. **Две неточности формулировок:** «совпадает до элемента» с §4.1 в `R-T-B6-08.md` (совпадают 20
   id, но не `files scanned`/`mentioned ids`) и «ни один файл зон не тронут» в сводке (net по
   диапазону — да; промежуточный `b6eb6f4b` трогает `audit/steps/STEP_1_MECHANICAL.xml`).

**Подтверждено:** полнота (61 файл = 19 сгенерированных + 42 исходника, все в таблицах, зоны пачек
1/4 net-нетронуты); существо всех **8** задач против строк доски `61-TASK-BOARD.md:145-170`, включая
реальность бага роутинга T-B6-22 и реальную красноту нового линта на висячей ссылке; отсутствие
скрытых изменений смысла директив (net-утечка комментариев = 0); все 6 гейтов и 7 тестовых файлов
перезапущены мной с exit 0 и числами, совпадающими с заявленными дословно. Два раскрытых расширения
объёма (T-B6-23 — 2 доп. файла аксиомов; T-B6-22 — 2 golden-теста) — необходимые следствия, не
разрастание.

**Рекомендация: принять пачку.** Порядок мержа — PR #28 → ребейз → `lead/kit-lint` →
`lead/sync-no-loss` / `lead/specs-match-code` с резолвом по §F. Пункты 2–4 завести отдельными
задачами (2 и 3 — в трек T-B6, 4 — к владельцу L-10).
