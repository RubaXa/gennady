# Финальный отчёт Codex-сессии «SDD v2 // RC v3» после стоп-кадра (2026-09-02 ~09:40 MSK)

Статус: сессия остановлена по стоп-кадру оператора. WIP закоммичен: `61ecb330` «wip(sdd): night iteration snapshot», дерево чистое, dbc-service-format.xml включён, tmp-сценарий удалён, pre-commit 5/5, OpenCode-сервер остановлен. (Проверено ведущим независимо: подтверждается.)

Ниже — отчёт сессии дословно, как выдан оператору.

---
Сейчас тестировать не нужно. Стоп-кадр выполнен, новых прогонов после него не было.

## Состояние

- Коммит: `61ecb3302562c90a00986b4d7e3597df3ae8e608`
- Сообщение: `wip(sdd): night iteration snapshot`
- Рабочее дерево чистое.
- `ai/kit/contract/spec/dbc-service-format.xml` включён.
- Временный `ai/flow-eval/scenarios.authoring.tmp.json` удалён.
- Обязательная проверка прошла 5/5.
- Фоновый сервер OpenCode остановлен.

## Корень провала draft.61

Утверждённые спецификации → в спецификациях не определено владение инфраструктурой → scaffold мысленно придумал порядок работ → Gate 1 утвердил этот мысленный план → только после утверждения создана первая настоящая задача → sdd-check обнаружил противоречия → агент начал чинить формулировки задачи и искать примеры → цель потеряна, оператор остановил прогон.

Главная проблема находится не в конкретной ошибке TodoMVC и не в «глупости» модели.

- [Confirmed] Scaffold разрешал утверждать ещё не материализованный план. Проверка запускалась слишком поздно — после Gate 1.
- [Confirmed] Спецификации не назначили владельца базовой инфраструктуры: runtime, пакетного менеджера, конфигурации и порядка установки зависимостей.
- [Confirmed] Агент сам придумал одну широкую задачу `IB-setup`, а затем проверяющий инструмент отверг часть придуманного формата.
- [Confirmed] После ошибки отсутствовала команда «вернуться к декомпозиции». Агент начал локально переписывать задачу.

Точная цепочка draft.61: router → sdd-state → scaffold intake → чтение scope/modules → пустые правила infra-base → мысленная декомпозиция → Gate 1 → sdd-new task IB-setup → sdd-check --task --authoring → ошибки формата/покрытия/capability → поиск примеров и локальная правка → STOP.

## Сопоставление с предыдущими прогонами

| Прогон | Что улучшилось | Где снова сломалось |
|---|---|---|
| draft.52 | Качественные scope/module-спеки | Выполнение развалилось на проверках |
| draft.55 | Scaffold дошёл до семи задач | Много раундов критика; переход в execute противоречив |
| draft.57/58 | Новые подсказки и маршрутизация | Вернулись поиски путей и непонимание скелета |
| draft.60 | Повторное использование критика очистило задачи | Execute заблокирован инфраструктурной политикой npm |
| draft.61 | Router правильно выбрал scaffold | Gate 1 снова пропустил непроверенную декомпозицию |
| final19 | Фактически созданы обе спеки | Ложный провал харнесса: session.diff не увидел untracked |
| final20 | Обе спеки заполнены, критик нашёл 2 реальные смысловые ошибки | Не уложился во временной бюджет; успехом не считается |

[Confirmed] Улучшения были, но не складывались в устойчивую цепочку от спецификаций до выполнения.

## Вердикт по решениям a77fbcf1

| Решение | Вердикт | Что делать |
|---|---|---|
| Capability adapters | Частично; сработали поздно, отвлекли от причины | Убрать обязательные машинные adapter-поля; оставить смысловую проверку провайдера |
| Scaffold feasibility/preflight | Не активировался в нужной точке | Проверять реальные черновики задач до Gate 1 |
| Phase verification plan | Помогает execute, не чинит scaffold | Сохранить |
| Worker checkpoint/replan | Не сработал; добавил сложность | Упразднить постоянные checkpoint/JSON-состояния |
| Свежесть собранных директив | Полезна | Сохранить проверку свежести, без sidecar-маршрутизации |
| Regression harness | Частично полезен, маскировал живые провалы | Перестроить вокруг полной последовательности и реальных файлов |
| Повторное использование критика | Помогло draft.60, но дорого и запутанно | Один короткий независимый смысловой просмотр |

## Почему зелёные тесты не поймали провал

Тестировались отдельные функции на заранее корректных задачах. Не тестировалась последовательность: спеки → декомпозиция → реальные задачи → проверка → утверждение → выполнение.

Отсутствовавшие инварианты: Gate 1 нельзя показывать до создания и проверки реальных задач; инфраструктурный провайдер предшествует потребителям; runtime/пакетный менеджер определены до установки зависимостей; общие файлы без параллельных владельцев; негативные сценарии обязательны; ID требований в задачах и тестах; заполненная спека без подсказок скелета; харнесс учитывает untracked; тайм-лимит без лишнего интервала после последнего наблюдения.

Pre-commit 5/5 доказывает внутреннюю целостность кода, но не проходимость живого процесса моделью.

## Принятые выводы внешнего аудита

- [Confirmed] ~70% чтения — директивы и маршрутизация, форматы ~23%. Главный резерв — сокращение директив.
- [Confirmed] Скелет sdd-new уже совпадает со структурным форматом. Подсказки — внутрь секций как удаляемые комментарии.
- [Likely] Большие примеры вредят дешёвым моделям (copy-bias). Нужны минимальные примеры.

## План до RC (предложение сессии)

1. Сократить authoring-директиву до маршрута: что создать, чем проверить, когда остановиться.
2. Перенести подсказки скелета в удаляемые комментарии.
3. Мягкий режим `sdd-check --spec <path> --authoring`: секции, маркеры, негативные сценарии, ID требований; в черновике — подсказки, не блокирующие ошибки.
4. Создавать реальные черновики задач до Gate 1.
5. Один независимый смысловой просмотр согласованности и порядка инфраструктуры.
6. Обобщённый интеллектуальный тест полной последовательности.
7. После зелёных локальных тестов — ровно один authoring-прогон на deepseek-v4-flash.
8. После успешного authoring — заново TodoMVC: authoring с нуля → scaffold → execute.

## Непроверенное

- [Unknown] Пакет исправлений 05:48 не проверен как единое целое; final20 — по тайм-лимиту, успехом не считается.
- [Unknown] TodoMVC не перетестирован.
- [Unknown] Полная цепочка authoring → scaffold → execute не пройдена.
- [Confirmed] WIP-коммит — безопасная точка восстановления, не RC.

Итог сессии: RC отклонён. Следующий шаг — согласованный пакет из коротких директив, мягкого authoring-receipt и проверки реальных задач до Gate 1.
