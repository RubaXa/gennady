# Adversarial Audit — Codex-сессия «SDD v2 // RC v3»

**База:** pack-audit.json (13 797 строк транскрипта, 64.7 MB, 0 malformed; omitted: 60 narrative, 1752 actions, 2009 tool results) + read-only git по worktree `/Users/k.lebedev/Developer/gennady/.claude/worktrees/sdd-v2-rc52-followup`. Окно сессии: 2026-09-01 18:01Z → 2026-09-02 06:03Z (~12 ч), сессия на момент снятия пака ещё активна.

## 1. Противоречия и незакрытые completion claims

- **[Confirmed] «Полностью подтверждён» ≠ финал.** 00:18Z (line 6639): «Финальный scaffold полностью подтверждён: судья pass, 3359 тестов, 0 ошибок» → 03:21Z (line 10741): scaffold «снова начал слишком долго разбираться в наследовании правил»; 03:26Z (line 10885): аудит находит в scaffold-фикстуре тикет с меткой `simulation` и чужим заголовком «Normalize game state». Заявление о подтверждении было преждевременным.
- **[Confirmed] «Harness доказан 14/14» → тут же новые дефекты harness.** 02:56Z (line 10141): «Локальный harness зелёный 14/14, цепочка P1→P2 доказана» → 03:15Z (line 10596): judge-парсер считал `Verdict: PASS` за fail (3-й дефект стенда); 06:00Z (line 13770): `session.diff` не видит untracked-файлы → `artifact=none` был ложным. «Доказано» каждый раз означало «доказано в пределах текущего слепого пятна измерителя».
- **[Confirmed] Незакрытый completion: authoring так и не пройден.** Единственный успех authoring — 03:45Z (line 11334, «Approval #1 — ожидает оператора»), но он получен ДО последующих правок директив/стенда. Все финальные прогоны после — провалы: 03:58Z бюджет превышен (line 11649), 04:42Z запрещённый `--version 2>/dev/null` (line 12387), 05:18Z лимит наблюдений (line 12893), 05:38Z «не готово за 15 минут» (line 13210), 06:00Z «9 минут, 31 чтение, artifact=none» (line 13740). Пакет фиксов 05:48Z (line 13580) **ни разу не проверен живым прогоном** — после него сразу синтаксическая ошибка сборки (line 13588) и новый дефект evidence (line 13770).
- **[Confirmed] 8 часов работы не закоммичены.** Последний коммит `c84aff31` — 22:01Z. Всё после (judge-фикс, механические запреты observer, coverage-цепочка, mermaid-диагностика, 7 фиксов от 05:48Z) — **78 dirty-файлов, +2739/−425, uncommitted**. Заявленная дисциплина «pre-commit 5/5» фактически прекратилась после 22:01Z.
- **[Confirmed] Ложнозелёный pre-commit признан самой сессией.** 21:36Z (line 3634): старые тесты инспектора были прибиты к удалённым шагам (`STEP_1B_PREFLIGHT`, 11-шаговый execute) — «зелёный pre-commit проверял старую машину». Это прямой ответ на вопрос делегации №7 и противоречие всем прежним «5/5».

## 2. Лечение симптомов vs причины

- **[Confirmed] Revert-цикл в git.** `6d05208f` «prove project plan before scaffold gate» → `bd608134` Revert → `76e82975` — повторное применение того же коммита (тот же patch-id). (См. реконсиляцию в 03-execution-state.md: это манёвр восстановления базы, не метание.)
- **[Likely → частично исправлено] Эскалирующий blacklist observer — симптоматика.** Хронология запретов: `dist/cli` (02:05Z, line 8906) → весь `node_modules/gennady/**` (02:15Z, line 9140) → `2>&1` (04:29Z, line 12224) → `--version/--help/stderr` (04:42Z, line 12387). Каждый инцидент добавлял правило вместо ответа «почему модель лезет в бандл». Смягчение: 04:08Z (line 11904) сессия всё же нашла обобщённую причину (CLI обрезал Mermaid-ошибку до бесполезной; правила error-handling не активированы в 4 authoring-owner'ах) — это фикс причины, а не симптома.
- **[Confirmed — в пользу сессии] Анти-TodoMVC-дисциплина декларировалась и в основном соблюдалась.** Повторные явные отказы от локальных патчей: line 7126, line 8528, line 9264. Оговорка: почти весь объём правок пришёлся на **стенд и фикстуры**, а не на production-flow — т.е. сессия большую часть ночи чинила собственный измеритель.
- **[Confirmed] Паттерн «не засчитываю прогон» ≥ 7 раз** (lines 6105, 6499, 6707, 8298, 8785, 8906, 12387). Каждый раз причина формально новая — это не строгий цикл, но и не сходимость для ветки authoring: 5+ прогонов подряд «не готово» с последовательной сменой диагноза (mermaid → бюджет → shell-обвязка → монолитный owner).

## 3. Гипотезы оператора против evidence

**(a) Перегруженные директивы / невыполнимые рамки — Confirmed частично (для authoring-маршрута).**
Прямые измерения самой сессии: 37 аксиом подключены, но не активированы в шагах (21:24Z, line 3267); таблицы остановок не привязаны к шагам (21:29Z, line 3432); rule cascade не передал P1 обязательное TS-правило при том, что lint его требует (00:44Z, line 7126); «подготовка до первого артефакта ~10 минут» (23:26Z, line 5630); финальный диагноз 06:00Z (line 13740): «слишком длинный монолитный owner — модель читает весь scope/module-план до записи». НО: execute-маршрут после починки фикстур **проходил на дешёвой модели полностью** (03:45Z, line 11334) — значит рамки не «невыполнимы в принципе», а избыточны именно для authoring на дешёвой модели.

**(b) Непроходимые фикстуры/критерии эвала — Confirmed, самый массовый класс.**
Каталог доказанных дефектов фикстур, при которых эвал был непроходим независимо от качества агента: «approved»-примеры без канонической V2-спеки (21:05Z, line 2830); отсутствующее правило `node-test.xml` в корне fixture (22:11Z, line 4449); `package.json`-скрипты несовместимы с receipt-гейтом (22:17Z); файловые заглушки вместо реальных `type-check/test/coverage` (23:48Z, line 6105); тикет P1 требует тестовый файл P2 до P2 (00:10Z, line 6499); неразрешимый glob `src/*.test.ts` в fingerprint (00:20Z, line 6707); `lcov.info` vs `coverage-final.json` (01:45Z, line 8528); producer/reader coverage — одна и та же команда в обеих ролях (02:36Z, line 9522); Fibonacci-fixture с лишним `src/fibonacci.ts`, уводившим router в развилку восстановления (22:45Z, line 5004).

**(c) «Что-то третье»: нестабильный измеритель/судья — Confirmed как самостоятельная причина.**
Стенд давал и ложные FAIL, и ложные PASS: завершённая сессия помечена `unknown/stuck`, а судья всё равно `pass` (23:12Z, line 5334); `Verdict: PASS` парсился как fail (03:15Z, line 10596; фикс подтверждён в `judge.ts:30`); `session.diff` не возвращает untracked → ложный `artifact=none` (06:00Z, line 13770); утечка `NODE_TEST_CONTEXT` из harness в песочницу → нулевой coverage-профиль (02:55Z, line 10110); `/var` vs `/private/var` symlink → 0% покрытия (02:53Z, line 10057); headless OpenCode не может обслужить интерактивный `question`-tool (22:45Z, line 5004); диск переполнен песочницами (04:20Z, line 12105); тест читал `AUTHORING_READY=no` как «окружение не готово» (05:02Z, line 12693).

**Итого evidence поддерживает все три гипотезы одновременно, с разным весом по маршрутам: execute проваливался в основном из-за (b)+(c), authoring — из-за (a).**

**Гипотеза «агент вошёл в цикл» — Disputed.** Строгого цикла (повтор идентичных действий) нет: каждая итерация закрывала новую, документированную причину, и execute/scaffold дошли до pass. Но у мета-расследования **не было критерия остановки**: 12 часов, ~15+ прогонов, ветка authoring не сошлась, последние ~8 часов правок не зафиксированы коммитом. Это «неограниченная итерация без exit-бюджета», операционально неотличимая от цикла для наблюдающего оператора.

## 4. Что сессия не проверила / приняла на веру

- **[Unknown] Исходный кейс draft.61 (TodoMVC) не перетестирован.** Всё живое тестирование ушло на toy-сценарии (Fibonacci, slugify, TTT).
- **[Unknown/Likely пропущено] Шаг 4 делегации — сверка с draft.52/55/57/58/60 — в паке следов не имеет.**
- **[Unknown] Шаги 3 и 6 делегации (сопоставление отклонений draft.61 с кодом `a77fbcf1`; вердикты по capability adapters / checkpoint / replan)** — структурированных вердиктов нет; сессия перескочила в режим fix-and-rerun.
- **[Confirmed] Пакет из 7 фиксов 05:48Z не верифицирован живым прогоном.**
- **[Unknown] Полная цепочка authoring→scaffold→execute одним flow ни разу не пройдена** — только изолированные ветки.
- **[Disputed по мандату] Делегация требовала «ничего не изменять до согласования» — сессия ушла в масштабную реализацию.** Смягчение: оператор дал «давай делать» в 20:22Z, мандат легально эволюционировал, но исследовательские пункты (3, 4, 6) остались невыполненными.

## 5. Транскрипт, compaction, потерянный контекст

- **[Confirmed] 15 compaction-событий** за 12 часов при 64.7 MB / 13 797 строк; 0 malformed.
- **[Likely] Влияние compaction:** невозврат к пунктам делегации 3–4–6 и дрейф из «расследования» в «марафон fix-and-rerun» согласуются с потерей ранних рамок задачи после сжатий.
- **[Confirmed, малый риск] Workspace-binding:** команды передавали `workdir` worktree явно, git-состояние worktree консистентно, смешения с основным checkout не обнаружено.
- **[Confirmed] Главный операционный риск — 78 незакоммиченных файлов**: все поздние фиксы существуют только в рабочей копии и частично не проверены сборкой/прогоном.

## Итоговая позиция скептика

Ни одна из двух гипотез оператора не является единственной причиной — обе подтверждены, плюс третья: **сам эвал-стенд был нестабилен и генерировал и ложные провалы, и ложные успехи**, из-за чего сессия потратила большую часть времени на починку измерителя, а не flow. «Цикл» — не подтверждён как повтор, но подтверждён как итерация без критерия остановки. Самые опасные незакрытые пункты: непроверенный пакет фиксов 05:48Z, несходящийся authoring-маршрут, неперетестированный TodoMVC и 8 часов uncommitted-работы.
