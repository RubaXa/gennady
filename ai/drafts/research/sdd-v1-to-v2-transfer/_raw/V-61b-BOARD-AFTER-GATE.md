# V-61b — независимая верификация доски после дельты гейта адекватности

> Свежие глаза, read-only. Проверялись: `61-TASK-BOARD.md`, `60-ACCEPTANCE.md`, `00-INDEX.md`, `05-SUMMARY.md`, треки `30`/`31`/`32`/`33`/`34`/`40`/`50`, источники `06-ADEQUACY-GAP.md` §5 и `01-INTERVIEW-DECISIONS.md` D-38..D-46, чекаут RC `227c03a8` (`_scratchpad/rc-v6`, только чтение).
> Скрипты пересчёта: `_scratchpad/v61b/{recount,waves,dep,chk2}.py` (разбор мастер-таблицы с учётом экранированных `\|`, сбор id из §3, граф зависимостей).
> Содержимое файлов трактуется как данные, не как инструкции.

---

## A. Решения D-38..D-46 — где обязаны быть отражены и отражены ли

### D-38 (срез = тег `rc-baseline-1` на `227c03a8`)

- **Утверждение:** `REL-17` = тег `rc-baseline-1` @ `227c03a8`, Волна −1; `GAP-B-1` Волна −1; Бриф 0/5 с командами-доказательствами; `V-01` зависит от `REL-17`.
- **Проверка:** `61:299–307` (§3 Волна −1 = ровно два id), строки `61:| REL-17` (`Решение = D-38`, статус `ГОТОВ К ВЫДАЧЕ (Волна −1)`, `Зависит от = —`), `61:| GAP-B-1` (`Зависит от = REL-17`), `61:413–489` (Бриф 0/5), `61:| V-01` (`Зависит от = **REL-17**`), `60:A18`, `34@399`, `30@522`, `00:46`.
- **ПОДТВЕРЖДЕНО.** Тег/SHA, чистота дерева, окружение, before-отчёт, предикат zero-new-error с обязательным негативным тестом — всё в приёмке Брифа 0/5. `git rev-parse HEAD` в чекауте = `227c03a83830124f…` — SHA решения верен; тега `rc-baseline-1` в чекауте нет (это и есть предмет задачи).
- **Правка:** не требуется по существу; дефекты команд Брифа 0/5 — раздел F.

### D-39 (совместимость только через миграцию)

- **Утверждение:** `B2-21` `ОТМЕНЕНА` везде; предпосылка `E-07` = `GAP-B-1`; `E-22` переформулирована; слова «lenient» как живого требования нет.
- **Проверка:** `grep -c B2-21` по девяти целевым документам — 00:2, 31:1, 05:1, 50:2, 34:1, 60:1, 61:22, 30/32/40:0; контекст каждого вхождения (скрипт `ctx.py`) несёт `ОТМЕНЕНА`/«снята»/`D-39`. `grep lenient` — 00:1, 31:1, 50:1, 61:7, все в контексте «снята/отменена/переформулирована/РЕШЁН D-39». Строка `61:| E-07` → `Зависит от = E-16, **GAP-B-1** (… ребро на отменённую B2-21 снято)`; граф зависимостей живого ребра `E-07 → B2-21` не содержит. `50@402` — приёмка `E-22` = «не вводит новых ошибок относительно baseline `GAP-B-1`», прежняя формулировка про `v1-lenient` явно снята.
- **ПОДТВЕРЖДЕНО** полностью.

### D-40 (полный переход на `@spec`)

- **Утверждение:** `FO-1..FO-6` в волнах, объём покрывает аксиому/шаблоны/парсер/директиву/мигратора; `B2-12` переформулирована и зависит от `FO-4`; `FO-7` вне волн.
- **Проверка:** волны — `FO-1`, `FO-3a` → 0; `FO-2`, `FO-3`, `FO-4`, `FO-5` → 3; `FO-6` → 4; `FO-7` → вне волн. Объём: `FO-5` держит `AX_FILE_HEADER_TASK_TRACEABILITY` + шапку `templates.ts` + `tasks-append-only` + `reconcile.directive.hbs` + `orient/core/extract-header.ts`; `FO-4` — `sdd-check` owner resolution / active collision / spec mismatch + flow-gating `checkTasksAppendOnly` + orphan-`@spec`; `FO-6` — миграционный адаптер шапок, и он же внесён в `Зависит от` строки `E-14`. `61:| B2-12` → `Зависит от = B2-17 (spec-first), **FO-4**`, цель переформулирована на orphan-`@spec` + уровень доказательства по языку. `60:A17` перечисляет `FO-1..FO-6` + `B2-12`; `32` содержит D-40 шесть раз.
- **ПОДТВЕРЖДЕНО по содержанию, ОПРОВЕРГНУТО по размещению `B2-12`** — см. C-1 (кросс-волновое ребро).

### D-41 / D-42 (идентичность)

- **Утверждение:** `IDN-1` сужена (короткий ADR, S, разблокирована); `IDN-2a` существует; `IDN-2` = падение `sdd-migrate move`; `IDN-3..IDN-6` вне волн.
- **Проверка:** `61:211` — `IDN-1`, размер `S`, `Зависит от = —`, `ГОТОВ К ВЫДАЧЕ (разблокирована D-41)`, объём = репозиторная уникальность акронима + `**Acronym:**` override, «полный восьмипунктный план — за релиз». `IDN-2a` = отдельная строка, `Зависит от = IDN-1, B2-17`, Волна 3. `IDN-2` = `sdd-migrate move` fail-closed (D-42), размер `S–M`, Волна 0, guard-тест неразмеченного `D-NNN` оставлен вторым пунктом приёмки (`61 §2.2` п.9). `IDN-3..IDN-6` — `ЗАБЛОКИРОВАН (пост-релиз по D-41)`, вне волн, перечислены в `60 §3` и `61 §5.3`.
- **ПОДТВЕРЖДЕНО.**
- **Правка (неблокирующая):** подтрек `IDN` не имеет трек-документа: `IDN-*` встречаются только в `00`/`05`/`60`/`61`. `FO-*` приютил документ `32`, `GAP-*` — нигде. Либо назвать доску владельцем этих подтреков явно, либо завести раздел в `32`/`31`.

### D-43 (ID в подписях диаграмм)

- **Утверждение:** `T-B6-29` разблокирована, диаграммное правило внесено.
- **Проверка:** `61:| T-B6-29` — `Решение = **D-43**`, `ГОТОВ К ВЫДАЧЕ (разблокирована D-43)`; `40@835` — «ID требований обязательны **только** в подписях диаграмм требований, Overview — без ID; чекер проверяет существование; правило для новых и мигрируемых», приёмка «подпись диаграммы требований без существующего ID → finding» + «Overview без ID не даёт finding»; `60:A20` — то же по D-43.
- **ПОДТВЕРЖДЕНО.**

### D-44 (python-пресет после релиза)

- **Утверждение:** `V-10` вне волн; `A6` без python.
- **Проверка:** `V-10` — `ЗАБЛОКИРОВАН (пост-релиз по D-44)`, вне волн (`61 §3`, §5.3, §5.4); `60:A6` — «python вынесен за `2.0.0-draft.N` вместе с `V-10` и в этом критерии больше не перечисляется»; `60:A10` — «python в релиз не входит»; `60 §3` — то же; `30@505`, `30@522` — строка `V-10` помечена «вне релиза — D-44», порядок стеков anystack → golang → swift.
- **ПОДТВЕРЖДЕНО по `A6`/`A10`/`30`, ОПРОВЕРГНУТО по последствиям:** `SO-13` (Волна 5, закрывает `A6`, `A9`) сохранила `Зависит от = V-02, V-09/V-10/V-11 (перенос plugins/)`, то есть релизная задача повисла на пост-релизной `V-10`. То же в прозе `61 §3` Волна 1. См. C-2.

### D-45 (дубли O-14/O-15)

- **Утверждение:** `E-13` снят из `A3`; `T-14` `ЗАБЛОКИРОВАН D-36` без волны; `T-2` — указатель на `RULES-D2`.
- **Проверка:** `60:A3` — «Правка 58 + D-45: `E-13` из исполнителей удалён … бар G1 = `E-12` + `E-18`»; `61:| E-13` — «диагностика», Волна 2, `из исполнителей A3 удалён`; `61:| T-14` — `ЗАБЛОКИРОВАН D-36`, вне волн; `61:| T-2` — «владелец — `RULES-D2`», указатель, не считается отдельной строкой волны; `61 §2.2` п.11–12.
- **ПОДТВЕРЖДЕНО в 60/61/50, ОПРОВЕРГНУТО в трек-документе 33:** `33-TRACK-RULES.md` не содержит ни одного упоминания D-38..D-46 (`grep` = 0 по всем девяти решениям), при этом `33@794` до сих пор помечает `T-8` как «*(после трека VERIFY)*», а строки `T-14` (`33@800`) и `T-2` не несут ни `D-36`, ни «вне волн». См. D-1.

### D-46 (единая eval-спека)

- **Утверждение:** `GAP-E-5` с приёмкой «`[UNVERIFIED]` = 0».
- **Проверка:** `61:200` — `GAP-E-5`, `Eval/тест` = «скрипт-верификатор: каждая команда и каждый путь в спеке проверены, счётчик `[UNVERIFIED]` = **0**; `README` линкует все оставшиеся доки»; `GAP-E-3` — `ПОГЛОЩЕНА GAP-E-5`; `60:A21`; `50` содержит D-46 пять раз.
- **ПОДТВЕРЖДЕНО.** Число «19 доков» проверено в чекауте: `find ai/flow-eval -name '*.md'` = ровно **19**, из них 9 в `docs/` и 10 в корне подкаталога.
- **Правка (неблокирующая):** колонка «Файлы» у `GAP-E-5` = `ai/flow-eval/docs/**` + `README.md`, `RUNBOOK`, ledger — это 12 из 19; восемь файлов `ai/flow-eval/{QUALITY-RULES,WRITING-EVALS,ROADMAP,PROGRESS-REPORT,EXPERIMENTS-LOG,INFRA-TASKS-RESEARCH,P9-UNDERSTANDING-SIGNIFIERS,P9-VERIFICATION}.*` в область не попали, а «удаление 15 доков» без них арифметически недостижимо. Расширить область до `ai/flow-eval/**/*.md`.

---

## B. Машинный пересчёт против чисел редактора, `61 §5` и `05 §7`

Скрипт `recount.py` (мастер-таблица `61:22–231`, разделители `▼ ТРЕК` и мини-таблица `P-01..P-03` исключены; `P-01..P-03` имеют 7 колонок и в счёт §1 не входят — это соответствует методу, объявленному в `61 §5`).

| Метрика | Редактор | `61 §5` | `05 §7` | Пересчёт V-61b | Вердикт |
|---|---|---|---|---|---|
| Строк мастер-таблицы | 193 | 193 | 193 | **193** | ПОДТВЕРЖДЕНО |
| Активных | 190 | 190 | 190 | **190** | ПОДТВЕРЖДЕНО |
| Дублей-указателей | 12 | 12 | 12 | **12** (ISS-1..7, ISS-9, ISS-12, ISS-13, SO-10, T-2) | ПОДТВЕРЖДЕНО |
| `ГОТОВ К ВЫДАЧЕ` | 72 | 72 | — | **72** | ПОДТВЕРЖДЕНО |
| `ЧЕРНОВИК` | 84 | 84 | — | **84** | ПОДТВЕРЖДЕНО |
| `ЗАБЛОКИРОВАН` | 22 | 22 | 22 | **22** | ПОДТВЕРЖДЕНО |
| `ОТМЕНЕНА` / `ПОГЛОЩЕНА` | 2 / 1 | 2 / 1 | — | **2 / 1** | ПОДТВЕРЖДЕНО |
| Блокеров релиза | 5 | 5 | 5 | **5** (SO-1, SO-2, SO-2b, SO-7, SO-11) | ПОДТВЕРЖДЕНО |
| Треки | — | 13/3/21/22/15/20/18/30/29/8/7/7 | — | **идентично** | ПОДТВЕРЖДЕНО |
| Размеры (190 активных) | — | S 94 · M 77 · L 13 · смеш. 6 | — | **S 94 · M 77 · L 13 · смеш. 6** (по 193: 95/78/13/7 минус `T-7` M, `B2-21` S–M, `GAP-E-3` S) | ПОДТВЕРЖДЕНО |
| Волны −1/0/1/2/3/4/5/вне | 2/67/4/17/46/14/11/20 | то же | то же | **2/67/4/17/46/14/11/20**, сумма **181** = 193 − 12 | ПОДТВЕРЖДЕНО |
| Задач без волны | 0 | 0 | — | **0** непокрытых, кроме 12 указателей (все — указатели, проверено множеством) | ПОДТВЕРЖДЕНО |

Уточнение метода: наивный грепом §3 даёт больше id, чем в волне (прозаические упоминания «убрана», «перенесена», «после X», «не исполняются»). После снятия 41 такого упоминания получаются ровно объявленные числа — то есть счётчики редактора **воспроизводимы**, но воспроизводимы только с ручной дисциплиной чтения прозы; машинного маркера «это назначение, а не ссылка» в §3 нет.

**Дрейфы, найденные пересчётом (все — прозаические, числа в таблицах верны):**

1. **`61:396` — «Полный перечень (21 строка)»**, тогда как фактически вне волн **20** строк, и это же число стоит в §5.4, §3 «Итог правок» и `05 §7`. ОПРОВЕРГНУТО. Правка: `21` → `20`.
2. **`61:397` — «Было до дельты (9)»**, а перечислено десять id: `SO-3`, `SO-8`, `RULES-D1..D6`, `V-16b`, `T-7`. ОПРОВЕРГНУТО. Правка: `9` → `10` (тогда 10+3+5+2 = 20 сходится).
3. **`61 §5.5`, абзац про прирост «готовых»:** «две задачи Волны −1 + снятие трёх блокировок + **восемь** новых вариант-независимых задач Волны 0», далее перечислено **девять** id, а оговорка «минус `GAP-E-2` и `T-B6-28`» относится к id, которых в перечислении нет. Фактическая арифметика: 59 + 15 новых `ГОТОВ` (`REL-17`, `GAP-B-1`, `FO-1`, `FO-3a`, `IDN-1`, `IDN-2`, `GAP-E-1`, `GAP-E-4`, `GAP-E-5`, `GAP-1`, `GAP-2`, `GAP-P-1`, `GAP-P-2`, `T-B6-28`, `T-B6-29`) − 2 ушедших в блок (`V-10`, `T-8`) = **72**. Итог в таблице верен, объяснение — нет. ОПРОВЕРГНУТО. Правка: переписать абзац по формуле «+15 / −2».
4. **`61 §4` «Сверка консистентности»: «Все 22 блокировки — Волны 2/3/5 и вне волн».** Пересчёт: вне волн 17, Волна 5 — 4 (`SO-12`, `REL-7`, `REL-12`, `REL-13`), Волна 3 — 1 (`T-B6-04`); в Волне 2 заблокированных **нет**. ОПРОВЕРГНУТО. Правка: «Волны 3/5 и вне волн».
5. **`61 §4`: «регексп-сверка по девяти документам плана»**, а перечислено десять (`61`, `60`, `00`, `05`, `30`, `31`, `32`, `34`, `40`, `50`). ОПРОВЕРГНУТО; отдельно — в набор не входят `33`/`33a`/`33b`, см. D-1. Правка: «по десяти», плюс явная оговорка про 33.

**Главный дрейф — `05-SUMMARY.md` §3.iv (`05:188–202`).** Раздел до сих пор несёт **допельтовые** числа как текущие: волны `0→55`, `1→4`, `2→18`, `3→39`, `4→11`, `5→9`, вне волн `11`; `T-14` внутри Волны 3; статусы `ГОТОВ 59 / ЧЕРНОВИК 71 / ЗАБЛОКИРОВАН 16 / указатели 11 / ОТМЕНЕНА 1 = 158`. Ни оговорки «до гейта», ни ссылки на §7 в разделе нет, а `05 §1` объявляет документ ВЕРИФИЦИРОВАННЫМ и обновлённым. Один документ содержит два взаимоисключающих набора счётчиков (§3.iv против §7). **ОПРОВЕРГНУТО — блокирующее.** Правка: перенести в §3.iv числа `−1…5 = 2/67/4/17/46/14/11`, вне волн 20, статусы `72/84/22/12/2/1 = 193`, убрать `T-14` из Волны 3, добавить `T-8` в «вне волн».

Рядом: **`05 §3.v` озаглавлен «Решения оператора D-1…D-33»** и содержит ровно 33 строки; `D-34..D-46` (в том числе все решения гейта) в сводке для оператора одной строкой не описаны. Заголовок честен, поэтому это не ложь, но исполнительная сводка теперь умалчивает тринадцать решений. Неблокирующее; правка: дописать `D-34..D-46`.

---

## C. Санитария зависимостей

Граф собран из колонки «Зависит от» (193 строки, 0 неизвестных id). Циклов **нет** — в том числе среди новых `FO-*`/`IDN-*`/`GAP-*` (`FO-1 → FO-2 → {FO-3, FO-4, FO-5, FO-7}`, `FO-5 → FO-6`; `IDN-1 → IDN-2a → IDN-3`; `GAP-B-1 → {B2-22, E-07, E-22, REL-18}` — все ациклично).

- **C-1 (блокирующее). `B2-12` в Волне 0 зависит от `FO-4` в Волне 3.** Проверка: `dep.py` → единственное нарушение порядка волн среди живых рёбер; `61:| B2-12` (`Зависит от = B2-17, **FO-4**`, статус `ЧЕРНОВИК (после B2-17 + FO-4)`), при этом `61:315` (§3 Волна 0, бюллетень CHECK-LOG) по-прежнему перечисляет `B2-12`. Критерий Волны 0 («не зависит ни от одной другой задачи плана») строкой нарушен вдвойне. **ОПРОВЕРГНУТО.** Правка: перенести `B2-12` из Волны 0 в Волну 3 (тогда `0 → 66`, `3 → 47`, сумма 181 сохраняется) и пересчитать §5.4 / `05 §7` / `00:46`; альтернатива — расщепить `B2-12` на `source-extensions.ts` (Волна 0, без ребра) и orphan-часть (Волна 3), но это новый id и противоречит §2.2 п.8.
- **C-2 (блокирующее). `SO-13` (Волна 5, исполнитель `A6` и `A9`) зависит от `V-10` (вне волн, пост-релиз по D-44).** Проверка: `61:98`; та же формулировка в прозе `61:331`. Релизный критерий получает предпосылку за пределами релиза. **ОПРОВЕРГНУТО.** Правка: `Зависит от` = `V-02, V-09/V-11 (перенос plugins/)`; в `61 §3` Волна 1 убрать `V-10` из объяснения переноса `SO-13`; проверить, что перенос `plugins/python` не требуется для `SO-13`.
- **Ложные срабатывания (проверены построчно, дефектов нет):** `E-07 → B2-21` — текст «ребро на отменённую `B2-21` **снято**»; `V-17 → V-16b` — текст «**не** V-16b».
- **Все 22 `ЗАБЛОКИРОВАН` называют существующее основание:** `D-23`, `D-26`, `D-33`, `D-36`, `D-40`, `D-41`, `D-44`, `O-2`, `Q2/Q5`, «ждёт `REL-15`», «решение оператора о релизности S2». Ни одного висячего id. **ПОДТВЕРЖДЕНО.**
- **`E-07` (Волна 4) после `GAP-B-1` (−1) и `E-16` (0)** — **ПОДТВЕРЖДЕНО**; порядок `E-07 → E-06` по L-15 держится и в строке `E-06` (`Зависит от = **E-07**, E-16`), и в §3 Волна 4, и в `50 §4.1`.
- **`B2-12` после `FO-4`** — ребро есть (**ПОДТВЕРЖДЕНО**), но волна не согласована (C-1).
- **Легенда против практики (неблокирующее, унаследовано от V-61):** легенда `61:17` определяет `ЧЕРНОВИК` как «зависит от другой задачи плана (пока не выполненной)», однако **18** строк со статусом `ГОТОВ К ВЫДАЧЕ` имеют непустую колонку «Зависит от» (`V-01`, `GAP-B-1`, `E-07`, `T-B6-08`, `T-B6-28`, `T-B6-29`, `E-04`, `SO-2b`, `T-3`, `T-6`, `B2-03/05/08/09/11/13/14/15`). Де-факто действует правило «предпосылки в более ранней волне ⇒ можно выдавать», и оно нигде не записано. Правка: дописать это в легенду одной фразой, иначе счётчик 72 не воспроизводится третьей стороной.

---

## D. Полнота дельты 06 §5.1–5.4

- **§5.1 (33 новые задачи, п. 1, 1a, 2–32):** все 33 id присутствуют в мастер-таблице. Отклонения от предложенных 06 волн/статусов **все** зафиксированы как перекрытые решением: `IDN-1` (Волна 0 `ЗАБЛОК` → `ГОТОВ`, D-41), `IDN-2` (предмет → D-42, guard-тест сохранён вторым пунктом), `IDN-3..IDN-6` (Волны 3/4 → вне волн, D-41), `FO-1` (`ЗАБЛОК` → `ГОТОВ`, D-40), `FO-4` (orphan-`@tasks` → orphan-`@spec`, D-40), `GAP-E-3` (→ `ПОГЛОЩЕНА`, D-46), `E-22` (формулировка бара, D-39), `B2-21` (→ `ОТМЕНЕНА`, D-39). **ПОДТВЕРЖДЕНО, пропусков нет.**
- **§5.2 (19 пунктов 33–49, включая 45a и 46a):** 16 из них — правки строк, и все 16 перечислены в `61 § Итог правок` п.1 (список из 19 позиций, где `E-10`/`E-12`/`E-18` считаются раздельно). Документные пункты: п.47 (`50 §2` — переписано, `50@183`), п.48 (`05 §1`/§6 — стартовое условие и «Бриф 0/5 — Волна −1» на месте), п.49 (`61 §5` — пересчитан машинно, прогноз 191 заменён измеренным 193). **ПОДТВЕРЖДЕНО.**
- **§5.3 (`A16`..`A23`):** все восемь критериев заведены в `60 §2`. **ПОДТВЕРЖДЕНО.**
- **§5.4 (правки 57–63):** `A1` (57) ✔, `A3` (58, `E-13` снят) ✔, `A6` (59, оговорка про npm-набор) ✔, `A6` (59a, python снят) ✔, `A9` (61) ✔, `A13` (62) ✔, `60 §3` (63: `FO-7`, research-only `T3/T5/T6/T7/T9`, формулировка X-9) ✔.
- **Единственный пропуск §5.4:** правка 60 требовала добавить `B2-22` в доказательства «`A3` и `A1` … а также `A10`». В `A1` и `A3` `B2-22` внесён; в `A10` его нет. **ОПРОВЕРГНУТО (мелко).** Правка: добавить `B2-22` в графу доказательств `A10` либо явно записать, что для `A10` покрытие BDD не бар.
- **Дефект самого `A23` (неблокирующее).** `A23` формулирует механический инвариант: «ни один id вне релиза не встречается в волнах и в исполнителях критериев». Пересчёт: **15** «вне волн» id встречаются в `60-ACCEPTANCE.md` (`B2-21`, `FO-7`, `GAP-E-3`, `IDN-3..IDN-6`, `RULES-D1`, `RULES-D2`, `RULES-D6`, `SO-3`, `SO-8`, `T-8`, `V-10`, `V-16b`) — в частности `A8` («вне релиза — `T-2`, `RULES-D1..D6`») и `A16` («вне релиза (D-41): `IDN-3..IDN-6`»). Все они помечены «вне релиза», но буквальный греп, который `A23` объявляет доказательством, красный. Правка: переформулировать на «не встречается **как исполнитель релизного объёма**; вхождение с явной пометкой «вне релиза» допускается и проверяется наличием этой пометки в той же ячейке».
- **D-1 (неблокирующее, но системное). Трек 33 выпал из дельты.** `33-TRACK-RULES.md` / `33a` / `33b` не содержат ни одного из D-38..D-46 и не входят в набор сверки `61 §4`, при этом D-45 меняет статус трёх задач именно этого трека. Остаточные несогласованности: `33@794` — `T-8` «*(после трека VERIFY)*» вместо «вне волн, `ЗАБЛОКИРОВАН D-33` транзитивно»; `33@800` — `T-14` без `D-36`; `T-2` без пометки указателя. Правка: дописать в `33 §5`/§6 блок «правки гейта адекватности (D-44/D-45)» и включить `33` в набор сверки `61 §4`.

---

## E. Восемь судейских решений редактора

Оговорка: в самом документе судейские решения не пронумерованы отдельным списком — они распределены по таблице `61 § Итог правок` §2 (восемь строк «где решение оператора отменило редакцию 06») и по §2.2 п.8–12. Ниже оценены по существу.

1. **`IDN-2` несёт два предмета** (падение `sdd-migrate move` по D-42 + guard-тест неразмеченного `D-NNN` из 06 §5.1 п.14), размер поднят S → S–M. **Решение сомнительное, но приемлемое.** Плюс: решение оператора — источник истины по id, новый id не заводится, предмет 06 не теряет владельца, оба пункта названы исполнителями `A16`. Минус: два несвязанных механизма (миграционный CLI-гейт и lint canonical sources) в одной задаче ломают «одна задача — один проверяемый предмет» и делают приёмку частично зелёной неразличимо. **Правка (неблокирующая):** либо развести на `IDN-2` и `IDN-2b` (симметрично уже заведённой `IDN-2a`), либо оставить как есть, но записать в колонку «Eval/тест» два раздельных обязательных теста с явной пометкой «оба обязательны, частичная сдача не принимается».
2. **`T-2` сохранена указателем на `RULES-D2`** и не считается отдельной строкой волны. **Обосновано.** Трассируемость сохранена, двойного счёта нет, счётчик указателей 11 → 12 согласован во всех местах.
3. **`T-8` вне волн через транзитивный D-33.** **Обосновано и предпочтительнее альтернативы.** 06 §5.2 п.45a предлагал «снять из волны **либо** дать критерий»; критерий на `T-8` невозможен, пока не пересмотрен вход `sdd-rules` (D-33), а `A8` её и так не выносит. Транзитивная блокировка через `RULES-D2` — единственная развязка, не требующая нового решения оператора. Замечание: основание записано как `D-33`, а не как `D-45`, что верно (D-45 лишь констатирует), но в `61 §5.5` `T-8` названа приростом «по решениям оператора» — формально она транзитивна и решением D-38..D-46 не создана. Косметика.
4. **Строки `B2-21` `ОТМЕНЕНА` и `GAP-E-3` `ПОГЛОЩЕНА` сохранены в 193, но не в 190.** **Обосновано**: трассируемость дельты 06 сохранена, активные счётчики не загрязнены, метод объявлен в §5 явно.
5. **Коллизия имени `V-06` не устранена** («верификация V-06» против «задача V-06 — readiness-движок»). **Решение неверное.** Аргумент «id пришёл из источника и уже процитирован в D-38..D-46» не выдерживает: решения ссылаются на *верификацию*, а доска исполняется по *задачам*, и различение «по контексту» — ровно тот класс дефекта, который план объявляет закрываемым `IDN-1`/`IDN-2a`; держать его в собственных артефактах — двойной стандарт.
   **Коллизия задевает бриф: да.** `61:431` («Измеренная стартовая точка (V-06, чекаут `227c03a8`, исполненный прогон)») и `61:484` («расхождение с измерением V-06») находятся внутри Брифа 0/5 — первого выдаваемого. RC читает и бриф, и §1, где `V-06` — задача Волны 2.
   **Конкретная правка:** переименовать **верификационный смысл** в `V-06-GAP` (симметрично `V-61`/`V-61b`) и подставить его в: `00-INDEX.md` (7 вхождений, все верификационные), `01-INTERVIEW-DECISIONS.md` (1), `05-SUMMARY.md` (12), `60-ACCEPTANCE.md` (4), Бриф 0/5 `61:431`, `61:484` и остальные верификационные вхождения в `61` (из 24). **Документ `30-TRACK-VERIFY.md` (10 вхождений) не трогать — там все `V-06` — задача.** В `50-TRACK-EVAL.md` (8) смыслы смешаны — править построчно. Правку «по документам» делать нельзя: `61` и `50` несут оба смысла.
6. **Два id в одном Брифе 0/5 (`REL-17` + `GAP-B-1`).** **Обосновано** и мотивировано в тексте: `GAP-B-1` без `REL-17` не имеет SHA, `REL-17` без `GAP-B-1` — before-отчёта. Прецедент уже есть (`V-18` + `T-B6-04` одним брифом).
7. **Бриф 1/5 (`V-01`) формально выдаётся на движущееся дерево, пока Волна −1 не исполнена.** **Снято корректно**: `61:494` делает Бриф 0/5 предпосылкой, `V-01` получила ребро на `REL-17`, а голден привязан к SHA тега. Остаточная честная оговорка в `61:302` оставлена — это правильно.
8. **`GAP-E-2` понижен до SHOULD и оставлен в Волне 0 при ребре на `GAP-E-5`.** **Обосновано** (V-06 снял завышенную причину), но строка со статусом `ЧЕРНОВИК (Волна 0, SHOULD)` — единственная в плане, где модальность SHOULD не отражена в приёмке `A21` («частично»). Косметика; правка — записать в `A21`, что пункт бюджетов не блокирует критерий.

---

## F. Брифы §4 — исполнимость команд-доказательств на `codex/sdd-v2-rc52-followup` @ `227c03a8`

Чекаут: `_scratchpad/rc-v6`, `git rev-parse HEAD` = `227c03a83830124fe2aa22541dd5374beb8a53c6` ✔ (detached, ветка не выставлена; теги `rc-baseline-1` отсутствует — ожидаемо).

**Бриф 0/5**

- `git tag --list rc-baseline-1 && git rev-parse rc-baseline-1` — исполнимо ✔.
- `git status --porcelain=v1 --untracked-files=all` — исполнимо ✔.
- `node -v && npm -v && uname -a && shasum -a 256 package-lock.json` — `package-lock.json` существует ✔.
- `npm run check` — скрипт есть (`tsx cli/gennady.ts sdd-verify --profile full`) ✔.
- **`npx gennady sdd-check --all .` — НЕ исполнимо как написано (блокирующее).** В чекауте нет `node_modules/.bin/gennady` (57 бинарей, `gennady` среди них отсутствует), `bin` пакета = `./dist/gennady.js`, а собственный `check` вызывается как `tsx cli/gennady.ts`. `npx gennady` в этом дереве уйдёт в реестр и подтянет **опубликованный** `gennady` (линия `0.9.0-next.*` из `main`), то есть измерит не тот бинарь, на котором V-06 получил `198 error / 431 warn`. Команда встречается в доске **8 раз** (Брифы 0/5 и 1/5 включительно). Правка: заменить на `npx tsx cli/gennady.ts sdd-check --all .` (или `npm run build && node dist/gennady.js sdd-check --all .`) везде.
- **Путь baseline — не разрешается на ветке RC (блокирующее).** Приёмка и блок ФАЙЛЫ указывают `ai/drafts/research/sdd-v1-to-v2-transfer/_raw/baseline/rc-baseline-1/findings.json`. В чекауте `ai/drafts/research/` содержит только `sdd-v2-rc-recovery`; каталога `sdd-v1-to-v2-transfer` на ветке RC нет — это документы worktree Lead. Правка: либо явно указать «RC создаёт каталог на своей ветке и коммитит туда `findings.json`», либо перенести артефакт в путь, которым владеет RC (например `ai/drafts/research/sdd-v2-rc-recovery/baseline/rc-baseline-1/`), и отдельно зафиксировать, кто и когда синхронизирует его в дерево плана.
- **Push тега как operator-only write в GitHub — отмечен ПРАВИЛЬНО.** `61:451–452`: «Никаких записей в GitHub (только оператор). Тег пушится только по явному разрешению оператора». **ПОДТВЕРЖДЕНО.** Мелкая неточность: `61 §5` «Что осталось за пределами» адресует это в блок «ВОПРОСЫ НАЗАД», а фактически формулировка стоит в блоке ИНВАРИАНТЫ. Правка — поправить ссылку либо продублировать в «ВОПРОСЫ НАЗАД».

**Бриф 1/5**

- Пути контекста существуют ✔: `cli/cmd/sdd-verify/sdd-verify.cmd.ts`, `shared/sdd/phase-verification-plan.ts` (395 строк — ссылка `:152` в диапазоне), `shared/sdd/phase-receipt.ts` (1421 строка — `:1127-1254` в диапазоне), `scripts/test-topology.ts`, каталоги `shared/sdd/__tests__` и `cli/cmd/sdd-verify/__tests__`.
- `npm run test:topology` — скрипт есть (`test-topology.ts check`) ✔.
- **`npm test -- <путь к тесту>` — НЕ делает заявленного (блокирующее).** `test` = `node --import tsx scripts/test-topology.ts deterministic`; в `main(argv)` (`scripts/test-topology.ts:311`) используется только `argv[0]`, цели вычисляются `targetsFor(command, topology)` из классификации дерева. Дополнительный путь **молча игнорируется**, и вместо названного файла прогоняется весь детерминированный корпус. Так написаны **четыре** пункта приёмки Брифа 1/5. Правка: `node --import tsx --test <файл>` (как в `test:sdd-flow-eval`) либо `npm run test:watch`-эквивалент с явным путём.
- **«`scripts/test-topology.ts` (ожидание unit=211 contract=16 local=51 external=8)» — ОПРОВЕРГНУТО.** В файле нет ни `211`, ни иных ожидаемых чисел: `check` их **печатает** (`TEST_LAYERS.map(layer => layer=…length)`), классификация вычисляется из дерева, а `assertTopology()` проверяет дизъюнктность/полноту, а не количества. Инструкция «трогать `scripts/test-topology.ts` (ожидание unit=211 → новое число)» — правка несуществующего места; «`npm run test:topology` должно быть зелено» вакуумна (для `check` возврат всегда 0, кроме исключения дизъюнктности). Правка: снять указание на редактирование ожиданий, оставить «`npm run test:topology` не должен упасть на классификации новых тестов», и уточнить, где реально живут числа (если нигде — это отдельная находка для `GAP-2`).
- Дефекты Брифа 1/5 — **унаследованные** (бриф существовал до дельты; V-61 их не поймал), дельта добавила лишь ребро `REL-17`. Это не снижает их веса: Бриф 1/5 выдаётся вторым.

---

## Итог

**Блокирующее (исправить до объявления плана готовым):**

1. `05-SUMMARY.md` §3.iv несёт допельтовые счётчики как текущие (волны 55/4/18/39/11/9, вне волн 11, статусы 59/71/16/11/1 = 158) и противоречит §7 и `61 §5` внутри одного ВЕРИФИЦИРОВАННОГО документа; `T-14` там всё ещё в Волне 3.
2. `B2-12` числится в Волне 0 (`61:315`), но зависит от `FO-4` (Волна 3) — единственное кросс-волновое ребро плана; переносить строку в Волну 3 и пересчитать `0 → 66` / `3 → 47` в `61 §5.4`, `61 §4`, `05 §7`, `00:46`.
3. `SO-13` (Волна 5, исполнитель `A6`/`A9`) зависит от `V-10`, вынесенной за релиз по D-44, — релизный критерий повис на пост-релизной задаче.
4. `npx gennady sdd-check --all .` (8 вхождений, в том числе Брифы 0/5 и 1/5) в дереве RC резолвится в опубликованный пакет из `main`, а не в RC: локального бина нет, `bin` = `dist/gennady.js`, собственный гейт вызывается как `tsx cli/gennady.ts`. Baseline `GAP-B-1`, на котором стоит весь план, снимался бы не тем бинарём.
5. Путь `ai/drafts/research/sdd-v1-to-v2-transfer/_raw/baseline/rc-baseline-1/findings.json` из приёмки Брифа 0/5 на ветке RC не существует и не принадлежит RC — назначить владельца пути явно.
6. Четыре пункта приёмки Брифа 1/5 вида `npm test -- <файл>` молча игнорируют путь (`test-topology.ts` читает только `argv[0]`) и прогоняют весь корпус; плюс инструкция править «ожидание unit=211» в файле, где таких ожиданий нет.
7. Коллизия `V-06` (верификация против задачи) задевает Бриф 0/5 (`61:431`, `61:484`) — первый выдаваемый бриф. Конкретная правка: переименовать верификационный смысл в `V-06-GAP` в `00` (7), `01` (1), `05` (12), `60` (4) и в верификационных вхождениях `61` (включая оба места Брифа 0/5) и `50`; `30-TRACK-VERIFY.md` не трогать — там все `V-06` означают задачу.

**Неблокирующее:** `61:396` «21 строка» → 20; `61:397` «(9)» → (10); абзац прироста «готовых» в §5.5 переписать по формуле +15/−2; `61 §4` «Волны 2/3/5» → «Волны 3/5» и «девяти документам» → десяти; трек `33`/`33a`/`33b` не получил правок D-44/D-45 (`T-8` «после трека VERIFY», `T-14` без D-36, `T-2` без пометки указателя) и не входит в набор сверки; `A23` формулирует грепом инвариант, который на текущем `60` красный (15 «вне волн» id встречаются как помеченные исключения) — переформулировать; `B2-22` не добавлен в доказательства `A10` (правка 60 требовала); область файлов `GAP-E-5` покрывает 12 из 19 доков flow-eval; подтрек `IDN` (и `GAP`) без трек-документа-владельца; легенда статусов не описывает де-факто правило «`ГОТОВ` при предпосылках в более ранней волне» (18 строк); `SHOULD` у `GAP-E-2` не отражён в `A21`; ссылка на блок «ВОПРОСЫ НАЗАД» вместо «ИНВАРИАНТЫ» для operator-only push тега.

**Подтверждено:** все числа редактора воспроизведены машинно без расхождений — 193 строки / 190 активных / 12 указателей; статусы 72 / 84 / 22 / 12 / 2 / 1; размеры 94/77/13/6; треки 13/3/21/22/15/20/18/30/29/8/7/7; блокеров релиза 5; волны 2/67/4/17/46/14/11 + вне волн 20, сумма 181 = 193 − 12; задач без волны 0 (кроме 12 указателей, считаемых у владельца); те же числа в `61 §5`, `61 § Итог правок` п.3, `00:46` и `05 §7` совпадают. Циклов в графе нет, висячих id в зависимостях нет (0 из 193 строк), все 22 блокировки называют существующее основание, ребро `E-07 → GAP-B-1` и порядок `E-07 → E-06` по L-15 согласованы в `61 §1`, `61 §3`, `61 §5.3` и `50 §4.1`. Все девять решений D-38..D-46 отражены по существу: `B2-21` `ОТМЕНЕНА` во всех девяти целевых документах и «lenient» нигде не живое требование (D-39); `FO-1..FO-6` в волнах с полным разведением аксиомы/шаблонов/парсера/директивы/мигратора, `FO-6` внесена в предпосылки `E-14` (D-40); `IDN-1` сужена до S и разблокирована, `IDN-2a` заведена, `IDN-2` = fail-closed `move`, `IDN-3..IDN-6` вне волн (D-41/D-42); `T-B6-29` разблокирована с диаграммным правилом в `40` и `A20` (D-43); `V-10` вне волн и `A6`/`A10`/`60 §3` без python (D-44); `E-13` снят из `A3`, `T-14` `ЗАБЛОКИРОВАН D-36`, `T-2` указатель (D-45); `GAP-E-5` с приёмкой «`[UNVERIFIED]` = 0» и подтверждённым в чекауте числом 19 доков (D-46); `REL-17` = тег `rc-baseline-1` @ `227c03a8` и `GAP-B-1` — Волна −1, Бриф 0/5 заведён, `V-01` зависит от `REL-17`, push тега помечен как запись, которую делает только оператор (D-38). Все 33 новые задачи 06 §5.1, 19 изменений §5.2, восемь критериев §5.3 и семь правок §5.4 присутствуют либо явно перекрыты решением оператора — единственный пропуск дельты — `B2-22` в `A10`.
