ВЕРИФИКАЦИЯ — Пачка 17 «Фазовый агент не выходит за свои файлы и умеет остановиться»

Проверяющий: `plan-verifier` (свежие глаза). Только чтение дерева RC; сборка/тесты запускались, рабочее дерево осталось чистым (`git status --short` пуст до и после).
Предмет: `rc-v6`, ветка `lead/phase-agent-bounds`, HEAD `6de96024`, база `61b86fb8`; коммиты `70361d49` (T-B6-12), `e5e9a9b1` (T-B6-27), `6b60f7e9` (ISS-8), `6de96024` (T-B6-16) — состав и порядок совпадают с заявленным.
Отчёты: `R-T-B6-12.md`, `R-T-B6-27.md`, `R-ISS-8.md`, `R-T-B6-16.md`, `R-BATCH-17-phase-agent-bounds.md`.
Источники требований: `61-TASK-BOARD.md` §1 строки 32 (ISS-8), 151 (T-B6-12), 176 (T-B6-27), 241–245 (V14-2a..e), `62-BATCH-QUEUE.md` §«Пачка 17», `40-TRACK-DIRECTIVES-SKILLS.md` §1.7 (D7.1–D7.6) и §4.4, `20-ISSUES-VERDICTS.md` #19, `02-LEAD-DECISIONS.md` L-16.

---

## A. Полнота — ПОДТВЕРЖДЕНО

`git diff --stat 61b86fb8..6de96024` → 11 файлов, +207/−27. Все 11 имеют строку в таблице сводного отчёта (четыре сгенерированных `phase-execution-protocol.directive.xml` + `steps/STEP_{1,2,3}` сведены в одну строку — допустимо, они производные одного шаблона). Пофайловые таблицы: T-B6-12 — 5 строк, T-B6-27 — 1, ISS-8 — 2, T-B6-16 — 4; сумма 12 при 11 уникальных файлах, потому что `phase-execution-protocol.directive.xml` пересобирался дважды (T-B6-12 и ISS-8) — учтено верно. Заявленные пофайловые дельты (`+18/−13` у `audit-halt-activation.mjs` и т. д.) сходятся с `git diff --stat` по каждому коммиту.

Зона соблюдена: `git diff --name-only 61b86fb8..6de96024 | grep -E '^(shared|cli|specs|tasks)/|^ai/flow-eval/'` → пусто. Изменения ровно в `ai/kit/**`, сгенерированных `ai/directives/sdd-v2/**` и одном тесте инспектора.

**Тест инспектора не ослаблен — ПОДТВЕРЖДЕНО.** `ai/inspector/core/__tests__/parse-directive.test.ts:93` — единственная добавленная строка `'H_NO_PROGRESS',` внутри `assert.deepEqual(ids, [...])` над отсортированным списком. Проверка осталась точным равенством, список вырос 5 → 6 и по-прежнему совпадает с таблицей `<HaltConditions>` в `execute.directive.xml`. Механическое следствие нового halt-id, поведение инспектора не тронуто.

---

## B. Существо

### T-B6-12 — ПОДТВЕРЖДЕНО с одной оговоркой

Собраны оба ранее висячих аксиома: `phase-execution-protocol.directive.hbs:11-12` — `ax-permitted-bash-commands`, `ax-blocker-escalation`. На базе `61b86fb8` у обоих было 0 шаблонов-потребителей (перепроверено на извлечённом дереве базы), у `ax-re-dispatch` — тоже 0, у `ax-phase-scope-lock` — 1. Появилась таблица `<HaltConditions>` (`:18-23`) с `H_BLOCKED` (`:21`) и `H_OUT_OF_PHASE_WRITE` (`:22`); до этого таблицы в директиве не было вовсе — совпадает с D7.4. Строка-доказательство доски грепается дословно: `«phase worker owns failures only inside its own Target Files»` (`:31`).

**Отклонение исполнителя (ERROR OWNERSHIP прозой, а не в `ax-phase-scope-lock.xml`) — допустимо по букве, расходится с §4.4 трека.** Список «Файлы» и в `62-BATCH-QUEUE.md` «Пачка 17», и в `61-TASK-BOARD.md:151` — только `phase-execution-protocol.directive.hbs`, поэтому файл вне списка не тронут законно. Механически «один дом» не нарушен: `ai/kit/__tests__/axiom-home.test.ts` ловит только повторное определение `<Axiom id="…">`, а здесь добавлена проза, не тег; `npm test` зелен. Практического расхождения сегодня нет — у `ax-phase-scope-lock` ровно один потребитель. Но `40-TRACK-DIRECTIVES-SKILLS.md:779` (§4.4, самый дешёвый путь) прямо называет домом D7.2 именно `ax-phase-scope-lock.xml`, а §1.7 строка D7.2 меряет наличие по этому файлу — значит строка D7.2 формально остаётся «НЕТ» по своему адресу. Правка: неблокирующая задача-довесок «перенести абзац ERROR OWNERSHIP в `ax-phase-scope-lock.xml`, оставив в директиве только ссылку и `H_BLOCKED`».

**Неточность отчёта.** `R-T-B6-12.md` §4 «Отклонение 1» утверждает, что доска «НЕ называя `ax-phase-scope-lock.xml` вовсе». Строка 151 в колонке «Задача» называет его прямо: «`ax-phase-scope-lock`/`ax-permitted-bash-commands`/`ax-blocker-escalation` собраны». Верно только про колонку «Файлы». Там же доска поручает T-B6-12 «исправить `audit-halt-activation.mjs:135-141`» — отчёт называет это «явно передано» T-B6-27; передача подтверждается лишь примечанием «(+ T-B6-27 companion)», а не текстом задачи. На результат не влияет (T-B6-27 в этой же пачке), но формулировку надо поправить.

**Попутный фикс `H_OUT_OF_PHASE_WRITE` — ПОДТВЕРЖДЕНО по существу, ОПРОВЕРГНУТО по механизму.** Дефект реален: `ax-phase-scope-lock.xml` заканчивается «Any other write or deletion → `H_OUT_OF_PHASE_WRITE`», на базе этот текст попадал в собранный `steps/STEP_1_ORIENT.xml`, а строки в таблице не было и в аллоулисте тоже. Но объяснение «иначе мой же `audit:halts` был бы красным» неточно: `audit:halts` был бы красным **и до правки**. Проверено прогоном на извлечённой базе `61b86fb8`: как есть — «clean»; с одной строкой `H_ID_RE.lastIndex = 0; AX_ID_RE.lastIndex = 0;` в начале `auditDirective` — «⚠ 1 halt-activation violation(s): `ai/directives/sdd-v2/phase-execution-protocol.directive.xml` — `H_OUT_OF_PHASE_WRITE` mentioned … but not a row in its own HaltConditions». То есть нарушение существовало и на базе, а гейт его **молча пропускал**.

**Находка верификатора (новая, в файле этой пачки).** `ai/kit/audit-halt-activation.mjs:107` объявляет `const H_ID_RE = /(?<![A-Za-z0-9_])H_[A-Z0-9_]+/g` (и рядом `AX_ID_RE`), а `:279` и `:299` используют их как `!H_ID_RE.test(text)`. `.test` на `/g`-регулярке двигает `lastIndex`, и дальше `auditDirective` (`:261`, `:264`) читает те же регулярки через `body.matchAll(H_ID_RE)` — `matchAll` наследует `lastIndex` исходной регулярки, поэтому часть фрагмента не сканируется вовсе. Класс дефекта: гейт активации остановок может недосчитывать упоминания и давать ложное «clean». На текущем HEAD последствий нет — прогон исправленного варианта на `6de96024` тоже «clean» (33+33), скрытых нарушений сегодня не осталось. Правка: отдельная неблокирующая задача (S) — сбросить `lastIndex` перед каждым сканом + тест-кейс в `ai/kit/__tests__/audit-halt-activation.test.ts` на директиву без `<HaltConditions>`, упоминающую halt из собранного аксиома.

### T-B6-27 — ПОДТВЕРЖДЕНО

Обе записи `ALLOWLIST_CROSS_DIRECTIVE_REFS` были фикцией — перепроверено самостоятельно: `review-lifecycle.directive.hbs` подключает `ax-stateless-flow`, `ax-operator-language`, `ax-tool-invocation`, `ax-operator-agreement` (и два контракта), `ax-permitted-bash-commands` среди них нет; `grep -c AX_PERMITTED_BASH_COMMANDS ai/directives/sdd-v2/review-lifecycle.directive.xml` → 0; `ax-deviation-self-resolve` не подключён ни в один шаблон и ни в одну собранную директиву (0 файлов); `H_PAUSED_AWAITING_OPERATOR` в `execute.directive.hbs` → 0 вхождений. Инертность записей подтверждается кодом: Set читается только когда id реально упомянут в этом же файле (`:262`).

**Диапазон `:138-150` — ПОДТВЕРЖДЕНО дословно.** На базе строки 138–142 — комментарий первой фикции, 143 — сама запись, 144–149 — комментарий второй, 150 — запись; `]);` на 151. Бриф называл `:135-141`, то есть покрывал только первую половину; исправление обеих записей одним коммитом обосновано и зону не расширяет.

**Мелочь отчёта.** В §3 п.1 приведён вывод `grep -n '{{>' … review-lifecycle.directive.hbs` из четырёх строк, тогда как команда даёт шесть (ещё два `contract/process/*`). Цитируемый вывод должен совпадать с командой — либо сузить команду, либо привести вывод целиком.

### ISS-8 — ПОДТВЕРЖДЕНО частично; приёмка брифа НЕ ПРОВЕРЯЕМА

Исключение узкое — ПОДТВЕРЖДЕНО чтением `ax-permitted-bash-commands.xml:56-79`: «strictly under `.claude/tmp/<fixture>/`», `git init`/`add`/`commit` «inside that path ONLY — never the checkout's own `.git`, never a path outside `.claude/tmp/`», путь пишется в Handoff, каталог удаляется до возврата Handoff, явная оговорка «does not relax the ban on mutating the checkout's own repository». Пункт 3 issue #19 закрыт отдельным абзацем (`:33-36`) — безусловный read-only `git status --porcelain -- <Target Files>`. Пункт 4 (`:51-54` в старой нумерации) решён вторым путём: §5 остаётся за `sdd-verify`, и добавлено «the fixture-repository exception above does not create one». Инвариант не ослаблен.

**Замечание по форме (неблокирующее).** Аксиом открывается «This role's bash commands stay within this list:» и списком «Must run / May run» (`:3-11`), а исключение стоит после блока «**Forbidden:**» (`:54`), в списке «May run» ни `mkdir -p .claude/tmp/<fixture>`, ни `git init` не появились. Читателю-агенту разрешение легче не заметить; стоит добавить в «May run» одну отсылочную строку.

**Код-сторона #19 действительно не сделана — ПОДТВЕРЖДЕНО, и адрес в отчёте неточен.** Отчёт ссылается на `phase-run.ts:326-361`; фактический снапшот живёт в `cli/cmd/sdd-verify/workspace-mutation.ts:101` — `const EXCLUDED_TOOL_DIRS = new Set(['.git', 'node_modules'])`, применяется по имени каталога на `:138`. `.claude/tmp/**` не исключён; `grep -rn '\.claude/tmp' shared cli` даёт только payload-ридер и хелпы. Последствие конкретное: фаза, которая по новому разрешению создаёт фикстуру во время §5-строки, ловит `ERR_CLI_SDD_VERIFY_EXTRA_FAILED` — «§5 verification must be read-only» (`cli/cmd/sdd-verify/phase-run.ts:352-360`), то есть ровно тот сценарий, ради которого исключение и вводилось. **Отдельная задача ISS-8b нужна, размер S** (walker уже считает repo-относительный путь `repoPath(root, absolute)`, поэтому точечное исключение `.claude/tmp` пишется без переработки обхода).

Предлагаемая строка доски (колонки как в `61-TASK-BOARD.md` §1):

`| ISS-8b | ISS | Снапшот `sdd-verify` игнорирует `.claude/tmp/**`, чтобы именованное fixture-исключение ISS-8 не падало в «§5 verification must be read-only» | `cli/cmd/sdd-verify/workspace-mutation.ts`, `cli/cmd/sdd-verify/__tests__/phase-run.test.ts` | S | ISS-8 | фикстура: файл создан под `.claude/tmp/<fixture>/` во время §5 → снапшот-граница не срабатывает; файл вне `.claude/tmp/` → по-прежнему срабатывает | #19 п.2 | — | нет | ГОТОВ К ВЫДАЧЕ | код-половина #19; парная — ISS-8 (текст аксиома) |`

**Приёмка «сценарии G1/G3» — НЕ ПРОВЕРЯЕМА.** G1/G3 — это группы эвалов из `50-TRACK-EVAL.md` §3, а не идентификаторы сценариев: `grep -rn '\bG1\b' ai/flow-eval` → 0 попаданий, минимальные эвалы (`E-G1-go-execute` и др.) сами ещё черновики. Формулировка `R-ISS-8.md` §3 п.3 «Сценарии G1/G3 (`ai/flow-eval/scenarios.json`)» вводит в заблуждение — файл существует, но этих сценариев в нём нет и быть не может. Строка ISS-8 на доске не должна получать статус ВЫПОЛНЕНО без оговорки «текстовая часть; приёмка G1/G3 остаётся за треком EVAL».

### T-B6-16 — ПОДТВЕРЖДЕНО по правилу останова; про запись вердикта — ОПРОВЕРГНУТО (текст не нейтрален)

Аксиом собран (`execute.directive.hbs:22`), строка `H_NO_PROGRESS` добавлена (`:42`), правило продублировано в STEP_6_AUDIT_REVIEW (`:179-183`). Строка-доказательство доски грепается. Расширение существующего `ax-re-dispatch.xml` вместо нового id соответствует и файловому списку брифа, и смыслу («Provable-progress»); первый абзац не тронут.

Сознательный отказ от both-way — **соответствует брифу дословно**: `62-BATCH-QUEUE.md` «Пачка 17», строка «Риск»: «в этой пачке правится только правило останова, both-way про запись вердикта остаётся за отложенной темой (§3.4)». Отнесение к V14-2 корректно: механизма записи в дереве нет (`V14-2a`/`V14-2b` — ГОТОВ К ВЫДАЧЕ / ЧЕРНОВИК), а сама строка `T-B6-16` на доске помечена «правка приёмки — V14-2e».

**Но утверждение отчёта «текст сформулирован достаточно обще, чтобы не противоречить будущему both-way» — ОПРОВЕРГНУТО.** `ax-re-dispatch.xml` перечисляет условия отсутствия прогресса как «no changed diff, no new tool output, no new fact the prior attempt did not already have», и завершает «A genuinely new edit … resets the count». Запись `[verdict: pending-operator]` — это изменённый дифф тикета и новая правка, то есть по букве нового текста она **сбросит счётчик** и купит ещё один диспатч. Доска же требует обратного: «повтор блокирующего набора, единственная дельта которого — новая запись `[verdict: pending-operator]`, всё равно даёт `H_NO_PROGRESS` (запись — не новый evidence)». Сегодня противоречия нет (писать вердикт нечем), но оно возникнет ровно в момент выполнения `V14-2b`.

**Дыра во владении.** `V14-2e` — бухгалтерская задача, её файлы `60-ACCEPTANCE.md`, `61-TASK-BOARD.md`; `V14-2c` правит `execute.directive.hbs` и `deviation-review.directive.hbs`. Ни одна не владеет `ax-re-dispatch.xml`, где и нужна оговорка «a deviation-record verdict entry is not new evidence». Правка (неблокирующая): дописать `ax-re-dispatch.xml` в колонку «Файлы» задачи `V14-2c` либо добавить эту оговорку явным пунктом в `V14-2e`, отметив в строке `T-B6-16`, что текст аксиома пока нейтральности не даёт.

---

## C. Смысл директив — ПОДТВЕРЖДЕНО

`git diff 61b86fb8..6de96024 -- ai/kit/templates ai/kit/axiom` прочитан целиком. Смысловая сводка: (1) фазовый агент впервые получает список разрешённых команд и правило эскалации как собранные аксиомы, а не как прозу; (2) у него впервые появляется таблица остановок с двумя строками; (3) в STEP_1/2/3 добавлены три привязки к этим аксиомам; (4) аксиом команд получает два узких послабления по git; (5) оркестратор получает шестую остановку и правило конечности редиспатча. Скрытых изменений поведения сверх задач нет: ни одна ранее существовавшая формулировка не удалена и не ослаблена, все правки — добавления либо переформулировки без смены смысла (единственная переформулировка — перенос уточнения в STEP_3_VERIFY, сделанный ради целостности фразы `run the phase's exact declared commands`, что независимо подтверждается зелёным `stateless-sdd-flow-contract.test.ts`).

Сгенерированные директивы — свежая сборка, проверено независимо: `npm run build:directives` в отдельной копии HEAD в песочнице верификатора, затем `diff -r <копия>/ai/directives <rc-v6>/ai/directives` → каталоги идентичны; `npm run check:directives-fresh` → «✓ ai/directives/** matches a fresh rebuild». Дерево RC при этом не трогалось.

---

## D. Регрессии и пересечения — ПОДТВЕРЖДЕНО (с замечанием о нестабильности прогона)

| Команда | Мой результат | Заявлено | Итог |
|---|---|---|---|
| `npm test` | `# tests 3605 / pass 3595 / fail 0 / cancelled 0 / skipped 10`, exit 0 (прогоны 2 и 3) | 3605/3595/0 | совпало |
| `npm run check` | `[sdd-verify] ✅ ALL PASS (5/5)`: type-check 4.5s, test:coverage 58.3s, lint 10.4s, format 2.2s, yagni 0.8s | ALL PASS 5/5 | совпало |
| `npm run build:directives && npm run check:directives-fresh` | «Generated 55 directive(s)», «✓ matches a fresh rebuild» + побайтовое сравнение каталогов | то же | совпало |
| `npm run audit:sdd-templates` | 5/5 под-гейтов: fresh, axioms 28, contracts 28+33, halts 33+33, budgets — все «✓» | то же | совпало |
| `npm run gate:sdd-check-baseline` | «OK — no error outside the baseline (baseline commit 227c03a8…, tag rc-baseline-1)» | то же | совпало |

**Замечание.** Первый из трёх прогонов `npm test` дал `# tests 3543 / fail 1 / cancelled 5` при длительности 107 с против 44 с у зелёных прогонов. Два последующих прогона — чисто, `grep '^not ok'` пуст. Профиль (усечённое число тестов + «cancelled») соответствует известной недетерминированности раннера, ради которой заведена аналитическая задача `REL-15` (IPC-крах `node --test`), а не изменениям этой пачки: все 11 файлов — директивы, аксиомы и один снапшот-тест. Неблокирующе, но в отчёт пачки стоит вписать, что 3605/3595/0 воспроизводится не с первого раза.

**Пересечения.** `git merge-tree --write-tree` для `6de96024` против `origin/codex/sdd-v2-rc52-followup` (`71ae952d`) и против всех открытых PR — `origin/lead/eval-honest-outcome` (#33), `origin/lead/test-speed` (#36), `origin/lead/verify-core` (#37) — во всех четырёх случаях конфликтов нет, вывод — только SHA дерева. Пересечение файловых множеств пустое во всех четырёх (`comm -12`). PR #31 и #34 уже влиты (`MERGED` 2026-09-08 20:13 и 20:15 UTC), поэтому в списке открытых их нет. Пачка 14 (`lead/journal-round`) на `origin` отсутствует — `merge-tree` **НЕ ПРОВЕРЯЕМ**; по объявленным файлам (`shared/sdd/check.ts`, `group-receipt.ts`, `phase-receipt-check.ts`, `cli/cmd/sdd-log/**`, новый `shared/sdd/execution-log.ts`) пересечение с зоной пачки 17 пустое конструктивно. Оговорка брифа «с пачкой 18 мержить последовательно» остаётся в силе: обе трогают `ai/kit/axiom/**`.

---

## E. Сводный отчёт-черновик PR

Без кодов понятен: раздел «Что это и зачем» объясняет обе половины (границы файлов и умение остановиться) обычными словами, четыре задачи описаны по-человечески, таблица «файл → смысл» читается без обращения к тикетам. Mermaid «было» полностью сверен с базой и подтверждён (три аксиома с нулём потребителей, отсутствие `<HaltConditions>`, пять halt-id в `execute`, две фиктивные записи, тройной запрет git по #19). Mermaid «стало» в сводном отчёте — стрелки соответствуют реальным подключениям (`:11`, `:12`, `:22`), но узлы не несут `file:line` вовсе; на уровне сводки допустимо.

**Пофайловые mermaid требуют правки — ОПРОВЕРГНУТО «стрелка = file:line».** В `R-T-B6-12.md` схема «стало» ссылается на несуществующие файлы `STEP_1_ORIENT.hbs:24-37`, `STEP_2_IMPLEMENT.hbs:38-46`, `STEP_3_VERIFY.hbs:47-57`: шаблонов с такими именами нет (`ls ai/kit/templates/sdd-v2/` — только `.directive.hbs`), шаги живут внутри `phase-execution-protocol.directive.hbs` на строках 25-38, 39-47 и 48-58 соответственно, то есть и адрес, и границы смещены. В `R-T-B6-16.md` узел «execute.directive.hbs:19,42-44» неверен дважды: партиал на `:22`, а прозаическая правка STEP_6_AUDIT_REVIEW — на `:179-183` (верна только ссылка `:42` на строку таблицы). В `R-T-B6-27.md` узел «audit-halt-activation.mjs:135-141 NOTE» расходится с собственным §4 того же отчёта (`:138-150`) и с фактом (NOTE занимает `:138-155`).

**Стало неверным описание базы.** Сводный отчёт утверждает, что `origin/lead/kit-lint` @ `61b86fb8` «на момент старта этой пачки ещё не влита», и предлагает ребейз «после мержа #30». PR #30 влит 2026-09-08 19:21:19 UTC, а первый коммит пачки датирован 19:42 UTC — то есть база была влита **до** старта. Сегодня `git diff --stat origin/codex/sdd-v2-rc52-followup...6de96024` уже даёт ровно 11 файлов, +207/−27: ребейз ничего не сократит, потому что сокращать нечего. Абзац «База ветки» и хвостовую оговорку в «Командах пуша» надо переписать.

---

## Итог

**Блокирующее: нет.** Ни один гейт не красный, зона не нарушена, инварианты не ослаблены, слияние с целевой веткой и всеми открытыми PR чистое.

**Неблокирующее (правки до создания PR — сводный отчёт идёт телом PR):**
1. `R-BATCH-17-phase-agent-bounds.md` — переписать абзац «База ветки» и хвост «Команд пуша»: #30/#31/#34 влиты, база актуальна, diff PR уже равен 11 файлам.
2. `R-T-B6-12.md`, `R-T-B6-16.md`, `R-T-B6-27.md` — исправить `file:line` в схемах «стало» (см. §E); в `R-T-B6-12.md` §4 «Отклонение 1» снять утверждение, что доска не называет `ax-phase-scope-lock.xml`.
3. `R-T-B6-12.md` §4 «Отклонение 2» — переформулировать: нарушение `H_OUT_OF_PHASE_WRITE` существовало и на базе, гейт его молча пропускал; добавить ссылку на находку про `lastIndex`.
4. `R-ISS-8.md` — заменить адрес кода на `cli/cmd/sdd-verify/workspace-mutation.ts:101,138`; снять формулировку «сценарии G1/G3 (`ai/flow-eval/scenarios.json`)» (G1/G3 — группы эвалов, не сценарии).
5. `R-T-B6-27.md` §3 п.1 — привести вывод `grep` целиком (шесть строк, не четыре).
6. Сводный отчёт — вписать наблюдаемую нестабильность `npm test` (1 красный прогон из 3, профиль `REL-15`).

**Неблокирующее (строки доски):**
7. Завести `ISS-8b` (S) — снапшот `sdd-verify` игнорирует `.claude/tmp/**`; готовая строка приведена в §B.
8. Завести задачу (S) на `lastIndex`-дефект `ai/kit/audit-halt-activation.mjs:107,279,299` + тест-кейс.
9. Дать владельца оговорке «запись вердикта — не новый evidence» в `ax-re-dispatch.xml`: расширить «Файлы» у `V14-2c` либо явный пункт в `V14-2e`.
10. Рассмотреть перенос абзаца ERROR OWNERSHIP в `ax-phase-scope-lock.xml` (§4.4 трека 40); строка D7.2 до этого остаётся «НЕТ» по своему адресу.
11. ISS-8 на доске закрывать с оговоркой «текстовая часть; приёмка G1/G3 — за треком EVAL».

**Подтверждено:** состав и порядок 4 коммитов; 11 файлов и +207/−27; соблюдение зоны; сборка обоих аксиомов и `ax-re-dispatch`; обе новые таблицы остановок и шесть halt-id в `execute`; обе строки-доказательства доски грепаются дословно; фиктивность обеих удалённых записей аллоулиста и точность диапазона `:138-150`; узость fixture-исключения и сохранность §5-барьера; неослабленность снапшот-теста инспектора; свежесть сгенерированных директив (побайтово); `npm test` 3605/3595/0, `check` 5/5, `audit:sdd-templates` 5/5, `gate:sdd-check-baseline` OK; отсутствие конфликтов и пересечений файлов с rc52 и PR #33/#36/#37.

**Рекомендация: PR после правок** — правки только текстовые (пп. 1–6) плюс четыре строки на доске (пп. 7–11). Код и директивы в текущем виде принимаются как есть; ветку перед PR ребейзить на `origin/codex/sdd-v2-rc52-followup` (`71ae952d`) — слияние проверено чистым.
