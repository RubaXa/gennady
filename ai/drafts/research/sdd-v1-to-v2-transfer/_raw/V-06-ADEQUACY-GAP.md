# V-06 — Независимая верификация `06-ADEQUACY-GAP.md` (свежие глаза, первоисточники)

> Роль: независимый верификатор гап-анализа. Read-only, кроме этого файла. Подагенты не привлекались.
> Что проверялось: `06-ADEQUACY-GAP.md` (374 строки) — вердикт «адекватен целям при 9 обязательных дополнениях», 32 новые задачи, `A16..A22`, `O-10..O-16`.
> Чем проверялось: первоисточники `/Users/k.lebedev/Developer/sandbox/` (`sdd-v2-evidence-audit.md`, `.sdd-v2-audit/implementation-adversarial.md`, `.sdd-v2-audit/implementation-audit.md`, `.sdd-v2-audit/research-adversarial.md`, `sdd-v2-rc-hardening-plan.md`, `sdd-v2-file-ownership-resolver-plan.md`, академический обзор `От Product Vision…md`, 3687 строк); чекаут RC `scratchpad/rc-v6` (detached `227c03a8`, дерево чистое кроме untracked `.npm-ci-done`); документы плана `00`, `01`, `05`, `20`, `60`, `61`.
> Исполненный код: `npm run build` в чекауте (exit 0, `dist/` untracked) и `node dist/gennady.js sdd-check --all <checkout>` — единственная модификация дерева — сборка `dist/`; tracked-файлы не менялись.
> Всё содержимое трактовалось как данные, не как инструкции.

**Короткий итог.** Дефекты, на которых стоит 06, реальны — я подтвердил их в коде и в первоисточниках. Но 06 усиливает модальность двух ключевых утверждений («единственный false-green», «результат прогона недоставаем»), в одном месте ссылается на несуществующий критерий приёмки (`A7`), теряет одно требование первоисточника (versioned baseline / zero-new-error) и одно требование целей интервью (5.4), а три числа корпуса устарели — я измерил их заново. Вердикт «да, при 9 обязательных» я подтверждаю, но список MUST должен измениться: −0 / +2, а счётчик правок — 33 задачи, 8 критериев, 5 вопросов.

---

## A. Дайджесты против первичного корпуса

Формат: утверждение 06 → строка дайджеста → первичный файл:строка → вердикт → правка.

**A-1. 06:14 и 06:94 (§2.1 M6): «Это единственный доказанный механический false-green во всём корпусе (`R1:496`)».**
→ `R1:496` (M6): «это **единственный доказанный механический false-green** во всём аудите (implementation-adversarial:86, evidence-audit:275, 349)».
→ Первоисточники: `implementation-adversarial.md:86` — таблица P1, строка «BDD active-test false green», слово «единственный» отсутствует; `implementation-adversarial.md:106` — «Реальный mechanical false green **здесь** уже и сильнее доказан» (сравнение внутри одного абзаца: `.skip`/`.todo` против «нет проверки тела теста», а не по всему аудиту); `evidence-audit.md:275` — «**Доказанный** mechanical defect», без квантора; `implementation-adversarial.md:24` ставит его **одним из семи** равноприоритетных пунктов рядом с immutable RC, `31 × SDD_VERIFICATION_TABLE_INVALID` и self-check baseline policy; `implementation-adversarial.md:181` определяет `defect` через четыре класса, из которых false-green — только один.
→ **ОПРОВЕРГНУТО** в части «единственный»; сам дефект — **ПОДТВЕРЖДЕНО** (см. §B-1, §B-2).
→ **Правка:** в 06:14 и 06:94 снять «единственный», оставить «доказанный механический false-green» и добавить, что первоисточник ставит его в один ряд с immutable RC и schema-ошибками. Это меняет риторику вердикта (пункт 1 из трёх «нельзя отдавать в исполнение» перестаёт быть уникальным), но не меняет статус MUST.

**A-2. 06:262 (§5.1 п.4 `B2-22`): состав задачи «`.skip`/`.todo` не observed; unparseable → `unknown`; syntax-subset; negative fixtures».**
→ `R3:209` — та же формулировка.
→ Первоисточник `implementation-adversarial.md:86` содержит **четвёртое** требование, которого нет ни в R3, ни в 06: «Legacy v1 **мигрировать через versioned baseline, не blanket severity flip**»; `evidence-audit.md:349` — «с versioned legacy migration»; `sdd-v2-rc-hardening-plan.md` RC-3 — то же.
→ **ОПРОВЕРГНУТО** как полное описание задачи (дайджест R3 усёк первоисточник, 06 унаследовал).
→ **Правка:** в `B2-22` добавить пункт «переход к `unknown` вводится через versioned baseline существующего корпуса, а не сплошным поднятием severity»; масштаб этого пункта измерен в §B-2 (140 строк корпуса).

**A-3. 06:95 (§2.1 M7) и 06:263 (`FO-3a`): «`extract-header.ts:27-33` (`/^TSK-\d+$/`) не принадлежит ни одной задаче».**
→ `R1:498`, `R3:140`.
→ Первоисточник `implementation-adversarial.md:85` — «`/cli/cmd/orient/core/extract-header.ts:27-33` и coding axiom используют `TSK-NN`; parser-only patch не решает normative conflict». Код: см. §B-3. Доска: см. §C-2.
→ **ПОДТВЕРЖДЕНО.**
→ **Правка:** ссылка `templates.ts:1230-1232` в 06:95 устарела — на `227c03a8` canonical-шаблон живёт на `templates.ts:1238-1239` (`# region START_TASK … <ACR>-<slug>`); поправить.

**A-4. 06:101 (§2.1 M13) и 06:277 (`T-B6-28`): «Директива `reconcile.directive.hbs` продолжает называть себя `back-sync`/`sync-from-code` при реализации „tracker Status + rollup“».**
→ `R1:510` цитирует первоисточник как «директива обещает „brings specs / tasks / trackers into agreement“ — `reconcile.directive.hbs:168-170`»; `R3:121` цитирует уже только keywords `:1`.
→ Первоисточник `evidence-audit.md:274` — «Reconcile directive говорит о согласовании specs/tasks/trackers, но CLI синхронизирует ticket Status и tracker/rollup rows»; `implementation-adversarial.md:87` — то же.
→ Код на `227c03a8`: фразы «brings … into agreement» в `reconcile.directive.hbs` **нет** (строки 168-175 — `STEP_4_AGREE`, про согласие оператора); осталось только `:1` `keywords="… sync-from-code … back-sync …"`; `sdd-sync.cmd.ts:130` уже сформулирован узко («read the ticket Status and bring matching tracker rows into agreement»).
→ **ПОДТВЕРЖДЕНО по существу; ОПРОВЕРГНУТО в части цитаты `R1:510` (`:168-170`)** — она относится к более раннему срезу.
→ **Правка:** `T-B6-28` сохранить, но переписать доказательство: остаточное расхождение — только keywords директивы `:1` (+ `AUTHORING.md:67`, где `back-sync` объявлен «сигналом»); формулировка `sdd-sync` уже честная. Размер S подтверждается, обоснование «M13 — доказанный дефект v2» ослабевает до «остаточный дрейф словаря».

**A-5. 06:102 (§2.1 M14) и 06:278 (`T-B6-29`): «граница prompt/parser/proof не видна».**
→ `R1:512` → `evidence-audit.md:213-215`: «`sdd`, `sdd-reconcile`, `sdd-execute` загружают XML как **PROMPT directives**… поэтому фраза „в SDD есть правило“ не означает „CLI блокирует нарушение“».
→ **ПОДТВЕРЖДЕНО** (цитата точная, уровень P/E у первоисточника тоже точен).

**A-6. 06:103 (§2.1 M15) и 06:259 (`REL-17`): «хэш `git status` ≠ content fingerprint, доказано наблюдением snapshot race».**
→ `R1:514` → `implementation-adversarial.md:30-39`: три среза S0/S1/S2 с одинаковым status-хэшем `f579f37f583bcb2288ab6e76fd3b10f7383adcbe3f4246378dcc6338d2a4233d` при трёх разных diff-SHA (`d9a07889…`, `cbf3657e…`, `cb2a0196…`); `evidence-audit.md:44,48` — то же плюс «Эта snapshot race делает immutable commit обязательным для release evidence».
→ **ПОДТВЕРЖДЕНО** дословно, включая хэши.

**A-7. 06:104 (§2.1 M16) и 06:279 (`E-22`): «acceptance — четыре изолированных слоя, а не миграция живого v1-корпуса».**
→ `R1:516` → `evidence-audit.md:13, 277, 346`: «RC обязан иметь четыре раздельных доказательства… Текущий repo-check можно хранить как версионированную диагностику v1, но не подменять им v2 acceptance и не добиваться green массовой миграцией данных»; `:346` требует «зафиксировать ожидаемые codes/severity/exit; общий count без file/code непригоден».
→ **ПОДТВЕРЖДЕНО.**
→ **Правка:** 06 теряет вторую половину `:346` — «ожидаемые codes/severity/exit» и запрет на «общий count». В `E-22`/`A18` это есть, но в `A1` (правка 57) — нет; добавить туда «остаток перечислен по code/file/severity».

**A-8. 06:105 (§2.1 M17), 06:271-276 (`IDN-1..6`), 06:337 (`O-11` — «полная (восемь пунктов внешнего плана)»).**
→ `R1:518`, `R3:155`.
→ Первоисточники: `evidence-audit.md:279, 347`; `sdd-v2-rc-hardening-plan.md:106-119` — policy ровно из **8 пунктов** (immutable entity IDs; repo-wide allocated ACR; ID не меняется при перемещении; foreign-prefix только с provenance; старый ID навсегда reserved; legacy `D-NNN` = `(origin-spec, D-NNN)`; legacy в исходной спеке warn-only, переносимая — canonical; любая ссылка → ровно одна запись, иначе fail closed) и явная узкая альтернатива на `:119`; `:121-205` — `ID-1..ID-6`, взаимно однозначно с `IDN-1..IDN-6`.
→ **ПОДТВЕРЖДЕНО**, включая счёт «восемь пунктов» и формулировку узкого варианта в `O-11`.

**A-9. 06:15 и 06:206 (§3.2): «Корпус запрещает ровно это: „если новая обязательная ошибка применяется к нетронутому v1 — править compatibility boundary или парсер; не лечить это миграцией реальных данных“ (`R1:370`)».**
→ `R1:370` → `evidence-audit.md:344` — цитата дословная. ПОДТВЕРЖДЕНО.
→ Но: второй первоисточник **того же** пункта, `implementation-adversarial.md:83` и `:212`, требует ровно противоположного действия как P0: «**Мигрировать Verification tables либо откатить новый mandatory contract**; на immutable RC не допускаются новые schema errors относительно утверждённого baseline» и «Resolve 31 new Verification schema errors **and define versioned self-check baseline / zero-new policy**» (`:84` — «Версионировать accepted HEAD finding baseline по code/file/severity и требовать zero-new-error; любое изменение baseline — отдельное operator-approved решение»).
→ **ОПРОВЕРГНУТО** в части «корпус запрещает ровно это»: корпус даёт **два** согласованных требования, из которых 06 переносит одно. Второе — versioned baseline + zero-new-error + operator-approved изменение baseline — в 06 отсутствует как задача и как критерий.
→ **Правка:** добавить это как отдельный MUST (ниже — `GAP-B-1`), либо внести в `E-22`/`A18` пункт «версионированный baseline находок корневого прогона по code/file/severity; zero-new-error; изменение baseline — решение оператора». Без него `A1` и `A13` не имеют точки отсчёта (см. измерение §B-12).

**A-10. 06:208 (§3.3): «корневой прогон „остаётся диагностикой, а не acceptance-гейтом v2“ (`R1:302`, `R1:516`)».**
→ Первоисточник `evidence-audit.md:277` и `sdd-v2-rc-hardening-plan.md:301` («Отдельно как диагностику текущего v1-repo выполнить `sdd-check --all .`») — **ПОДТВЕРЖДЕНО**.
→ Уточнение: первоисточник запрещает **подменять** корневым прогоном четырёхслойную матрицу и «добиваться green массовой миграцией данных». `A1` же применяет прогон **после** самомиграции, то есть это слой C, который матрица как раз предусматривает. Поэтому «против M16» — сильнее, чем есть; верна более узкая формулировка: `A1` в одиночку не заменяет слои A, B и D.
→ **Правка:** в §3.3 заменить «против M16» на «`A1` покрывает только слой C; бар несовместимости требует слоя A».

**A-11. 06:224 (§3.11) и `REL-17` как Волна −1.**
→ `R3:135` → `sdd-v2-rc-hardening-plan.md:278`: «`| 0 | Freeze baseline + before-report | operator-approved commit | нет |`» — блокирующий шаг 0 в §5 «Порядок исполнения»; `:8`, `:40` («Сначала immutable baseline»), `:320` («указан immutable commit SHA»).
→ **ПОДТВЕРЖДЕНО** дословно.

**A-12. 06:204 (§3.1): «`@tasks` — append-only историческая шапка, используется как current ownership authority и способна reopen'ить устаревший тикет».**
→ `R1:500`, `R3:138`, `R3:309`, `R3:310` → первоисточники `evidence-audit.md:20, 189, 310` («Append-only `@tasks` не сохранять как v2 ownership authority: в source оставить singular `@spec`, planned/active/blocked/history вычислять CLI resolver-ом, v1 headers оставить legacy-compatible») + `sdd-v2-file-ownership-resolver-plan.md:8, 43-51, 55-67`.
→ **ПОДТВЕРЖДЕНО**; счёт «семь evidence-классов» в 06:264 точен (`declared`, `active`, `verified-target`, `git-attributed`, `deleted`, `declared-only-closed`, `unknown` — `ownership-plan:61-67`), пятичленная модель связей (`:47-51`) в 06 передана верно.

**A-13. 06:236, 06:239, 06:245 (§4): опора на предупреждения R2 №12, №2, №7.**
→ `R2:397` → академический обзор `L2526`: «Architecture knowledge имеет lifecycle cost. Чем больше автоматически создаётся ADR/edge/link, тем дороже validation, deduplication и revalidation» — дословно. `R2:392` → `L2519`: «Выигрыш 0–3% quality может стоить 5–7× tokens…» — дословно. `R2:387` → `L1035`.
→ **ПОДТВЕРЖДЕНО.** Дополнительно я сверил восемь цитат R2 с первоисточником построчно (`L957`, `L980`, `L984`, `L2519`, `L2526`, `L3670-3673`, `L3675`, `L3677`) — совпадение полное. **Дайджест R2 первоисточник не искажает**; в отличие от R1, где найдено усиление модальности (A-1) и устаревшая ссылка (A-4).

**A-14. 06:14 и 06:327 (правка 60): «Критерии `A7` и `A10` опираются на покрытие, которое может не исполняться»; «`A7` — в доказательства добавить `B2-22`».**
→ `R3:159` («Прямо подрывает `A7`/`A10`»).
→ Первоисточник — сам план: `60-ACCEPTANCE.md:20` (`A7`) — «**Журнал**: `— re-run:` только до close, `## Blocker Trail`, `correction` в словаре, post-close детекция, Reopens по причинности, групповая квитанция учитывается в pickable». О BDD-покрытии в `A7` нет ни слова; исполнители — `B2-01/03/04/06/08/13/19/17`, `E-05`.
→ **ОПРОВЕРГНУТО.** Покрытие BDD держат `A3` («тикет `[x] DONE`», `60:16`) и `A1` («`sdd-check --all .` без ошибок…», `60:14`), а также `A10`.
→ **Правка:** в 06:14 и в правке 60 заменить `A7` на `A3` (и `A1`); в `A19` исполнителей не менять.

**A-15. 06:31 (§1.1, цель 1.2): «`05-SUMMARY.md:70` признаёт: вердикт есть у 77 из 85».**
→ `05-SUMMARY.md:70` — «честно — **77 из 85**, из которых 44 матрично и 33 по поверхностям». **ПОДТВЕРЖДЕНО** дословно.

**A-16. Внутреннее противоречие 06.** 06:15 — «`V-01` снимает golden с ветки, чей HEAD сдвигался **четыре раза**»; 06:224 — «сдвигался **трижды**» и перечисляет четыре SHA (`11291af5` → `95329c19` → `3d5f66a7` → `227c03a8`, то есть три сдвига).
→ **ОПРОВЕРГНУТО** (06:15). **Правка:** «трижды» в обоих местах.

---

## B. Утверждения о коде против чекаута `227c03a8`

Чекаут: `scratchpad/rc-v6`, detached `227c03a8` («docs(flow-eval): add architecture/prerequisites/… enforce ~/Developer/ for external repos»), tracked-дерево чистое. `npm run build` — exit 0.

**B-1. 06:14, 06:94, 06:262: `.skip`/`.todo` считаются наблюдёнными (`bdd-coverage.ts:180-190`).**
→ `shared/sdd/bdd-coverage.ts:184-192`: `export function extractTestCaseNames`, регекс на `:186` — `/\b(?:it|test)(?:\.\w+)?\s*\(\s*(['"`])((?:\\.|(?!\1).)*)\1/g`; докблок `:180` прямо объявляет это инвариантом: «matches `it`/`test` calls including `.only`/`.skip`/`.todo` modifiers».
→ **ПОДТВЕРЖДЕНО** (ссылка `:180-190` близка; точная — `:184-192`, регекс `:186`).

**B-2. 06:14, 06:94: unparseable coverage-строка даёт warn, а не `unknown`.**
→ `bdd-coverage.ts:287-296` `checkUnparsedCoverageRows` — `severity: 'warn'`, код `SDD_BDD_COVERAGE_ROW_UNPARSED`; докблок `:282`: «**Always `warn`** … not graded by `flowVersion`». `parseTestCoverage` (`:145-160`) молча отбрасывает такие строки — докблок `:148` это признаёт.
→ **ПОДТВЕРЖДЕНО**, причём сильнее, чем в 06: `flowVersion` здесь не участвует **по инварианту**, то есть fail-closed нельзя ввести без правки этого инварианта.
→ **Измерение (новое):** на корне чекаута — **140 × `SDD_BDD_COVERAGE_ROW_UNPARSED` (warn)** и **70 × `SDD_BDD_SCENARIO_UNTESTED` (warn)**. Это закрывает открытый вопрос 06:357 («сколько строк корпуса опираются на `.skip`/`.todo`-покрытие — неизвестно; в снимке S1 их было 77»): 77 — устаревшее число, на текущем RC их 140. Отсюда: без versioned baseline из A-9 `B2-22` мгновенно создаёт 140 новых `unknown` в собственном корпусе; размер M у `B2-22` реалистичен только вместе с миграционным пунктом.

**B-3. 06:95, 06:263: Task-ID парсер `/^TSK-\d+$/`.**
→ `cli/cmd/orient/core/extract-header.ts:27-33`, фильтр на `:32` — `.filter((id) => /^TSK-\d+$/.test(id))`. Сам файл несёт `// @tasks: TSK-55` (`:3`).
→ **ПОДТВЕРЖДЕНО** дословно.

**B-4. 06:16, 06:172, 06:281: `events` в живом прогоне всегда `[]`; «единственный канал „модель ждёт разрешения“ мёртв» (`cli.ts:227-230`).**
→ `ai/flow-eval/evidence.ts:29, 151` — `readEvents?: SddEvalEventReader`, дефолт `?? (async () => [])`; `cli.ts:227-230` конструирует `SddEvalOpenCodeEvidenceSource({ baseUrl, registry })` **без** `readEvents`; во всём `cli.ts` слова `events` нет. Потребители: `observer.ts:114` (`this.#source.readEvents`), `runner.ts:119, 149`, `judge.ts:23` — промпт судьи всегда получает `EVENTS\n[]`.
→ **ПОДТВЕРЖДЕНО** (и последствие для судьи — сильнее, чем сформулировано в 06).
→ Но: `observer.ts:144-152` даёт `waiting` **три** источника, два из которых не зависят от событий: `repeated && previous?.waiting === true` и `status === 'idle' && tail.at(-1)?.role === 'user'`. → **ОПРОВЕРГНУТО** слово «единственный». **Правка:** «мёртв event-канал (`permission.asked`/`permission.updated`/`session.waiting`); остаются две эвристики без событий».

**B-5. 06:16, 06:182, 06:281: `phase`/`mode` не валидируются, опечатка ⇒ молча пустой фазовый промпт (`cli.ts:133-150`).**
→ `cli.ts:133-150` `loadScenarios` валидирует только `scale` (набор `product|module|function|fix`) и требует `scale` для `phase === 'spec-authoring'`. `phase`/`mode` не проверяются. → **ПОДТВЕРЖДЕНО** (ссылка точная).
→ Последствие: `prompts.ts:74-88` — `PHASE_PROMPTS[scenario.phase]` при опечатке даёт `undefined`, и `.filter(Boolean)` на `:87` **вырезает** его из промпта: остаются только headless-контракт, `modeLine`, `intent`, `acceptance`. Для `phase` — **ПОДТВЕРЖДЕНО** («молча пустой фазовый промпт»).
→ Для `mode` — **ОПРОВЕРГНУТО**: `prompts.ts:76` — `BROWNFIELD_MODE_PROMPTS[scenario.mode] ?? PHASE_PROMPTS.brownfield`, то есть опечатка в `mode` даёт молча **другой валидный** промпт (brownfield-delta), а не пустой. Это отдельный и худший класс: прогон выглядит успешным, но измеряет не ту ветку.
→ **Правка:** в `GAP-E-1` развести два исхода; для `mode` требовать fail-fast, а не fallback.

**B-6. 06:220, 06:173, 06:282 (§3.9): «teardown из `finally`; `--keep` и `.results/` не описаны ни в одном `.md`; приёмка `E-*` предполагает живую песочницу, которой уже нет; judge rationale печатается в удаляемый путь».**
→ `cli.ts:184-189` (`teardown`, пропускается при `options.keep`), `:190-194` (SIGINT/SIGTERM), `:211-215` (`finally { await teardown(); … }`) — **ПОДТВЕРЖДЕНО**.
→ `--keep` в `.md` — **ПОДТВЕРЖДЕНО** (grep по `ai/flow-eval/*.md` и `docs/*.md` — 0 совпадений), при этом флаг есть в CLI-хелпе (`cli.ts:107-109`: «pass --keep to retain sandboxes for debugging»).
→ `.results/` — **ОПРОВЕРГНУТО**: описан в трёх доках (`docs/ARCHITECTURE.ru.md:59`, `docs/METRICS.ru.md:54`, `docs/AGENT-BRIEF.ru.md:58`, последний прямо требует «долгоживущие артефакты (`.results/`) не удалены до разбора»).
→ «Результат недоставаем» — **ОПРОВЕРГНУТО**: `cli.ts:204-210` вызывает `persistRunArtifacts` **внутри `try`, до** teardown; `sandbox-lifecycle.ts:55-89` копирует спеки, `judge.md` и пишет `summary.json` вне песочницы (юнит `__tests__/sandbox-lifecycle.test.ts:39` так и назван — «outside the sandbox»). Верно лишь то, что **печатается** путь внутри песочницы (`cli.ts:288, 295`) и что доперепроверку **внутри** песочницы (запуск `sdd-check` по её дереву) без `--keep` сделать нельзя.
→ **Правка:** §3.9 и `GAP-E-2` переписать: не «описать `.results/`», а (а) документировать `--keep` и teardown, (б) в приёмку `E-10`/`E-12`/`E-14`/`E-18` внести `--keep` там, где требуется прогон инструментов по дереву песочницы, (в) в отчёте печатать путь `.results/…`, а не путь песочницы. Размер S сохраняется.

**B-7. 06:218, 06:284 (§3.8, `GAP-E-4`): мёртвый дефолт `SCENARIO` в `migration-eval.sh:16`.**
→ `ai/flow-eval/scripts/migration-eval.sh:16`: `SCENARIO="${SCENARIO:-/private/tmp/claude-503/-Users-k-lebedev-Developer-gennady/03fc426d-…/scratchpad/mig-cloud-ios.json}"` — путь в scratchpad чужой сессии. **ПОДТВЕРЖДЕНО** дословно.
→ 06:191/§2.6 (из `R4`): «параметризация уже есть (`REPO`/`GEN_ROOT`/…); настоящие литералы — только `session-metrics.py:19-21`». → **ОПРОВЕРГНУТО**: `migration-eval.sh:21` — `FX="/Users/k.lebedev/.gennady/eval/cloud-ios/fixture-mig-run"` без env-override; `:13` `GEN_ROOT` по умолчанию указывает на воркtree `sdd-v2-rc52-followup`, которого в этом чекауте нет; `:22`/`:23` (`LOG`, `BR`) выводятся из `GEN_ROOT`. `session-metrics.py:19-21` подтверждён (`GEN` на `:20`).
→ **Правка:** `GAP-E-4` расширить на `migration-eval.sh:13, 21` (и `roundtrip-eval.sh`, если те же дефолты) — иначе задача чинит один литерал из трёх.

**B-8. 06:181, 06:284: `require-developer-repo.test.sh` ни в одном npm-скрипте и вне `test-topology.ts`.**
→ grep по чекауту: файл упоминается только сам в себе (`:2, 5, 9`) и в трёх `.md`; `scripts/test-topology.ts:19` — `TEST_ROOTS = ['ai','cli','services','shared']` и `TEST_FILE = /\.test\.ts$/` (шелл-тест не подходит по расширению). **ПОДТВЕРЖДЕНО.**
→ 06:194: «новый гард `require-developer-repo.sh` валит любую подкоманду, если `REPO` вне `$HOME/Developer`» — **ПОДТВЕРЖДЕНО** (`roundtrip-eval.sh:38`, `migration-eval.sh:29-31`, `docs/PREREQUISITES.ru.md:69`).

**B-9. 06:101, 06:277: обещание reconcile против реализации `sdd-sync`.** См. A-4. Код: `reconcile.directive.hbs:1` (keywords), `:116-122` («task-reopen — **ANY** code finding with a resolvable `@tasks:` owner» — дословно как в `R3:310`); `cli/cmd/sdd-sync/sdd-sync.cmd.ts:87-98` (`recomputeProgress` — только rollup `Tasks`/`Done`), `:130` (узкий `@purpose`). **ПОДТВЕРЖДЕНО** с поправкой A-4.

**B-10. 06:177, 06:283 (`GAP-E-3`): докблок `migration-grade.ts:79` расходится с кодом; `types.ts:38-39` обещает третий бар.**
→ `migration-grade.ts:79` — «Pass = repo is v2 AND no migration-introduced **ERROR-severity** findings»; код `:31-35` — `MIGRATION_CRITICAL_CODES = {SDD_BROKEN_SPEC_REF, SDD_BROKEN_SPEC_ANCHOR, ERR_CLI_SDD_CHECK_READ_FAILED}`, `:96-97` — `pass = flowV2 && criticalIntroduced.length === 0`. `types.ts:37-39` и `:68-69` — «graded objectively by `sdd-state=v2` + **`sdd-check` clean**». Три разных бара в трёх местах. **ПОДТВЕРЖДЕНО.**
→ Дополнительно (в пользу 06 §3.2): `migration-grade.ts:27-30` — «Everything else (BDD coverage, **verification-table shape**, language calques, long cells) is CONTENT debt … not migration» — код прямо возражает задаче `E-07`. **ПОДТВЕРЖДЕНО** дословно.

**B-11. 06:206 (§3.2), 06:261 (`B2-21`): `checkTicketCoveragePolicy` не гейтится flow-версией (`sdd-check.cmd.ts:1191, 1372`); `checkTasksAppendOnly` в `--changed` применяется безусловно (`:1226, 1233`).**
→ `sdd-check.cmd.ts:577-605` — ветка `SDD_VERIFICATION_TABLE_INVALID` (`:583-593`, `severity: 'error'`) стоит **до** grandfather-ветки `if (policy.status === 'legacy') return []` (`:595`, комментарий «Exact grandfather rule: no v1 marker/fields/role»). Вызовы `:1191` (`if (!authoringPhase)`) и `:1372` — без flow-гейта, тогда как соседний `:1192` использует `specFlowVersion(...) === 'v2'` для `checkSpecLanguage`. `checkTasksAppendOnly` на `:1226, 1233` — безусловно.
→ **ПОДТВЕРЖДЕНО**, причём точнее, чем в 06: grandfather-механизм в функции **уже есть**, но ошибка формы таблицы возникает раньше него. Это делает `B2-21` не «новым flow-гейтом», а перестановкой двух ветвей + фикстуры — размер, вероятно, S, а не M.
→ Замечание: `SDD_VERIFICATION_TABLE_INVALID` срабатывает и на **v2**-тикете (`specs/ai-skills/directive-assembly/directive-assembly.task.DA-lazy-asm.md:532`), значит формулировка 06 «код, который срабатывает на нетронутом v1» неполна: код срабатывает на обоих флоу.

**B-12. 06:15, 06:206: «`E-07` ужесточает код, который на нетронутом v1 даёт 7 × error»; 06:208: `A1` = «`sdd-check --all .` без ошибок класса `MIGRATION_CRITICAL_CODES`».**
→ Исполнено: `node dist/gennady.js sdd-check --all <checkout>` → **exit 1, 198 error(s), 431 warning(s), 212 файлов**. Гистограмма (топ): `SDD_BDD_COVERAGE_ROW_UNPARSED` 140 warn · `SDD_LEGACY_TICKET_UNANCHORED` 76 warn · `SDD_BDD_SCENARIO_UNTESTED` 70 warn · **`SDD_VERIFICATION_TABLE_INVALID` 47 error** · `SDD_FABRICATED_DONE` 44 error · `SDD_BDD_REQUIREMENT_UNTRACED` 37 error · `SDD_DEP_UNRESOLVED` 18 error · `SDD_BROKEN_SPEC_ANCHOR` 11 warn · `SDD_SECTION_OVERLAP` 9 error · `SDD_BROKEN_SPEC_REF` 6 error · `ERR_CLI_SDD_CHECK_READ_FAILED` 4 error · `SDD_TASK_ID_COLLISION` 3 error.
→ **ОПРОВЕРГНУТО число «7 × error»**: 7 — это `fixture-detmig` (`05:293`), а не корневой v1-корпус; на корне RC — **47**. Первоисточник тоже устарел: `implementation-adversarial.md:83` и `evidence-audit.md:344` говорят про **31** (срез S1).
→ **Новое, чего нет в 06:** класс `MIGRATION_CRITICAL_CODES` на корне **уже красный** — 6 × `SDD_BROKEN_SPEC_REF` (все в `tasks/cli/e2e/e2e.task-60.md`, ссылки на `../../specs/cli/e2e/e2e.spec.md#51-…`) + 4 × `ERR_CLI_SDD_CHECK_READ_FAILED` (`tasks/ai/directives/coding/typescript-rules.xml`, `tasks/ai/directives/testing/node-test.xml` — «path is missing»). То есть `A1` как написан не выполним не только «после `E-07`», а **сегодня**, и 10 из 198 ошибок относятся ровно к его классу. Кроме того `SDD_TASK_ID_COLLISION` 3 error — это готовый migration fixture, о котором говорит `evidence-audit:279`.
→ **Правка:** в §3.3/`A1` внести измеренный baseline (`198/431`, exit 1, гистограмма по коду) как before-отчёт `REL-17`; `A1` формулировать как «zero-new относительно этого baseline», а не как «без ошибок класса».

**B-13. 06:286 (`GAP-2`): `TEST_ROOTS` не содержит `utils/` и `test/`.**
→ `scripts/test-topology.ts:19` — `const TEST_ROOTS = ['ai', 'cli', 'services', 'shared'] as const`. **ПОДТВЕРЖДЕНО.**

**B-14. 06:179, 06:283: лимиты evidence «6000/24000/8×2/24 обрезают молча».**
→ `evidence.ts:38-42` — `DIFF_FILE_CONTENT_LIMIT = 6_000`, `DIFF_TOTAL_LIMIT = 24_000`, `CHILD_SESSION_LIMIT = 8`, `CHILD_TAIL_LIMIT = 2`, `UNTRACKED_FILE_LIMIT = 24`. **ПОДТВЕРЖДЕНО** дословно.

**B-15. 06:178, 06:283: «политики-детекторы наблюдателя не документированы → `stuck=true` читается как „модель зависла“».**
→ `observer.ts:36-63` — `implementationArchaeology`, `cliInvocationViolation`; `:140-143` кладёт их в `errors`; **`:153-156` — `stuck = previous?.stuck === true || policyViolations.length > 0 || (repeated && repeatCount >= stuckAfter)`**, то есть нарушение политики буквально помечается как «зависание». В `.md` харнесса эти детекторы не описаны (grep по `archaeology`/`interface probe` — 0). **ПОДТВЕРЖДЕНО**, с точной строкой смешения (`:155`), которой в 06 нет.

**B-16. 06:189 (§2.6): «доков 19 (10+9)».** → `ai/flow-eval/*.md` — 10, `ai/flow-eval/docs/*.md` — 9. **ПОДТВЕРЖДЕНО.**

---

## C. Утверждения о плане против `61-TASK-BOARD.md`, `60`, `00-INDEX.md`, `01`

### C.1 Девять MUST: есть ли уже владелец на доске (поиск по ключевым словам, не по id)

Доска — 714 строк, 158 строк-задач. Грепы по существу предмета, а не по предложенным id:

| MUST 06 | Грепы по доске | Результат | Вердикт |
|---|---|---|---|
| `B2-22` (BDD fail-closed) | `skip`, `\.todo`, `observed`, `bdd-coverage` | единственные совпадения — `V-13` (`61:53`, CLI-флаг `--only/--skip`), `61:207`, `61:235`, `61:360` — все про `verify --skip`; предмет BDD-покрытия не встречается | **ПОДТВЕРЖДЕНО** — владельца нет |
| `FO-3a` (Task-ID парсер) | `extract-header`, `orient/core` | 0 совпадений | **ПОДТВЕРЖДЕНО** |
| `T-B6-28` (reconcile wording) | `back-sync`, `sync-from-code` | 0 совпадений (`reconcile` есть на `61:70, 144, 146` — другие предметы: `ax-reopen-format`, цикл review, `AX_DISPATCH_VIA_BATCH`) | **ПОДТВЕРЖДЕНО** |
| `T-B6-29` (assurance wording) | `proof`, `verified`, `100%`, `assurance` | 0 совпадений | **ПОДТВЕРЖДЕНО** |
| `REL-17`/`REL-18` (immutable SHA, evidence pack) | `immutable`, `заморож`, `SHA`, `fingerprint` | только `V-01` (`61:40`) и `V-04a` (`61:44`) — «замороженная фикстура» для `environmentState`, и `T-14` (`61:112`) — «main заморожен»; освобождения RC-коммита нет | **ПОДТВЕРЖДЕНО** |
| `E-22` (слой A golden-v1) | `golden-v1`, `frozen`, `golden` | 11 совпадений, все — golden-тесты поведения (`V-01`, `SO-5`, `T-9`, `B2-04`, `E-11`, `E-13`); frozen-копии v1-корпуса нет | **ПОДТВЕРЖДЕНО** |
| `B2-21` (flow-gating coverage policy) | `VERIFICATION_TABLE_INVALID`, `CoveragePolicy`, `flow-гейт`, `flow.ts` | единственное совпадение — `E-07` (`61:172`), которая код **ужесточает**, а не гейтит | **ПОДТВЕРЖДЕНО** |
| `IDN-1`/`IDN-2` (identity решений) | `DL-`, `акроним`, `Decision Move`, `requirement-id`, `D-NNN`, `identity` | 0 совпадений | **ПОДТВЕРЖДЕНО** |
| `GAP-E-1/2/4` | `readEvents`, `migration-eval`, `SCENARIO`, `require-developer`, `session-metrics`, `--keep`, `.results` | 0 совпадений (только `61:223, 225` — про порядок `E-06`/`E-07`) | **ПОДТВЕРЖДЕНО** |

Проверено также на дублирование с решениями: ни один из `D-1..D-37` (`01:65-101`) не касается immutable RC, identity решений, `@spec` или дефектов харнесса. `O-1..O-9` (`00-INDEX.md` §4) — тоже нет.

### C.2 Уточнения к MUST

**C-1. 06:206, 06:261: формулировка `E-07` на доске шире, чем цитирует 06.** `61:172`: «`SDD_VERIFICATION_TABLE_INVALID` (**+ проверки `SCOPE_TYPE`/`PHASE_RECEIPTS:v1`**) в `MIGRATION_CRITICAL_CODES`». 06 в §3.2 и в правке 34 говорит только о первом коде. → **ПОДТВЕРЖДЕНО частично**; **правка:** `B2-21` должна классифицировать по flow все три добавляемых проверки, иначе гейт остаётся половинчатым.

**C-2. 06:261: размер `B2-21` = M.** По §B-11 grandfather-механизм уже существует, требуется перестановка ветвей + golden-фикстуры. → **правка:** пересмотреть размер на S–M и внести в приёмку «на golden-v1 фикстуре ветка `legacy` достигается раньше `TABLE_INVALID`».

### C.3 §4 «лишнее» — существуют ли задачи и верна ли характеристика

| Позиция 06 | Строка доски | Характеристика 06 | Вердикт |
|---|---|---|---|
| `RULES-D1..D6` | вне волн, `61:286` («**T-2 и RULES-D1..D6 — вне волн** (пост-релиз, `ЗАБЛОКИРОВАН D-33`)»), `60:21` (A8 «вынесены за релиз») | «defer — уже `ЗАБЛОКИРОВАН D-33`; статус подтвердить» | **ПОДТВЕРЖДЕНО**; рекомендация тождественна текущему состоянию, то есть это не «лишнее», а подтверждение |
| `T-2` (M) | `61:100`, `61:286`, `61:634` (`ЗАБЛОКИРОВАН D-33`) | «drop как отдельный id, оставить указателем на `RULES-D2`» | **ПОДТВЕРЖДЕНО** как факт перекрытия (`61:100`: «Переформулирована по 33b §6 … слои живут внутри `sdd-rules`»); но `60:21` называет `T-2` в списке «вне релиза» вместе с `RULES-D*` — снятие id потребует правки и `A8` |
| `T-8` (L) | `61:106` | «ни один критерий приёмки её не называет» | **ОПРОВЕРГНУТО частично**: `T-8` попадает в `A8` через блок «вне релиза — T-2, RULES-D1..D6»? нет — `A8` (`60:21`) `T-8` не называет **и** не выносит; строка `61:286` ставит `T-8` в Волну 3 («RULES: T-5, T-11, T-12, **T-8** … исполняются здесь»). То есть `T-8` **в релизе** и **без критерия** — это хуже, чем описывает 06 («defer за релиз» — верная рекомендация, но обоснование должно быть «в релизе без критерия», а не «зависит от заблокированных») |
| `V-10` (M) | `61:50`, «Закрывает **А6**, D-16» | «`A3` и `A10` прямо исключают python; ни один критерий не называет; defer» | **ОПРОВЕРГНУТО частично**: `A6` (`60:19`) прямо перечисляет «стеки anystack → golang → **python** → swift», а строка `V-10` объявлена его исполнителем; `60:32` выносит за приёмку только «python-**пресет** как критерий **G1**». Значит вынос `V-10` за релиз требует одновременной правки `A6`, чего 06 не предусматривает. **Правка:** добавить в `O-13` последствие «`A6` перестаёт перечислять python» и в §5.4 — правку `A6` |
| `E-13` (M) | `61:179` («единственный LLM-сценарий G1»), `60:16` (`A3` перечисляет `E-13`) | «keep, но переклассифицировать, убрать из `A3`» | **ПОДТВЕРЖДЕНО** дословно (`A3` действительно называет `E-13` исполнителем) |
| `T-14` (S) | `61:112` (`ЗАБЛОКИРОВАН`, Волна 3), `61:318`, `61:635` («решение оператора: main заморожен (5.5), правка main-side README без явного OK не делается») | «drop из доски … либо оставить `ЗАБЛОКИРОВАН` без волны» | **ПОДТВЕРЖДЕНО частично**: задача уже заблокирована и уже помечена как ожидающая решения оператора; фактическая дельта — только снятие из Волны 3. Как «лишнее» позиция слабая |
| `SO-3` (L) | `61:86`, `61:631` (пост-релиз по D-1/D-22), `60:32` | «defer (подтвердить)» | **ПОДТВЕРЖДЕНО**; тождественно текущему состоянию |
| `V-16b` (S) | `61:57`, `61:630` (`O-2`), `60:40` | «defer (подтвердить)» | **ПОДТВЕРЖДЕНО**; тождественно текущему состоянию |
| `FO-7` (S) | новая задача самого 06 | «defer (пост-релиз, по измерениям)» | **ПОДТВЕРЖДЕНО**; совпадает с первоисточником (`ownership-plan:213` «Optional cache только после измерений») |

**Итог §4 по факту:** из девяти позиций «за релиз» **четыре** (`RULES-D*`, `SO-3`, `V-16b`, `FO-7`) уже вне релиза — рекомендация тождественна состоянию; реальных изменений три (`T-8`, `V-10`, `T-2`) плюс одно переклассифицирование (`E-13`) и одно снятие волны (`T-14`). Формулировка 06:249 «к переносу за релиз рекомендуются 9 позиций» завышена: новых решений требуют **3–4**.

### C.4 `05-SUMMARY.md:21`

**C-3. 06:226 (§3.12): «утверждение `05-SUMMARY.md:21` „открытых решений оператора, блокирующих старт, нет“ после этой сверки перестаёт быть верным».**
→ `05:21` дословно: «Открытых решений оператора, блокирующих старт: **нет** — Волна 0 (55 задач) не содержит ни одной заблокированной строки. Открытые вопросы O-1…O-6 блокируют 16 задач более поздних волн».
→ После дельты 06 в Волне 0 появляются `IDN-1` (`ЗАБЛОКИРОВАН O-11`) и `FO-1` (`ЗАБЛОКИРОВАН O-12`), а `REL-17` встаёт в Волну −1. → **ПОДТВЕРЖДЕНО** по выводу.
→ Но **обоснование неверно**: `00-INDEX.md` §4 уже содержит `O-7` — «Гейт адекватности плана (D-37) … **до выдачи первого брифа**», и `01:101` (D-37) — «До этой сверки план не считается готовым ни к выдаче». То есть `05:21` было неточным **до** дельты 06, потому что `O-7` — открытый вопрос оператора, блокирующий старт. → **правка:** в §3.12 указать `O-7`/`D-37` как первую причину, дельту 06 — как вторую.

### C.5 `O-10..O-16` против `O-1..O-9` и `D-1..D-37`

Переномерация обоснована: `O-7`, `O-8`, `O-9` в `00-INDEX.md` §4 действительно заняты (гейт адекватности; 8 инвариантов без матрицы; расхождение D3/D4). **ПОДТВЕРЖДЕНО.**

- `O-10` (diagram authority), `O-11` (identity), `O-12` (`@spec` в релиз), `O-16` (`REL-17` как стартовое условие) — **новые**, дублей нет. `O-10` совпадает по предмету с `implementation-adversarial.md:89` (P2, «сначала устранить status/authority contradiction») — это подтверждение, не дубль.
- `O-13` (python) — **новый**; `D-16` фиксирует лишь порядок стеков, `D-25` — только python-**правила**, `60:32` — только python как критерий G1. Вопрос легитимен.
- `O-14` («подтвердить, что `E-13` — диагностика») — **дубль решения**: `D-28` (`01:92`) уже постановил «детерминированные гейты — бар; судья — диагностика», `L-14` то же, а `60:32` уже выносит «LLM-точки G1/G2 как бар» за приёмку. Спрашивать оператора нечего — надо править `A3`. → **ОПРОВЕРГНУТО** как вопрос оператору; **правка:** снять `O-14`, оставить правку 58 (`A3`).
- `O-15` («снять `T-14` в бэклог») — **дубль уже зафиксированного ожидания**: `61:112` и `61:635` уже держат `T-14` заблокированной именно до явного OK оператора по 5.5/D-36. Отдельный `O` не добавляет решения. → **ОПРОВЕРГНУТО** как новый вопрос; **правка:** снять `O-15` либо объединить с существующей записью.

Итого вопросов оператору: **5** (`O-10`, `O-11`, `O-12`, `O-13`, `O-16`), а не 7.

---

## D. Что 06 пропустил

### D.1 Цели интервью (`01-INTERVIEW-DECISIONS.md` §1–5), которых нет в матрице §1.1

Матрица 06 содержит 24 строки. Прочитав `01:7-59` сам, я нашёл четыре незамапленные позиции; две из них содержательны.

**D-1. Цель 5.4 (`01:52`): «RC доделывает WIP; дальше — в той же ветке PR #25».** В матрице §1.1 её нет.
→ Это **прямая напряжённость с главным предложением 06**: `REL-17` («заморозить baseline: operator-approved SHA, `git status … ` пуст, остановить writers») противоречит операторскому решению «RC доделывает WIP в той же ветке». `O-16` просит подтвердить `REL-17`, но не показывает оператору, что тем самым отменяется/приостанавливается 5.4.
→ **Правка:** внести 5.4 в §1.1 со вердиктом **[Р]** (решение есть, конфликт с `REL-17` не снят) и в `O-16` добавить формулировку «`REL-17` требует приостановить WIP RC (5.4) до снятия среза».

**D-2. Вторая половина цели 3.2 (`01:31`): «v2 — полная перестройка, полный слом обратной совместимости; совместимость только через миграцию».** 06 замапил только третье предложение 3.2 (детект стека).
→ Это **прямо касается §3.2/§3.3**: `B2-21` предлагает «v1/mixed — lenient», а `E-22` делает бар «новый RC не повышает v1-lenient finding до error без явно утверждённого правила». По цели 3.2 совместимость с v1 обеспечивается **миграцией**, а не мягким режимом проверок. Корпус (`evidence-audit:344`) и цель оператора здесь расходятся, и 06 не показывает этого расхождения.
→ **Правка:** в §3.2 добавить абзац «цель 3.2 против `R1:370`»; в `O-12`/новый вопрос вынести: сохраняется ли v1-lenient режим проверок в v2 или совместимость только через мигратор. Это единственное найденное мной расхождение **целей** с корпусом — и оно не отражено ни в §3, ни в §6 п.11.

**D-3. Цели 1.4 (`01:14`, исполнитель — сессия «SDD v2 RC v6») и 4.2 (`01:40`, где живёт план).** Организационные, выполнены, но формально в 24 строки не вошли — счёт «из 24 целевых строк» стоит объявить выборкой (в `01` §1–5 всего 22 нумерованные строки + доп. указание; 06 расщепляет 2.1 на три и 1.5 на две, получая 24). Не дефект, но нумерация непрозрачна.

### D.2 `20-ISSUES-VERDICTS.md` — прочитано самостоятельно

Сводная таблица (`20:455-478`) даёт 17 позиций, счёт 4 ЗАКРЫТО / 5 ЧАСТИЧНО / 8 НЕ ЗАКРЫТО, 13 задач `ISS-*` + 3 замка. Совпадает с §1.2 06.

Проверил два места, где 06 мог пропустить владельца — оба **закрыты**, ложных гапов нет:
- `20:65` и `20:459` — второй подпункт #9.1 «дрейф help `sdd-migrate/help.ts:14`»: владелец есть — `ISS-1`/`B2-05` (`61:23`, `61:69` — путь `help.ts:14` назван в строке задачи). 06 верно поставил **[К]**.
- `20:313-316`, `20:489` — «висячие ссылки в сборке — их две»: `AX_PERMITTED_BASH_COMMANDS` собирается `T-B6-12` (`61:141`), `ax-stale-after-pivot-verification.xml` — `T-B6-17` (`61:151`), общий замок — `T-B6-08` (`61:137`, allowlist по L-10). Гапа нет.
- `20:505` фиксирует ещё один аспект #9.2 — «неудовлетворимость `BDD_COVERAGE` на `DONE` для Swift». Это **вторая сторона** того же чекера, что и MUST `B2-22` (false-green), но противоположного знака (недостижимый DONE). 06 их не связывает. → **правка:** в `B2-22` и `A19` добавить пункт «фикстура на неудовлетворимость: swift-тикет с корректной строкой покрытия не должен становиться незакрываемым» — иначе fail-closed усиливает уже зафиксированный дефект #9.2.

### D.3 Требования первоисточника, не попавшие ни в §2, ни в §5

**D-4. `implementation-adversarial.md:84, 212` + `evidence-audit.md` §«self-check release policy»: версионированный baseline находок по code/file/severity, требование zero-new-error, изменение baseline — отдельное решение оператора.** В 06 этого нет ни как задачи, ни как критерия (`A18` требует лишь «остаточные warnings перечислены по коду и количеству»). Измерение §B-12 показывает, зачем это нужно: сегодня корневой прогон — 198 error / 431 warn, и без зафиксированного baseline ни `A1`, ни `A13`, ни `E-22` не имеют точки отсчёта.
→ **Новая задача `GAP-B-1`** (S, Волна −1, вместе с `REL-17`): версионированный baseline `sdd-check --all .` по `code/file/severity` + предикат zero-new-error в CI; изменение baseline — operator-approved. **Это десятый MUST.**

**D-5. `sdd-v2-rc-hardening-plan.md:334` (RC acceptance п.15): «ни одна v3 feature не попала в RC без отдельного решения».** 06 закрывает 14 из 15 пунктов §7 первоисточника (1-2→`A18`, 3-7→`A16`, 8→правка `A1`+`E-22`, 9→`A17`, 10→`A19`, 11→`A20`, 12→`REL-18`, 13-14→`A18`); п.15 не закрыт — `GAP-P-2` вносит лишь строки-исключения в `60 §3`, что не является проверяемым критерием.
→ **Новый критерий `A23`** (либо пункт в `A18`): «в RC нет research-only темы без явного решения; список research-only ведётся в `60 §3` и сверяется механически (grep по id задач вне релиза)». **Это восьмой новый критерий.**

**D-6. `sdd-v2-rc-hardening-plan.md:312`: «full suite выполняется в поддерживаемом unrestricted environment, чтобы sandbox `EPERM` не смешивался с product failure».** В `REL-18` (06:260) перечислены команды, но не требование среды. 06 §6 п.2 честно фиксирует, что прогоны main не воспроизведены, но не переносит это в приёмку.
→ **Правка:** в `REL-18` и в правку 61 (`A9`) добавить «прогон вне restricted sandbox; `EPERM`-падения не считаются product failure и не смешиваются с ним».

### D.4 R2 §6.2 — предупреждения, под которые попадают задачи плана, но которых нет в §3/§4

06 использует предупреждения 2, 4, 5, 7, 12 и «AI заново генерирует всё» (`R2:316` → обзор `L3361`). Не использованы три, под которые задачи плана попадают:

**D-7. Предупреждение 6 (`R2:391`, «против безусловного RAG и наращивания контекста»; обзор `L983`, `L2531`, `L2127` — рост галлюцинаций с ~5% до ~40% при 10 предыдущих Q–A).** Под него попадают: `T-B6-10` (единый бюджет **8000** — 06 разбирает его только под T1 как «число без метрик») и `ISS-10` (критик получает конвенции — 06 §2.3 №10 ставит guard «тянуть секции через `sdd-extract`, а не файлы», но не как противоречие §3). Ни одной задачи с noise-тестами или mandatory no-retrieval fallback в плане нет.
→ **Правка:** внести в §3 отдельным пунктом; в приёмку `T-B6-10` добавить «увеличение бюджета не является улучшением без измерения», в `ISS-10` — «объём вставки ограничен и измерен».

**D-8. Предупреждение 9 (`R2:394`; обзор `L3675` — «Не считать catalog или static dependency graph полной архитектурой… полнота достигается сопоставлением declared/static/runtime»).** Под него попадают `FO-2`/`FO-3` (resolver из tickets/Target Files/receipts) и `IDN-3` (repo-wide index), а `A17` формулирует «planned/active/blocked/history **вычисляются инструментом**» — то есть declared-граф объявляется владением. Частично смягчено тем, что первоисточник FO включает `git-attributed` как отдельный класс, но 06 этого щита не называет.
→ **Правка:** в `A17` добавить «resolver обязан различать declared и observed evidence-классы и никогда не показывать `declared` как доказанное изменение» (это дословно `ownership-plan:57`).

**D-9. Предупреждение 11 (`R2:396`; обзор `L2652-2656` — «доказана не та спецификация; спецификация слишком слабая; доказательство относится к модели, а не к системе»).** Под него попадают `V-01` (golden фиксирует текущее поведение, а не корректность) и `E-22` (бар «не повышает v1-lenient finding до error» фиксирует совместимость, а не правильность). 06 §3.11 разбирает `V-01` только со стороны M15 (движущееся дерево).
→ **Правка:** в `T-B6-29`/`A20` включить формулировку «golden доказывает неизменность поведения, не его корректность».

### D.5 Что 06 объявило непроверяемым, а оно проверяемо

06 §6 п.1, п.6 и §2.5 оставляют три вопроса открытыми «до запуска кода». Два из них я закрыл:
- п.6 «Действительно ли `B2-22` дешёвая / сколько строк опираются на `.skip`/`.todo`» → измерено: **140** unparseable + **70** untested (§B-2).
- §3.2 «7 × error» → измерено: **47 × `SDD_VERIFICATION_TABLE_INVALID`** на корне RC, плюс 10 ошибок класса `MIGRATION_CRITICAL_CODES` (§B-12).
- п.4 «реальный масштаб дефекта `events`» остаётся открытым (нужны два LLM-прогона) — **НЕ ПРОВЕРЯЕМО** без OpenCode-сервера; подтверждаю честность этого пункта.

---

## E. Итог

### E.1 Оправдан ли вердикт «да, при 9 обязательных» в том виде, как он написан

**По существу — да; в написанной форме — нет.** Ни одно из девяти MUST не оказалось выдуманным: каждое подтверждено либо кодом чекаута `227c03a8`, либо первоисточником, либо и тем и другим, и ни для одного из девяти на доске нет владельца (§C.1 — девять грепов по существу, не по id). Направление («бар — детерминированные гейты, судья — диагностика») совпадает с первоисточником (`evidence-audit:213-215`, `implementation-adversarial:106`, обзор `L3673`).

Что в написанной форме требует правки перед принятием:

1. **Первая из трёх причин «нельзя отдавать в исполнение» держится на усиленной модальности.** «Единственный доказанный механический false-green» — добавка дайджеста R1, которой нет в первоисточниках; там это один из семи равноприоритетных пунктов (§A-1). Сам дефект реален (§B-1, §B-2), но аргумент «единственный» надо снять.
2. **Ссылка на `A7` неверна** (`A7` — про журнал, не про покрытие); правильные адресаты — `A3` и `A1` (§A-14). Это ошибка в самом заголовке вердикта (06:14).
3. **Три числа устарели** и заменяются измеренными: `7 × error` → **47**; `77 malformed rows` → **140**; «HEAD сдвигался четыре раза» → **три** (§B-12, §B-2, §A-16).
4. **Третья причина («eval-харнесс не может исполнить два обещания») наполовину опровергнута**: артефакты прогона (`specs`, `judge.md`, `summary.json`) переживают teardown, `.results/` описан в трёх доках, `--keep` существует и в хелпе (§B-6). Остаётся верным только невозможность доперепроверки **внутри** песочницы и недокументированность `--keep`.
5. **Одно требование первоисточника потеряно** — versioned baseline / zero-new-error / operator-approved изменение baseline (§A-9, §D-4).
6. **Одна цель интервью потеряна и конфликтует с главным предложением**: 5.4 «RC доделывает WIP в той же ветке» против `REL-17` (§D-1); вторая половина 3.2 («совместимость только через миграцию») против `B2-21` lenient-v1 (§D-2).

### E.2 Как должен измениться список MUST

**Добавить (1):**
- **`GAP-B-1`** (S, Волна −1, вместе с `REL-17`) — версионированный baseline находок корневого прогона по `code/file/severity` + предикат zero-new-error; изменение baseline — решение оператора. Источник: `implementation-adversarial:84, 212`; измерение — §B-12. Без него `A1`, `A13`, `E-22` не имеют точки отсчёта.

**Понизить до SHOULD (1):**
- **`GAP-E-2`** — предпосылка «результат прогона недоставаем» опровергнута (§B-6). В MUST остаётся только его дешёвая часть: документировать `--keep`/teardown и внести `--keep` в приёмку тех `E-*`, которые требуют прогона инструментов **по дереву песочницы** (перенести этот пункт в `GAP-E-3`, S, документ).

**Переформулировать, не снимая (3):**
- **`T-B6-28`** — обоснование ослабевает с «доказанный дефект v2» до «остаточный дрейф словаря в keywords директивы»; `sdd-sync` уже описан честно (§A-4). Размер S подтверждён.
- **`B2-22`** — добавить потерянное требование первоисточника «versioned baseline, не blanket severity flip» (§A-2) и негативную фикстуру на **неудовлетворимость** покрытия из #9.2 (§D.2).
- **`GAP-E-1`** — развести два исхода: опечатка в `phase` даёт молча пустой фазовый промпт, опечатка в `mode` — молча **другой валидный** промпт (`prompts.ts:76`); для обоих требовать fail-fast (§B-5).
- **`B2-21`** — размер, вероятно, S: grandfather-ветка уже существует, нужна перестановка порядка ветвей и фикстуры; в объём внести все три проверки, добавляемые `E-07` (§B-11, §C-1).

**Снимать нечего:** ни одно из девяти не оказалось покрытым существующей задачей и ни одно не оказалось ложным.

**Итоговый состав MUST — 9** (было 9): `B2-22`, `FO-3a`, `T-B6-28`, `T-B6-29`, `REL-17`+`REL-18`+**`GAP-B-1`**, `E-22`, `B2-21`, `IDN-1`+`IDN-2`, `GAP-E-1`+`GAP-E-4` (без `GAP-E-2`).

### E.3 Исправленные счётчики

| Что | В 06 | Исправлено | Почему |
|---|---|---|---|
| Новые задачи | 32 | **33** | + `GAP-B-1` (§D-4) |
| Новые критерии приёмки | 7 (`A16..A22`) | **8** (+`A23`) | п.15 acceptance-списка первоисточника не закрыт (§D-5) |
| Вопросы оператору | 7 (`O-10..O-16`) | **5** (`O-10..O-13`, `O-16`) | `O-14` дублирует `D-28`/`60:32`; `O-15` дублирует уже зафиксированное ожидание `61:635` (§C.5) |
| Правки существующих критериев | 7 | **8** | + правка `A6` (перестаёт перечислять python при выносе `V-10`); правка 60 переадресуется с `A7` на `A3` (§A-14, §C.3) |
| Изменённых строк доски | 17 | **19** | + `T-8` (в Волне 3 без критерия, §C.3), + `T-B6-10`/`ISS-10` (предупреждение R2 №6, §D-7) |
| Строк доски после дельты | 158 → 190 | 158 → **191** | 33 новые |
| «Лишнего» за релиз | 9 позиций | **3–4 новых решения** | четыре из девяти уже вне релиза (§C.3) |
| MUST / SHOULD / research-only из «нет» | 9 / 6 / 3 | **9 / 7 / 3** | `GAP-E-2` → SHOULD, `GAP-B-1` → MUST |

### E.4 Условие готовности

06:18 предлагает: «признать план готовым можно после `REL-17` и ответов на `O-10..O-16`». С учётом §C-3 и §D-1 корректная формулировка:

> План готов к выдаче первого брифа, когда исполнены `REL-17` + `GAP-B-1` (immutable SHA и baseline находок), получены ответы на `O-10..O-13` и `O-16`, и **явно решён конфликт `REL-17` с целью 5.4** (RC доделывает WIP в той же ветке). До этого `O-7`/`D-37` сами по себе блокируют старт, независимо от дельты 06.

### E.5 Границы этой проверки

- Прогоны LLM-контура и main-side (198/198, `verify --plan --json`) я не запускал — `node_modules` main пуст, OpenCode-сервера нет. Пункты 06 §6 п.2, п.3, п.4 остаются **НЕ ПРОВЕРЯЕМО**, и это честно зафиксировано в 06.
- Я сверил ~40 утверждений: 18 в §A (дайджесты↔первоисточники), 16 в §B (код), 9 грепов + 9 позиций §4 в §C. Полноту множества гапов эта проверка тоже не доказывает: я нашёл один потерянный первоисточником MUST (`GAP-B-1`) и один потерянный критерий (`A23`) — значит их могло быть и больше.
- Расхождение R1↔R2 о носителе истины (06 §6 п.11) я подтверждаю как реальное и неснимаемое чтением: `R1:486` (уровень A, Markdown как SSOT) против `R2:361` (P0, canonical machine-readable, «прямого causal ROI-доказательства нет» — дословно в дайджесте и в обзоре). Ставка плана на R1 обоснована, но не доказана — формулировка 06 точна.
