ОТЧЁТ 61 §4 — E-01: предикат «0 error(s) = pass» у R1 работает на реальном репозитории

СТАТУС: DONE

Рабочее дерево: `rc-w2`. Ветка `lead/eval-honest-outcome`, поверх `bd2adc45` (E-00).

КОММИТ (локальный, НИЧЕГО не запушено):
- `b34ffd8a7f383dcfb9adcb96ce97f76187efeb6b` fix(E-01): R1 parser recognizes sdd-check's real-repo "0 error(s), N warning(s)" as pass

---

## 1. Файлы

| Путь | Тип | Смысловое изменение | Чем доказано |
|---|---|---|---|
| `ai/flow-eval/quality-gate.ts:30` (`parseSddCheckResult`) | правка | Раньше распознавала pass ТОЛЬКО по буквальному «✅ clean» / «clean — N file»; реальный, обычный вывод `sdd-check` на любом репозитории с предупреждениями — строка `[sdd-check] N error(s), M warning(s) across K file(s)` — не совпадала ни с одним паттерном и падала в ветку «no verdict parsed» (FAIL), хотя сам `sdd-check` трактует `errors === 0` как чистый прогон (exit 0, `help.ts`: «0 clean (warnings allowed)»). Теперь регэксп `(\d+)\s+error\(s\)(?:,\s*(\d+)\s+warning\(s\))?` разбирает эту же строку: `errors > 0` — fail с count (как раньше); `errors === 0` — pass, с count предупреждений в `detail`, если они были. | `quality-gate.test.ts` (both-way на замороженном real-shaped выводе). |
| `ai/flow-eval/__tests__/quality-gate.test.ts` | правка (добавлены кейсы) | Новые both-way тесты: «"0 error(s), N warning(s)" → pass», «"N error(s), 0 warning(s)" → fail», плюс регрессионный guard — `errors > 0` побеждает даже если где-то в объединённом stdout+stderr случайно встретилась подстрока «clean». | Сам файл, `node --test`, зелёный. |

`git show b34ffd8a --stat`: 2 файла — обе строки таблицы присутствуют. Совпадает.

---

## 2. Архитектура было / стало

### Было — реальный вывод `sdd-check` с предупреждениями не распознавался вовсе

```mermaid
flowchart TD
  SC["gennady sdd-check --all .\n(реальный прогон в песочнице)"] --> OUT["stdout+stderr:\n\"[sdd-check] 0 error(s), 3 warning(s) across 12 file(s)\""]
  OUT --> PARSE["parseSddCheckResult()\nquality-gate.ts (до фикса)"]
  PARSE -->|"regex искал только буквальное «clean»"| NOMATCH["ни один паттерн не совпал"]
  NOMATCH --> FAIL["{ pass: false, detail: 'no sdd-check verdict parsed' }"]
  FAIL --> R1["R1 читается как СЛОМАННЫЙ\nна любом репо с предупреждениями"]
  style FAIL fill:#f99,stroke:#900
  style R1 fill:#f99,stroke:#900
```

### Стало — errors===0 из той же строки тоже даёт pass

```mermaid
flowchart TD
  SC["gennady sdd-check --all ."] --> OUT["stdout+stderr:\n\"[sdd-check] 0 error(s), 3 warning(s) across 12 file(s)\""]
  OUT --> PARSE["parseSddCheckResult()\nquality-gate.ts:30"]
  PARSE -->|"буквальное «✅ clean»"| CLEAN["pass: true, detail: 'sdd-check --all clean'"]
  PARSE -->|"errorMatch: N error(s)[, M warning(s)]"| EM{"errors > 0 ?"}
  EM -->|"да"| FAILC["pass: false, detail: 'N sdd-check error(s)'"]
  EM -->|"нет (errors===0)"| PASSW["pass: true,\ndetail: '0 sdd-check error(s), M warning(s) (warnings allowed)'"]
  PARSE -->|"ничего не совпало"| INC["pass: false, detail: 'no sdd-check verdict parsed'"]
  CLEAN & PASSW --> R1OK["R1 = pass — соответствует реальному\nexit-коду sdd-check"]
  style PASSW fill:#9f9,stroke:#090
  style R1OK fill:#9f9,stroke:#090
  style FAILC fill:#f99,stroke:#900
```

Узел: `quality-gate.ts:30-46` (полная функция после правки, см. диф §1).

---

## 3. Доказательства (ПРИЁМКА брифа)

**Пункт «both-way юнит на замороженном выводе»:**
```
$ node --import tsx --test ai/flow-eval/__tests__/quality-gate.test.ts
ok — parses "N error(s), M warning(s)" with errors=0 as pass, carrying warning count
ok — parses "N error(s)" with errors>0 as fail, carrying error count
ok — a stray "clean" substring elsewhere does not override a real error count > 0
ok — literal "✅ clean" still parses as pass (unchanged)
```
Полный прогон файла — часть `test:sdd-flow-eval` (см. сводный §3, 120/120 суммарно, 0 fail). ВЫПОЛНЕНО.

---

## 4. Отклонения, открытые вопросы

Нет. Задача закрыта буквально по приёмке — единственный измеримый критерий (`both-way юнит на замороженном выводе`) выполнен, оба направления кейса (pass/fail) покрыты, плюс добавлен незапрошенный, но уместный регрессионный guard (не расширяет риск, чисто аддитивный тест).

**Команда пуша для Lead** (общая для всей пачки — см. сводный отчёт `R-BATCH-06-eval-honest-outcome.md`).
