# R-PERF — скорость тестового прогона (ветка `lead/test-speed`)

**Старт замеров: 20:40:55.** Дерево: изолированный worktree `rc-perf`, база `3758a634`
(голова `codex/sdd-v2-rc52-followup`), ветка `lead/test-speed`.
Хост: 12 CPU (darwin 23.6), node v22.19.0, tsx 4.21.0.

> ⚠️ **Оговорка про измерительный стенд.** Машина оператора разделяемая: `load average` во время
> сессии плавал от ~20 до **120** (OpenCode, Codex, Claude.app, другие агенты). Первые замеры
> (база, итерация 2) сняты при низкой нагрузке; замеры после 21:00 — при `load ≈ 60–120` и
> инфлированы в 1.3–1.6 раза. Все сравнения ниже, где это важно, сняты **back-to-back**
> (контроль и вариант подряд, при одинаковой нагрузке). Часть «`npm test` идёт минуты» из жалобы
> оператора воспроизводится именно под такой нагрузкой: тот же самый HEAD дал 36 с при спокойной
> машине и 57–80 с при `load > 100`, вплоть до `cancelled` тестов по 30-секундному таймауту.

---

## 1. Замер ДО

| Прогон | wall | user | sys |
|---|---|---|---|
| `npm test` #1 | **51.90 c** | 219.5 c | 67.7 c |
| `npm test` #2 | 58.60 c | 213.1 c | 66.9 c |

**База = 51.90 c** (меньший из двух, как договорено).
Корпус: 290 файлов, 3566 тестов, 596 сюит, 0 fail, 10 skip.
`(user+sys)/wall ≈ 5.5` при `--test-concurrency=6` на 12 ядрах → **пул недозагружен вдвое**.

## 2. Профиль по файлам

`scripts/test-topology.ts` времена по файлам **не печатает**, и TAP-репортёр node не тегирует
результаты именем файла (файл всплывает в выводе только когда падает/отменяется). Профиль снят
внешним прогоном без правки репозитория: каждый файл отдельным
`node --test --import tsx --experimental-test-module-mocks <file>`, раскладка `xargs -P 12`.

Сумма по файлам **484.9 c** (под контеншеном `-P 12`), среднее 1.67 c, медиана 0.68 c, минимум 0.31 c.
**244 из 290 файлов < 1.5 c** — в них доминирует старт `node + tsx + node:test` (пол ≈ 0.31 c).
**Топ-15 = 218.6 c**, то есть ~45 % всей работы в 5 % файлов.

Работа по слоям: `local` 51 файл / **246 c (51 %)**, `unit` 215 файлов / 180 c,
`contract` 16 файлов / 54 c, `external` 8 файлов / 4.5 c.

### Топ-15 самых долгих файлов и причины

| c | слой | файл | причина |
|---|---|---|---|
| 23.12 | local | `cli/__tests__/tool-behavior/bootstrap-path.test.ts` | 12 независимых `it`, **18 последовательных** `runCli` = `spawnSync` реального `cli/gennady.ts` (`sdd-state`/`sdd-task`/`sdd-verify`) против свежего `mkdtemp` + `git init` фикстуры на каждый тест; `concurrency` не задан |
| 20.12 | local | `cli/cmd/inbox-review-plan/inbox-review-plan.test.ts` | 35 `it`, 16 прямых `spawnSync` реального CLI, всё последовательно; **самый хрупкий файл** — под нагрузкой стабильно ловит `test timed out after 30000ms` |
| 18.92 | local | `cli/__tests__/tool-behavior/sdd-verify-repair-adapters.test.ts` | реальные repair-адаптеры, `{ concurrency: 1 }` выставлен явно; внутри — реальные `npm`/`git` |
| 18.74 | local | `cli/__tests__/tool-behavior/clean-repo-composition.test.ts` | 1 `it`, но 5 `spawnSync` тяжёлых (`npm pack`/`npm install` в песочнице) |
| 18.63 | local | `cli/__tests__/tool-behavior/sdd-verify.test.ts` | 27 `it`, 38 `runCliAsync`, уже `{ concurrency: 4 }` — «правильный» образец; остаточная цена — реальный ladder гейтов |
| 17.28 | local | `cli/cmd/lint/__tests__/lint.cmd.test.ts` | 31 `it`, реальный tree-sitter + `spawnSync` |
| 14.69 | local | `cli/__tests__/tool-behavior/testcov.test.ts` | 22 независимых `it`, **23 последовательных** `runCli`; в одном тесте фикстура на 250 файлов |
| 14.03 | local | `cli/cmd/inbox/config.test.ts` | 10 `describe`, **31 последовательный** `runCli` (собственный хелпер с tsx-loader) |
| 13.62 | local | `cli/cmd/testcov/__tests__/testcov.cmd.test.ts` | 24 `it`, 5 `spawnSync`, последовательно |
| 12.28 | contract | `shared/common/__tests__/test-topology.test.ts` | контракт раннера: **сам спавнит `test-topology.ts` во всех трёх режимах** (`probeSpawns`) + читает `package.json` |
| 12.09 | contract | `ai/kit/__tests__/delta-assembly.test.ts` | полная пересборка директив в памяти на каждый кейс |
| 11.45 | local | `cli/__tests__/tool-behavior/sdd-task.test.ts` | 7 `runCli` последовательно + фикстуры |
| 9.65 | contract | `ai/kit/__tests__/check-directives-fresh.test.ts` | полный rebuild `ai/directives/**` и сравнение |
| 7.15 | contract | `cli/__tests__/directive-tool-contract/directive-tool-contract.test.ts` | собственный `runCli` c tsx-loader |
| 6.79 | local | `cli/__tests__/tool-behavior/sdd-v2-rc-composition.test.ts` | 4 `runCli` + композиция RC |

**Общая причина медленности:** корпус не CPU-bound по «полезной» работе, а **spawn-bound**:
слой `local` (51 файл) поднимает сотни реальных дочерних процессов `node + tsx cli/gennady.ts`,
которые внутри себя запускают ещё `npm`/`git`. Сборки `dist` внутри тестов нет, `sdd-check --all`
в тестах нет, tsx-кэш трансформаций есть (`$TMPDIR/tsx-503`) и работает — голый
`node --import tsx cli/gennady.ts --help` стоит всего 0.14 c, то есть **не компиляция, а сама
работа команд + количество процессов**.

Вторая причина, и она оказалась главной по вкладу: **планирование**. `node --test` раздаёт файлы
воркерам строго в порядке аргументов, а `targetsFor('deterministic')` отдавал один алфавитно
отсортированный union. Тяжёлый слой `local` был размазан по списку и частично стоял в очереди за
215 субсекундными `unit`-файлами, а пул из 6 воркеров вырождался в длинный однофайловый хвост.

---

## 3. Итерации

| # | гипотеза | правка | до | после | % | вердикт | SHA |
|---|---|---|---|---|---|---|---|
| 1 | пул из 6 воркеров на 12 CPU недозагружен → расширить до `availableParallelism()` | `OUTER_TEST_CONCURRENCY = max(6, availableParallelism())` | 51.90 | **57.85**, 2 таймаута | +11 % | ❌ откат | — |
| 2a | тяжёлый слой стоит в очереди за лёгким → раздавать longest-layer-first | порядок `local → contract → external → unit` | 51.90 | 43.67 | −15.9 % | ⏳ мало, уточняю гипотезу | — |
| 2b | тот же порядок + пул расширить до колена | + `OUTER_TEST_CONCURRENCY = min(10, max(6, availableParallelism()))` | 51.90 | **36.05** (подтверждение 42.37) | **−30.5 %** | ✅ **принято** | `8c6a5a8d` |
| 3 | после longest-first пул можно раскрыть на все 12 | `min(10,…)` → `max(6, availableParallelism())` = 12 | 36.05 | 60.23, 44 теста `cancelled` | +67 % | ❌ откат | — |
| 3b | (проба) tsx-loader — лишний оверхед на 290 процессов → нативный strip-types node 22 | убрать `--import tsx` из `runNodeTests` | 36.05 | 47.02 wall, **user 209 → 182 c (−13 % CPU)**, 4 fail | wall хуже | ❌ откат, вынесено в «требует решения» | — |
| 4 | тяжёлые сюиты последовательны внутри файла → перевести на существующий `runCliAsync` + `{ concurrency: 4 }` | `bootstrap-path.test.ts`, `testcov.test.ts`: `runCli` → `await runCliAsync`, `it` → `async` | 36.05 | 56.30 (изолированно файлы: **23.1 → 6.2 c** и **14.7 → 3.6 c**, 0 fail) | +56 % | ❌ откат | — |
| 4b | то же, но сузить внешний пул, чтобы 10×4 не переподписывало машину | + `OUTER_TEST_CONCURRENCY = min(7,…)` | 36.05 | 80.10, 2 `cancelled` | +122 % | ❌ откат | — |
| 4c | контрольная пара back-to-back при одинаковой нагрузке | контроль HEAD vs итерация 4 | 57.17 (HEAD) | 65.42 (ит. 4) | +14 % | ❌ откат подтверждён | — |

Финальный контрольный прогон принятого состояния (`load 61→85`): **47.44 c**, 3566 тестов,
0 fail, 0 cancelled, 10 skip.

### Что вошло в принятый коммит `8c6a5a8d`

`perf(test): dispatch heaviest test layer first and widen the outer pool`

* `scripts/test-topology.ts` — `DETERMINISTIC_LAYER_ORDER = ['local','contract','external','unit']`,
  `targetsFor('deterministic')` раздаёт файлы в этом порядке вместо алфавитного union;
  `OUTER_TEST_CONCURRENCY = min(10, max(6, availableParallelism()))`.
* `shared/common/__tests__/test-topology.test.ts` — контракт корпуса сравнивается
  **порядко-независимо** (`[...deterministic].sort()`), и **дополнительно** проверяется
  «ровно один раз» (`new Set(x).size === x.length`), чего в этой ветке ассерта раньше не было;
  ожидаемая concurrency читается из той же формулы; `--help` матчится по `\d+`.

Ослабления нет: множество файлов побайтово то же, число тестов/сюит/skip то же
(3566 / 596 / 10), ни один тест не удалён, не `.skip`, ни один ассерт не снят.

---

## 4. Отвергнутые гипотезы и почему

| гипотеза | почему отвергнута |
|---|---|
| Расширить внешний пул до числа ядер (12) | Внутри слоя `local` каждый файл сам поднимает 1–4 дочерних CLI, а `sdd-verify` — ещё четыре фикстурных. 12 внешних воркеров дают 30–48 одновременных процессов: wall растёт, и тесты начинают отменяться по 30-секундному таймауту. **10 — измеренное колено.** |
| Отказаться от tsx в пользу нативного strip-types node 22 | Технически работает (проверено на unit- и local-файлах, тесты зелёные) и режет **CPU на 13 %**, но wall не улучшил, зато ломает контракты, где `--import tsx` зафиксирован явно (`test-topology.test.ts`, `shared/sdd/__tests__/scripts.test.ts`), и требует синхронной правки 14 мест, включая `run-cli.ts`. Это решение по инфраструктуре, а не тихая перф-правка. |
| Внутрифайловый параллелизм в тяжёлых сюитах (`runCliAsync` + `{ concurrency: 4 }`) | **Изолированно даёт кратный выигрыш** (`bootstrap-path` 23.1 → 6.2 c, `testcov` 14.7 → 3.6 c, все тесты зелёные), но в общем прогоне умножается на внешний пул и переподписывает машину: 10×4 → +56 % wall, 7×4 → +122 %. Нужен общий бюджет процессов на весь прогон, которого сейчас нет. |
| Кэш компиляции tsx | Уже есть и работает (`$TMPDIR/tsx-503`); `gennady --help` = 0.14 c против 0.02 c у голого node — компиляция не узкое место. |
| Пересборка `dist` / `sdd-check --all` внутри тестов | В корпусе не обнаружено. |
| Замена реального spawn на фейк | Не применялось: в топ-15 нет ни одного файла, где spawn не был бы предметом самого теста (`tool-behavior/*` и `*.cmd.test.ts` — это чёрный ящик реального CLI). Подмена была бы ослаблением. |
| Перенос тяжёлых e2e в другой слой | Не применялось: `npm test` (`deterministic`) по определению = весь корпус; вынос из него ослабил бы коммит-гейт. |

---

## 5. Pre-commit по гейтам

Хук `scripts/git-hooks/pre-commit` = `npm run check` (`sdd-verify --profile full`) + 5 директивных гейтов.
Замеры сняты с живого хука на принятом коммите (умеренная нагрузка):

| гейт | время | как исполняется |
|---|---|---|
| `type-check` (`tsc --noEmit`) | 4.2 c | последовательно (foundation) |
| `test:coverage` | **42.6 c** | последовательно (foundation) — тот же корпус под `c8`, двумя партициями |
| `lint` | 2.9 c | ⟩ |
| `format` (`prettier --check --cache`) | **12.3 c** | ⟩ хвост качества, **уже параллельный** (`Promise.all` в `sdd-verify.cmd.ts:606`) |
| `yagni` | 2.7 c | ⟩ |
| `check:directives-fresh` | 0.55 c | последовательно |
| `audit:axioms` / `audit:contracts` / `audit:halts` / `check:directive-budgets` | 0.12 / 0.18 / 0.18 / 0.17 c | последовательно |

**Критический путь pre-commit ≈ 4.2 + 42.6 + max(2.9, 12.3, 2.7) + 1.2 ≈ 60 c**, из них
**71 % — `test:coverage`**. Хвост качества уже параллелится, директивные гейты стоят ~1 c —
оптимизировать там нечего.

Хорошая новость: `test:coverage` гоняет тот же корпус тем же раннером, поэтому принятая правка
`8c6a5a8d` удешевляет и его — цифра 42.6 c уже **после** правки.

---

## 6. Где остановились и почему

Остановка по двум причинам сразу:

1. **Исчерпание безопасных гипотез.** Всё, что осталось в списке, либо ломает зафиксированные
   контрактами решения (отказ от tsx), либо требует общего бюджета процессов на прогон
   (внутрифайловый параллелизм), либо ослабляет гейт (вынос слоя, подмена spawn).
2. **Стенд перестал быть измеримым.** `load average` вырос со ~20 до 120 сторонней нагрузкой;
   один и тот же HEAD давал 36 / 42 / 47 / 57 c. Дальнейшие вердикты «−20 %» на таком шуме были бы
   вымышленными. Лимит в 60 минут не достигнут (первый замер 20:40, остановка ~21:05).

**Цель ≤ 30 c не достигнута.** Достигнуто 36.05 c на спокойной машине (−30.5 % от 51.90 c).

## 7. Сколько занимает сейчас

* `npm test` — **36.05 c** best-case (спокойная машина), 42–47 c типично, 57–80 c при `load > 100`.
  Было 51.90 / 58.60 / до 80+ соответственно.
* `npm run check` + директивные гейты (полный pre-commit) — **≈ 60 c**, из них `test:coverage` 42.6 c.

## 8. Что безопасно для PR, а что требует решения

**Безопасно, готово в PR (`8c6a5a8d`):**
* longest-layer-first раздача файлов и `OUTER_TEST_CONCURRENCY = min(10, availableParallelism())`.
  Только планирование; множество файлов, число тестов, сюит и skip побайтово те же; контракт
  топологии стал строже (добавлен «ровно один раз»), а не слабее.

**Требует решения оператора (мерено, но не коммичено):**
1. **Внутрифайловый параллелизм тяжёлых сюит.** Патч готов и зелёный
   (`bootstrap-path` 23.1 → 6.2 c, `testcov` 14.7 → 3.6 c). Нужен общий бюджет процессов:
   либо внешний пул опускается до 3–4 при `{ concurrency: 4 }` внутри, либо вводится
   семафор на все дочерние CLI. Без этого выигрыш съедается переподпиской. Это отдельная задача
   по раннеру, не однострочник.
2. **Отказ от tsx-loader в пользу нативного strip-types node 22.** −13 % CPU корпуса. Ломает
   контракты `test-topology.test.ts` и `shared/sdd/__tests__/scripts.test.ts` (там `--import tsx`
   зафиксирован как решение) и требует правки 14 мест, включая `cli/__tests__/tool-behavior/run-cli.ts`.
   Инфраструктурное решение уровня спеки.
3. **`cli/cmd/inbox-review-plan/inbox-review-plan.test.ts` — хронически хрупкий.** 20 c, 16
   последовательных `spawnSync` реального CLI; под нагрузкой воспроизводимо ловит
   `test timed out after 30000ms` и роняет весь `npm test`. Это существующая проблема ветки,
   не следствие правок: она проявилась на HEAD без изменений. Кандидат либо на внутренний
   параллелизм, либо на повышенный `{ timeout }` в самом файле.
4. **`test:coverage` в pre-commit (42.6 c из 60).** Перенос покрытия на pre-push с оставлением
   `npm test` в pre-commit сократил бы коммит-гейт примерно вдвое, но это **ослабление
   коммит-гейта** — решение оператора, не перф-правка.
5. **Стенд.** Пока на машине оператора живут OpenCode/Codex/Claude с `load > 100`, любой
   локальный гейт будет «идти минутами» независимо от кода. Часть жалобы адресуется сюда.
