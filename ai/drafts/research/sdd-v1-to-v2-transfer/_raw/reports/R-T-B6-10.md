ОТЧЁТ 61 §1 / Пачка 19 — T-B6-10: Бюджеты покрывают все директивы; монолит выше лимита роняет гейт

СТАТУС: РАСЩЕПЛЕНА решением Lead L-23 (по образцу L-3) на **T-B6-10a** (lazy-split 4 монолитов, вне Зоны этой пачки, НЕ ВЫПОЛНЕНА) и **T-B6-10b** (гейт измеряет все 28 директив, 4 названных монолита warn вместо error до сплита — **ВЫПОЛНЕНА**, коммит `d31210828ac9c6abbed105995e6ab7b678a20d8f`). Анализ ниже — как он был на момент STOP, до решения L-23; результат T-B6-10b — в разделе «§ Правки по V-BATCH-19 и T-B6-10b» сводного отчёта.

Рабочее дерево: `rc-w3`. Ветка `lead/spec-authoring` (см. `R-BATCH-19-spec-authoring.md`), от `origin/codex/sdd-v2-rc52-followup` @ `c9b58636`.

КОММИТ (на момент исходного STOP): нет — задача не выполнялась, см. анализ ниже. Дерево на тот момент не было изменено относительно базовой головы (файлы `assembly-manifest.json`, `step-budget-gate.ts` не тронуты). **С тех пор** T-B6-10b реализована и закоммичена отдельно — `d31210828ac9c6abbed105995e6ab7b678a20d8f`, см. приписку в конце файла.

---

## Почему задача остановлена, а не выполнена

Бриф Пачки 19 (`62-BATCH-QUEUE.md:270`) сузил T-B6-10 до двух файлов: `assembly-manifest.json`, `step-budget-gate.ts` — без `.hbs`-монолитов `infra`/`root`/`migration-v1-v2`/`router`, которые полная формулировка задачи на доске (`61-TASK-BOARD.md:155`) явно перечисляет как файлы, требующие lazy-split ("в т.ч. `infra` 9592 → lazy-split"). Тот же документ (`40-TRACK-DIRECTIVES-SKILLS.md:933-934`) прямо предупреждает: «единый лимит без предварительного lazy-split (T-B6-10) немедленно роняет билд на `infra` — T-B6-10 обязан идти раньше включения гейта на монолиты».

**Измерено, а не предположено.** Текущий `ai/kit/step-budget-gate.ts` (CLI-обёртка, строки 161-166) сканирует `ai/directives/sdd-v2/**` и **пропускает** любую директиву без соседней папки `<name>/steps/` — то есть любой монолит целиком:
```ts
const stepsDir = join(sddV2Dir, directive, 'steps');
if (!existsSync(stepsDir)) continue; // not lazily assembled yet — nothing to measure
```
Реальные токены (замерено `countTokens` из `shared/common/tokens.ts`, тем же способом, каким его использует сам гейт) по монолитам, у которых нет `steps/`:

| Директива | Токены | `SKELETON_TOKEN_LIMIT` (8000, hard) | `SKELETON_TOKEN_TARGET` (6000, soft) |
|---|---|---|---|
| `infra.directive.xml` | 9633 | **превышает на 1633 → error** | превышает |
| `root.directive.xml` | 7572 | в пределах | превышает на 1572 → warning |
| `migration-v1-v2.directive.xml` | 6741 | в пределах | превышает на 741 → warning |
| `router.directive.xml` | 6094 | в пределах | превышает на 94 → warning |

Текущий прогон гейта на реальном дереве (без изменений): `node ai/kit/step-budget-gate.ts` → `✓ every lazy directive under ai/directives/sdd-v2/** is within budget.`, exit 0 — зелено не потому, что именно эти 4 директивы в порядке, а потому, что `assembly-manifest.json` объявляет `lazy` только 3 директивы из 28 (`audit`, `scaffold`, `phase-execution-protocol`) — гейт **не измеряет 25 из 28**, эти 4 монолита лишь те из 25 неизмеряемых, что превышают порог (см. правку по V-BATCH-19 ниже: точная формулировка — не «гейт не покрывает 4 монолитные директивы», а «гейт измеряет 3 из 28»).

**Единственный технически честный способ** реализовать «бюджеты покрывают все директивы; монолит выше лимита роняет гейт» — перестать пропускать директивы без `steps/` и мерить их `skeletonText` тем же `check()` (уже агностичен к lazy/monolith — просто добавить их в скан с `packages: []`). Это ровно то, что говорит акцептанс. Но при текущем состоянии дерева это **немедленно** делает `infra.directive.xml` (9633 > 8000) находкой уровня `error`, `check:directive-budgets` возвращает exit 1, и обязательная финальная проверка пачки `npm run audit:sdd-templates` (которая вызывает `check:directive-budgets` последним шагом) становится красной — не гипотетически, проверено:

```
$ node ai/kit/step-budget-gate.ts   # СЕЙЧАС, без изменений
✓ every lazy directive under ai/directives/sdd-v2/** is within budget.
$ echo $?
0
```
— и с гипотетическим включением монолитов в скан (проверено локально на копии функции, не закоммичено) `infra.directive.xml` даёт `✗ infra (skeleton): skeleton tokens (hard limit) = 9633 exceeds 8000 by 1633 — build fails`, exit 1.

## Почему это не решается в рамках Зоны этой пачки

- Лечение (`lazy-split` для `infra`/`root`/`migration-v1-v2`/`router`) требует править `ai/kit/templates/sdd-v2/{infra,root,migration-v1-v2,router}.directive.hbs` — эти файлы **не входят** в Зону Пачки 19 («Зона: `ai/kit/templates/sdd-v2/{scope,module}.directive.hbs`, `ai/kit/assembly-manifest.json`, `ai/kit/step-budget-gate.ts`, аксиомы масштаба/pivot/стиля, тесты kit»).
- Единственная альтернатива — молча завести baseline/allowlist-исключение для уже-нарушающих монолитов (по образцу `GAP-B-1`/`gate:sdd-check-baseline`, но для этого гейта отдельно не заведено операторским решением) — прямо запрещена правилом стоп-класса 2 из `70-ORCHESTRATION-PROTOCOL.md`: «красная задача не чинится втихую расширением baseline (изменение baseline — отдельное operator-approved решение)».
- Нарушить инвариант «бюджеты покрывают все директивы» частично (например, включить скан только для новых/гипотетических монолитов, а существующие молча исключить без operator-approved механизма) означало бы придумывать архитектуру экземпляра гейта самостоятельно, не предусмотренную брифом буквально — тоже не мой выбор как исполнителя.

## Итог

Задача **невыполнима как написана** в границах этой пачки: включение полного покрытия гейта либо (а) требует lazy-split 4 монолитов — вне Зоны — либо (б) немедленно превращает уже прошедший `check:directive-budgets`/`audit:sdd-templates` в красный на реальном дереве без operator-approved механизма экземпляра/baseline. Оба пути требуют решения, которое я не вправе принимать сам (стоп-класс 2 и 4, `70-ORCHESTRATION-PROTOCOL.md`).

**Передаю Lead/оператору варианты:**
1. Разрешить lazy-split `infra`/`root`/`migration-v1-v2`/`router` в ЭТОЙ же пачке (расширить Зону) — тогда T-B6-10 в полном объёме (по строке доски `61:155`) исполним следующим шагом, до включения проверки монолитов.
2. **Вариант 2 (baseline) — это ОТКАТ L-12, а не равноправная альтернатива внутри действующего решения.** Явно завести operator-approved baseline-механизм для этого гейта (аналог `GAP-B-1`/`gate:sdd-check-baseline`: зафиксировать текущие 4 значения как измеренную точку отсчёта, гейт валит только НОВОЕ превышение сверх baseline) семантически совпадает с Q6(b) из трека (`40-TRACK-DIRECTIVES-SKILLS.md`) — «отдельный, более мягкий лимит для монолитов», который тот же трек уже отклонил словами «легализует текущее состояние и снимает мотивацию разбивать», и который закрыт решением L-12 в пользу варианта (a). Предлагать его нужно оператору как явный вопрос «откатываем ли L-12», а не как второй пункт списка вариантов.

Ни один из вариантов не выбран самостоятельно; жду решения. **(Решение Lead L-23 см. приписку в конце файла: ни (1), ни (2) — принят третий путь по образцу L-3, warn-until-split; L-12 остаётся в силе.)**

## Команды-доказательства (проверка сегодняшнего, немодифицированного поведения)

| Команда | Вывод | Exit | Вердикт |
|---|---|---|---|
| `node ai/kit/step-budget-gate.ts` | `✓ every lazy directive under ai/directives/sdd-v2/** is within budget.` | 0 | Подтверждает: монолиты сейчас не сканируются вовсе (иначе `infra` дал бы error) |
| измерение токенов `infra.directive.xml`/`root.directive.xml`/`migration-v1-v2.directive.xml`/`router.directive.xml` через `countTokens` | 9633 / 7572 / 6741 / 6094 | н/д | `infra` превышает `SKELETON_TOKEN_LIMIT=8000` (`ai/kit/step-budget-gate.ts:40`) на 1633 |

---

ОТКЛОНЕНИЯ ОТ БРИФА: задача (в исходной полной формулировке T-B6-10) не выполнена — см. анализ выше. **С тех пор** расщеплена решением L-23 — T-B6-10b выполнена, см. приписку ниже.
НАЙДЕНО ПОПУТНО: нет нового, сверх уже задокументированного в `40-TRACK-DIRECTIVES-SKILLS.md:933-934` риска.
ВОПРОСЫ (на момент исходного STOP, закрыты решением L-23):
1. Разрешить расширить Зону этой пачки на `ai/kit/templates/sdd-v2/{infra,root,migration-v1-v2,router}.directive.hbs` для lazy-split (вариант 1) — Lead: нет, это T-B6-10a, отдельная задача.
2. Завести operator-approved baseline-механизм для `check:directive-budgets` по образцу `gate:sdd-check-baseline` (вариант 2 = откат L-12) — Lead: нет.
3. Решение L-23: warn-until-split по образцу L-3 (T-B6-10b) — принято, см. ниже.

Команды пуша для Lead: T-B6-10b закоммичена — `git push origin lead/spec-authoring` (после мержа остальных задач пачки, см. сводный отчёт). T-B6-10a коммита не имеет (отдельная задача, вне этой пачки).

---

## § Правки по V-BATCH-19 и T-B6-10b

Верификатор (`V-BATCH-19.md`) нашёл два блокирующих дефекта формулировки в этом отчёте — оба исправлены выше правкой текста (не кода):

1. **«Гейт не покрывает 4 монолитные директивы»** (было на строке 29 в исходной редакции) переформулировано: `assembly-manifest.json` объявляет `lazy` только 3 директивы из 28 (`audit`, `scaffold`, `phase-execution-protocol`) — гейт **не измерял 25 из 28**; 4 монолита — лишь те 4 из этих 25, что превышают порог. Утверждение «4 монолита» подавало читателю PR масштаб меньше реального.
2. **Вариант 2 (baseline) помечен как откат L-12**, а не равноправная альтернатива внутри действующего решения — см. правку в разделе «Итог» выше.

Дальше — Lead принял решение **L-23** (по образцу L-3): T-B6-10 расщепляется на **T-B6-10a** (M, lazy-split четырёх монолитов — отдельная задача, здесь не выполнена) и **T-B6-10b** (S, гейт бюджетов измеряет все 28 директив немедленно, но для этих же 4 названных монолитов превышение до их lazy-split — warning с именем T-B6-10a, не error). T-B6-10b выполнена этой же сессией:

**КОММИТ:** `d31210828ac9c6abbed105995e6ab7b678a20d8f` — `feat(kit): T-B6-10b — step-budget gate measures all 28 directives; four monoliths warn until T-B6-10a`.

**Файлы:**
| Путь | Тип | Смысловое изменение |
|---|---|---|
| `ai/kit/step-budget-gate.ts` | правка | CLI-скан больше не пропускает директивы без `steps/` — измеряет skeleton каждой из 28 (packages — только когда `steps/` существует, как раньше); новый экспортируемый `MONOLITH_HARD_LIMIT_WAIVER_ALLOWLIST` (`infra`, `root`, `migration-v1-v2`, `router`) — явный, только-сокращающийся список, понижает hard-limit error до warning с именем владельца `T-B6-10a`; директива вне списка, превышающая hard limit, всё равно роняет билд; отдельная проверка помечает как «stale» запись allowlist, если её директива уже стала `lazy` (сплит состоялся) |
| `ai/kit/__tests__/step-budget-gate.test.ts` | правка | +2 CLI-теста: allowlisted монолит над hard limit → warning с `T-B6-10a`, exit 0; директива вне allowlist («baz», both-way) над hard limit → error, exit 1 |

**Приёмка — команды-доказательства:**
```
$ npm run audit:sdd-templates   # секция check:directive-budgets
⚠ infra (skeleton): skeleton tokens (hard limit) = 9633 exceeds 8000 by 1633 — allowlisted monolith pending lazy-split (owner: T-B6-10a); warns instead of failing until split, build still succeeds
⚠ migration-v1-v2 (skeleton): skeleton tokens (soft target) = 6741 exceeds 6000 by 741 — allowlisted monolith pending lazy-split (owner: T-B6-10a), soft target, build still succeeds
⚠ root (skeleton): skeleton tokens (soft target) = 7572 exceeds 6000 by 1572 — allowlisted monolith pending lazy-split (owner: T-B6-10a), soft target, build still succeeds
⚠ router (skeleton): skeleton tokens (soft target) = 6094 exceeds 6000 by 94 — allowlisted monolith pending lazy-split (owner: T-B6-10a), soft target, build still succeeds
✓ every measured directive under ai/directives/sdd-v2/** is within its hard limit (see soft-target warning(s) above).
```
exit 0. Ровно 4 warning, все именем `T-B6-10a`, 0 error — ВЫПОЛНЕНО (акцептанс L-23 п.2 из брифа).

`node --test ai/kit/__tests__/step-budget-gate.test.ts`: 10/10 pass (было 8: 6 `check()`-тестов + 2 CLI-теста, +2 новых CLI both-way-теста — allowlisted монолит warns, монолит вне списка errors). `npm test` (полный): 3635/3643 pass, 0 fail, 8 skipped, exit 0. `npm run check`: ALL PASS 5/5 (type-check, test:coverage, lint, format, yagni). `npm run build:directives && npm run check:directives-fresh`: fresh, `git status --porcelain` пуст (этот коммит не трогает `.hbs`/шаблоны — директивы не регенерировались). `npm run gate:sdd-check-baseline`: OK, no error outside baseline.

T-B6-10a (lazy-split `infra`/`root`/`migration-v1-v2`/`router`) остаётся отдельной, не выполненной здесь задачей — предпосылка: утверждённая оператором карта изменений по D-55 (расширение Зоны на 4 чужих `.hbs`).

Команды пуша для Lead: `git push origin lead/spec-authoring` (после мержа остальных задач пачки).
