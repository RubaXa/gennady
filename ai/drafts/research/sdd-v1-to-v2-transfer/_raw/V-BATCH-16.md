ВЕРИФИКАЦИЯ — Пачка 16 «Легаси-тикеты проходят проверки без ложного закрытия» (B2-22, B2-10; B2-13 отложена)

Проверяющий: `plan-verifier` (свежие глаза, только чтение; мутации — в отдельном клоне, `git stash` не использовался).
Дата: 2026-09-10.
Проверяемое: ветка `lead/legacy-no-false-close`, 2 коммита (`f7b750c4` B2-22, `96276f54` B2-10) поверх `e80d09fc` (= `lead/migrator-red-first`, пачка 22 / PR #43), дерево `<scratch>/rc-perf`.
Отчёты исполнителя: `_raw/reports/R-B2-22.md`, `_raw/reports/R-B2-10.md`, `_raw/reports/R-BATCH-16-legacy-no-false-close.md`.
Требования: `62-BATCH-QUEUE.md` «### Пачка 16»; `61-TASK-BOARD.md` §1 строки `B2-22` (`:96`), `B2-10` (`:84`), `B2-13` (`:87`); `31-TRACK-CHECK-LOG.md` §4.1 (`:632`, `:636`); `06-ADEQUACY-GAP.md` §5.2 п.36 (`:307`); критерий `A19` (`60-ACCEPTANCE.md:32`); решения `D-38`, `D-39`, `L-3`, `GAP-B-1`, `D-49`/`L-16`; `70-ORCHESTRATION-PROTOCOL.md` § «Правила брифа по итогам аудита волн» (правила 2, 3, 4, 6, 7).
Клон для мутаций: `<scratch>/v16-mut` (`git clone --shared`, `node_modules` симлинком). Дерево `rc-perf` после всех прогонов: `git status --porcelain` → пусто, HEAD = `96276f54`.

# ВЕРДИКТ: ВЕРНУТЬ

По существу обе задачи сделаны и механически доказаны: `.skip`/`.todo` действительно перестали закрывать сценарий, `### Phase P<N>` действительно распознаётся, патч-инвариант якорей держится на всём корпусе, все числа отчётов воспроизведены до единицы, все гейты зелёные, конфликтов с девятью открытыми PR и релизной веткой нет. Возврат вызван тремя находками, две из которых блокирующие:

- **B-1 (блокирующее).** Код теперь прямо противоречит **активной** записи спеки `D-CK014`, которая словами запрещает то, что сделала B2-22 («severity фиксирована умышленно, без v1/v2-градации»). Спека не тронута ни строкой — нарушены spec-first (`B2-17`), правило брифа 4 и семантика самого токена `accepted` по `L-16` («следствие — спека дописывается под фактическое поведение»).
- **B-2 (блокирующее).** «Неактивный тест не считается observed» (`A19`) закрыт только для листовых модификаторов. `describe.skip(...)` — тот же класс неактивности, назван в аксиоме репозитория поимённо — по-прежнему даёт «observed»-имена: в дереве есть живой файл, откуда извлекаются 4 имени кейса при 0 запущенных тестов. Поддержанный синтаксис-subset (требование строки доски `B2-22`) этой дыры не называет.
- **B-3 (решение оператора).** Приёмка `B2-22` на доске и `A19` требуют **swift-фикстуру на неудовлетворимость**; поставленный тест `'unsatisfiability guard…'` неудовлетворимость не проверяет вовсе, а сам swift-сценарий я воспроизвёл — он и сегодня незакрываем. Исполнитель это честно пометил `pending-operator`.

Плюс два неблокирующих: два токена `accepted` исполнитель выставил себе сам (по `L-16` при записи ставится `pending-operator`), и три числа «сколько тестов добавлено» разошлись с прогоном.

---

## 1. Таблица файлов (все 7 строк `git diff --stat e80d09fc..HEAD`)

| Файл | Стат | Что делает | Моя проверка |
|---|---|---|---|
| `shared/sdd/bdd-coverage.ts` | 32 ± | `extractTestCaseNames` (`:187`) исключает `skip`/`todo` в любом звене цепочки модификаторов; `checkUnparsedCoverageRows` (`:296`) градуирует severity по `flowVersion` | ПОДТВЕРЖДЕНО диффом и двумя мутациями (§5, M1/M2); ограничение — §8 B-2 |
| `cli/cmd/sdd-check/sdd-check.cmd.ts` | 2 ± (1 строка, `:560`) | единственный прод-вызов передаёт `ticketFlowVersion(file, repoRoot)` — иначе параметр мёртв | ПОДТВЕРЖДЕНО: `grep -n ticketFlowVersion` даёт `:560` рядом с `:589`/`:597` (тот же приём у `checkBddCoverage`) |
| `shared/sdd/anchor-inject.ts` | 28 ± | `canonicalName` (`:17`, регекс `:22`) узнаёт `### Phase P<N>`; `Phases Overview` распознаётся и на `###` (`:24`); новая экспортируемая `hasPhasesWithoutOverview` (`:132`) | ПОДТВЕРЖДЕНО мутациями M3/M4; строка-в-строку сверено с mermaid (§4) |
| `cli/cmd/sdd-migrate/sdd-migrate.cmd.ts` | 10 + | импорт + WARN-строка в отчёте (`:310`) до ветки `skip`, поэтому предупреждение видно и для «уже заанкоренных» тикетов | ПОДТВЕРЖДЕНО прогоном по корпусу (19 WARN) и мутацией M4 |
| `shared/sdd/__tests__/bdd-coverage.test.ts` | 109 ± | негативные фикстуры `.skip`/`.todo`/`.skip.each`/`.only`, 3 теста градации, «unsatisfiability guard» | 29 → **41** тестов (**+12**, не +11 и не +14 — §8 N-2); характер guard-теста — §3.3 |
| `shared/sdd/__tests__/anchor-inject.test.ts` | 109 + | Phase-слово, `_FIX`, `Phases Overview` на `###`, побайтовая реконструкция, `describe hasPhasesWithoutOverview` | 17 → **25** (**+8**, не +9 — §8 N-2) |
| `cli/cmd/sdd-migrate/__tests__/sdd-migrate.cmd.test.ts` | 67 + | 3 интеграционных: WARN в dry-run, `--write` анкорит `### Phase P1`, тикет без фаз WARN не даёт | 16 → **19** (**+3**, совпадает) |

Ни один файл из `git diff --stat` не остался без строки. Файлов вне зоны брифа не тронуто: `shared/sdd/check.ts`, `shared/verify/**`, `ai/flow-eval/**`, `ai/directives/**` — в диффе отсутствуют (сверено `git diff --name-only`). `shared/sdd/migration-plan.ts` действительно не содержит логики фаз (`grep -n "Phase\|PHASE"` → 0) — отклонение R-B2-10 §4.1 **ПОДТВЕРЖДЕНО**.

## 2. Что перезапущено мной (синхронно; дерево `rc-perf` не менялось)

| № | Команда | Мой фактический вывод | Exit | Совпадает с отчётом |
|---|---|---|---|---|
| 1 | `git diff --stat e80d09fc..HEAD` | 7 files changed, 337 insertions(+), 20 deletions(-) | 0 | да |
| 2 | `node --import tsx --test shared/sdd/__tests__/bdd-coverage.test.ts` | `# tests 41 # pass 41 # fail 0` | 0 | да |
| 3 | `node --import tsx --test shared/sdd/__tests__/anchor-inject.test.ts` | `# tests 25 # pass 25 # fail 0` | 0 | да |
| 4 | `node --import tsx --test cli/cmd/sdd-migrate/__tests__/sdd-migrate.cmd.test.ts` | `# tests 19 # pass 19 # fail 0` | 0 | да |
| 5 | `node --import tsx --test cli/cmd/sdd-check/__tests__/sdd-check.cmd.test.ts` | `# tests 83 # pass 83 # fail 0` | 0 | да (83/83) |
| 6 | `npm --prefix <tree> test` | `# tests 3756 # pass 3748 # fail 0 # cancelled 0 # skipped 8` (34.2 с, с первой попытки) | 0 | да |
| 7 | `npm --prefix <tree> run check` | `[sdd-verify] ✅ ALL PASS (5/5)` (type-check 4.3s, test:coverage 51.5s, lint 10.2s, format 2.5s, yagni 0.6s) | 0 | да |
| 8 | `npm --prefix <tree> run gate:sdd-check-baseline` | `OK — no error outside the baseline (baseline commit 227c03a83830124fe2aa22541dd5374beb8a53c6, tag rc-baseline-1).` | 0 | да |
| 9 | `npm --prefix <tree> run build` | `✓ built in 4.27s` | 0 | да |
| 10 | `node dist/gennady.js sdd-check --all . --format json` | error **192**, warn **435**, total 627, `ok:false` | 1 | да (красный по числу error — ожидаемо по `D-39`) |
| 11 | `node --import tsx cli/gennady.ts sdd-migrate anchors --all .` (dry-run) | `DRY-RUN · 127 ticket(s)`; `would` **121**, `skip` **6**, `WARN` **19**, `ERR` 0 | 0 | да |
| 12 | `git status --porcelain -- tasks/` после №11 (в клоне) | 0 строк | 0 | да |
| 13 | `git -C rc-perf status --porcelain` в конце всей проверки | пусто | 0 | — |

### Числа sdd-check: перемерены на «до/после», а не приняты на слово

В клоне откачены **все четыре** прод-файла на `e80d09fc`, прогон повторён:

| | error | warn |
|---|---|---|
| до (`e80d09fc`) | **192** | **434** |
| после (`96276f54`) | **192** | **435** |

Дельта по кодам: единственное изменение — `warn SDD_BDD_SCENARIO_UNTESTED` 70 → 71. Пофайловая дельта: **ровно одна новая находка**, `warn|SDD_BDD_SCENARIO_UNTESTED|tasks/cli/vcs-context-resolver/vcs-context-resolver.task-68.md`; исчезнувших нет. `SDD_BDD_COVERAGE_ROW_UNPARSED` warn **140 → 140**, `SDD_LEGACY_TICKET_UNANCHORED` **76 → 76**. **192/192, 434/435, 140, 71, 76 — ПОДТВЕРЖДЕНО.** Требование `GAP-B-1`/`D-38` (ноль новых error) соблюдено.

## 3. Существо B2-22

**3.1. `.skip`/`.todo`/неактивный тест не считаются observed — ПОДТВЕРЖДЕНО (с оговоркой B-2).** Мутация M1 (§5) роняет 6 тестов. Регекс `:189` захватывает всю цепочку модификаторов, `skip`/`todo` в любом звене исключают имя; `it.only`/`.concurrent` остаются observed (есть тест). `it.skip.each([...])(…)` регексом вообще не матчится (после `(` идёт `[`, не кавычка) — консервативно и не регресс: старый регекс тоже не матчил.

**3.2. Реальные тикеты корпуса с `.skip` — их ровно один, не «2–3».** Мой независимый скан (`v16-skip-scan.ts`: старый регекс минус новый по всем тест-файлам, затем перекрёстная сверка с телами тикетов):

```
test files scanned: 481; inactive-only case names: 9
CLAIMED-BY-TICKET: tasks/cli/vcs-context-resolver/vcs-context-resolver.task-68.md
    case "non-GitLab host — throws with GitHub deferred" inactive in cli/cmd/_shared/__tests__/vcs-context-resolver.test.ts
tickets scanned: 138; coverage claims backed only by inactive tests: 1
```

То есть популяция дефекта в корпусе = **1 тикет** (а не «неизвестно», как записано в `31:636` и `06 §6 п.6` — теперь измерено). Сценарий перестал закрываться: ровно этот файл — единственная новая находка прогона №10, и она **warn**, потому что scope `cli` ещё v1. **ПОДТВЕРЖДЕНО.** Просьба брифа «проверь на 2–3 реальных тикетах» физически невыполнима: третьего и второго в корпусе нет.

**3.3. Фикстура на неудовлетворимость и swift — ОПРОВЕРГНУТО в части swift.** Поставленный тест (`shared/sdd/__tests__/bdd-coverage.test.ts:277`) вызывает `checkBddCoverage` с **вручную собранной** `Map` и проверяет, что корректная строка с активным тестом даёт `[]`. Это регресс-замок против переужесточения, но не проверка неудовлетворимости: путь `getTestFileIndex` → расширения файлов в нём не участвует. Я построил настоящую swift-фикстуру в клоне (DONE-тикет `tasks/cli/vcs-context-resolver/v16-swift.task-999.md` с одной корректной строкой `` `FooTests.swift` :: `testResolvesContext` `` + реально существующий `cli/cmd/_shared/__tests__/FooTests.swift` с этим `func`):

```
findings on swift fixture ticket: 3
  warn SDD_BDD_SCENARIO_UNTESTED   ← незакрываемо: индекс не знает .swift
  error SDD_VERIFICATION_TABLE_INVALID
  warn SDD_TRACKER_MISSING_ROW
```

Причина подтверждена в коде: `cli/cmd/sdd-check/sdd-check.cmd.ts:532` — `/\.(test|spec)\.(ts|tsx|js)$/`, ни `.swift`, ни `.mts`. `shared/sdd/source-extensions.ts` не существует. То есть диагноз R-B2-22 §4.2 **ПОДТВЕРЖДЁН**, но риск пачки («swift-тикет с корректной строкой покрытия не должен стать незакрываемым», `62 §Пачка 16`, `A19`, `61:96`) на выходе пачки **остаётся открытым**, и приёмочная колонка `B2-22` («фикстура на неудовлетворимость: swift-тикет…») не закрыта. См. вопрос оператору Q1.

**3.4. Поддержанный синтаксис-subset «зафиксирован» только в JSDoc.** Требование `61:96`/`31:636` «зафиксировать поддержанный синтаксис-subset» выполнено как `@invariant` у `extractTestCaseNames` (`shared/sdd/bdd-coverage.ts:179-185`) плюс имя теста `'supported subset: active modifiers other than skip/todo (.only, .concurrent) still count as observed'`. В спеках — ничего: `grep -rn "extractTestCaseNames\|\.skip\|inactive" specs/cli/sdd-check/sdd-check.spec.md` → 0. И сам subset неполон (§8 B-2). **ЧАСТИЧНО.**

**3.5. Градация v1 warn / v2 error — правомерна против `L-3`/`GAP-B-1`, но противоречит спеке.** `L-3` («новые коды — warn сейчас») здесь не применим: `SDD_BDD_COVERAGE_ROW_UNPARSED` — не новый код, он существует с `D-CK014`. `D-39` («мягкого режима для v1 нет») формально задевается, но строка доски `B2-22` сама называет дефектом то, что код «вне градации `flowVersion`», а в том же модуле градация — установившийся образец (`checkBddCoverage:257`, `SDD_TRACKER_*`, `D-YG003`). `GAP-B-1` соблюдён: корпус целиком v1, warn 140 → 140, новых error ноль (§2). **ПОДТВЕРЖДЕНО как правомерное** — но ровно эту правомерность отрицает активная запись спеки, см. §8 B-1.

## 4. Существо B2-10

**4.1. `### Phase P<N>` и `Phases Overview` на `###` — ПОДТВЕРЖДЕНО.** Мутация M3 (возврат регекса `^P(\d+)`) роняет 3 теста `anchor-inject` и 2 теста `sdd-migrate`. В корпусе `grep -rlE "^### Phase P[0-9]" tasks | wc -l` → **17** — это ровно число из прозы отчёта пачки; 19 WARN = эти 17 + `vcs-client.task-86.md` и `vcs-draft-note.task-87.md`, у которых якоря фаз уже стоят, а `PHASES_OVERVIEW` нет. Разночтения «17 vs 19» в отчётах **нет** — это разные величины, и обе верны.

**4.2. Патч-инвариант (`06 §5.2 п.36`) — ПОДТВЕРЖДЕНО собственным побайтовым тестом по всему корпусу.** Мой первый вариант (снять строки-якоря из результата и сравнить с оригиналом) дал 19 «расхождений» — **методическая ловушка**: у этих тикетов якоря уже есть в исходнике, и фильтр вырезал заодно их. Корректная формулировка инварианта — «только вставки, в исходном порядке»: результат должен быть последовательностью строк оригинала без единого удаления и правки, все лишние строки — только `<!--SECTION:X-->`:

```
corpus=137 withInjections=90 clean=90 violations=0
```

Выборка из пяти, на которых проверено побайтово: `tasks/agent-inbox/README.md`, `tasks/agent-mon-cli/bootstrap.task-48.md`, `tasks/agent-mon-cli/cmd/cmd.task-47.md`, `tasks/agent-mon-cli/state/state.task-45.md`, `tasks/agent-mon-cli/ui/ui.task-46.md`. Расхождение с «91» у исполнителя — разные множества файлов (`git ls-files "tasks/**/*.md"` не включает `tasks/README.md`; `sdd-migrate --all` берёт 127 тикетов). Не находка, но число «91» невоспроизводимо без указания множества — см. N-2.

**Важная оговорка, которой нет ни в одном отчёте:** инвариант «только вставки» держится для `injectAnchors`, но **не** для `sdd-migrate anchors --write` целиком. Реальный прогон `--write` по корпусу в клоне: `121 files changed, 1580 insertions(+), 386 deletions(-)`. Все 386 удалений — строки 2-колоночной таблицы Verification, заменяемые 3-колоночной (`| Command | Required by |`, разделители, `npm run type-check | typescript-rules`), то есть `upgradeVerificationTable`/`scaffoldFirstRound` из пачки 22 (`E-06`), внутри их собственной зоны правки. Формулировка приёмки («вне зоны правки») соблюдена, но утверждать «мигратор ничего не переписывает» нельзя.

**4.3. Проверка, которой не делал исполнитель: B2-10 не создаёт находок и при реальной записи.** Я прогнал `sdd-migrate anchors --all . --write` в клоне дважды — с новым `canonicalName` и с откаченным на `^P(\d+)` — и сравнил `sdd-check --all .` после каждой записи:

| | error | warn | различий по (severity, code, file) |
|---|---|---|---|
| мигратор без B2-10 | 446 | 525 | — |
| мигратор с B2-10 | 446 | 525 | **0** |

То есть распознавание `Phase P<N>` не добавляет ни одной находки даже после фактической миграции корпуса. (Сам скачок 192→446 — свойство миграции легаси-корпуса в v2 вообще, `D-39`, к этой пачке отношения не имеет; но он показывает, чего стоит ожидать от `E-14`.) **ПОДТВЕРЖДЕНО с запасом.**

**4.4. dry-run по умолчанию — ПОДТВЕРЖДЕНО.** №11/№12 в §2: 127 тикетов просканировано, `git status --short tasks/` → 0 строк. Дополнительно: `MigrateOutcome` возвращает `{ok:true}` и процесс выходит с 0 (`sdd-migrate.cmd.ts:344,352`).

**4.5. «Явный отказ без `PHASES_OVERVIEW`» — ЧАСТИЧНО (см. Q2).** Воспроизведено в клоне на реальном тикете:

```
$ node --import tsx cli/gennady.ts sdd-migrate anchors --file tasks/cli/lint/cli-lint.task-14.md --write
[sdd-migrate anchors] WRITE · 1 ticket(s)
  WARN  tasks/cli/lint/cli-lint.task-14.md — phase sections found but no Phases Overview section/anchor; …
  +     tasks/cli/lint/cli-lint.task-14.md — META, BDD, PHASE_P1, EXECUTION_LOG
1 written.  → exit 0, git diff: 1 file changed, 9 insertions(+)
```

Строка доски `61:84` говорит «**явный отказ** без `PHASES_OVERVIEW`», трек `31:632` — «явный **отказ/предупреждение**, а не тихий пропуск». Фактически: тикет мигрируется, файл пишется, `ok:true`, exit 0, WARN живёт только в тексте отчёта — ни один автомат (CI, `execute`, `E-14`) отличить такой прогон от чистого не может. По `31` — выполнено; по букве `61` — нет. Исполнитель пометил `pending-operator` корректно.

## 5. Мутации (правило брифа 2) — все замки уронены мной, в клоне, без `git stash`

| # | Мутация (файл:строка) | Команда | Мой вывод | Замок |
|---|---|---|---|---|
| M1 | удалена строка `bdd-coverage.ts:193` (`modifiers.some(skip\|todo) → continue`) | `node --import tsx --test shared/sdd/__tests__/bdd-coverage.test.ts` | `not ok 3 - extractTestCaseNames`, `not ok 4 - checkBddCoverage`, `# tests 41 # pass 35 # fail 6` | держит |
| M2 | `bdd-coverage.ts:301` → `const severity = 'warn';` | то же | `not ok 5 - findUnparsedCoverageRows / checkUnparsedCoverageRows`, `# fail 1` | держит |
| M3 | `anchor-inject.ts:22` → старый `/^P(\d+)(\b\|_FIX\b)/i` | `--test anchor-inject.test.ts` + `sdd-migrate.cmd.test.ts` | `# pass 22 # fail 3` и `# pass 17 # fail 2` | держит |
| M4 | `anchor-inject.ts:135` → `return false && …` (семантическая, не «нет экспорта») | то же | `# pass 23 # fail 2` и `# pass 18 # fail 1` | держит |

M4 сделана намеренно семантической: в отчётах красный доказан `SyntaxError: does not provide an export named 'hasPhasesWithoutOverview'`, а это доказывает лишь наличие импорта, не поведение. С семантической мутацией замок всё равно держит — претензии снимаю, но формулировку доказательства в отчётах надо поправить (см. §9).

## 6. Конфликты (правило «пачка не ломает соседей»)

`git merge-tree --write-tree --name-only HEAD <ref>` (все PR-хеды подтянуты `refs/pull/<n>/head`, после проверки удалены):

| PR | Ветка | Результат |
|---|---|---|
| #38 | `lead/phase-agent-bounds` (`b55f7cac`) | CLEAN |
| #40 | `lead/journal-round` (`5813eb68`) | CLEAN |
| #41 | `lead/spec-authoring` (`fbf7e513`) | CLEAN |
| #42 | `lead/verify-stacks` (`f83a4733`) | CLEAN |
| #45 | `lead/review-critic-bounds` (`f0c1703f`) | CLEAN |
| #46 | `lead/verify-gate-scope` (`a501929e`) | CLEAN |
| #48 | `lead/reopen-by-cause` (`2f726c3a`) | CLEAN |
| #49 | `lead/promises-not-wider` (`1d42874c`) | CLEAN |
| #50 | `lead/axioms-one-home` (`682d338e`) | CLEAN |
| релиз | `550d50dc` = тек. `origin/codex/sdd-v2-rc52-followup` (после #47/#51) | CLEAN |

Текстовой чистоты мало, поэтому сверх того — **фактическое слияние с релизом в клоне** (`merge --no-edit 550d50dc`, затрагивает `package.json` overrides, `package-lock.json`, `scripts/test-topology.ts` +227, `test-topology.test.ts` +165):

- `node --import tsx scripts/test-topology.ts check` → `unit=216 contract=23 local=57 external=8 experimental=158`, `excluded=5`, без ошибок;
- четыре затронутых тест-файла после слияния: `# tests 168 # pass 168 # fail 0`.

Пачка новых тест-файлов не заводит, поэтому владелец-слой из PR #51 ей ничего не должен. **Конфликтов и семантических столкновений нет.**

## 7. Соответствие правилам брифа (`70 § Правила брифа по итогам аудита волн`)

| Правило | Вердикт |
|---|---|
| 2. Замок уронен мутацией, в отчёте команда и вывод | ЧАСТИЧНО: мутации были, но вывод в отчётах пересказан («зафиксировано в сессии», «module has no export… / assertion mismatch»), а для B2-22 такого текста и быть не могло — там нет нового экспорта. Правка §9-3 |
| 3. Любое число — вывод команды | ЧАСТИЧНО: 121/6/19, 127, 192/434/435, 140/71/76, 3756/3748/8, 41/25/19/83 — все воспроизведены точно; «+11»/«+14»/«+9 тестов» и «91 файл» — нет (N-2) |
| 4. Спеки: закрывая задачу, снять или пересмотреть запись | НАРУШЕНО — B-1 |
| 6. Каждый открытый пункт несёт токен `L-16` | ФОРМАЛЬНО да (4 токена), по существу — N-1 (самоприсвоенный `accepted`) |
| 7. Без `git stash` | НАРУШЕНО фактически (оба отчёта: `git stash` / `git stash push --keep-index`), но инцидент уже внесён в текст правила 7 задним числом; повторно не наказываю, фиксирую как известное |

## 8. Находки

### B-1 (блокирующее) — код пачки прямо противоречит активной записи спеки `D-CK014`; спека не тронута

- Где: `specs/cli/sdd-check/sdd-check.spec.md:222` — `D-CK014`, **Status: active**: «severity фиксирована умышленно, **без v1/v2-градации** (в отличие от `SDD_BDD_SCENARIO_UNTESTED`)»; и `:104` — «`SDD_BDD_COVERAGE_ROW_UNPARSED` … (**warn, всегда** — сегодня такая строка тихо пропадает из проверки, см. D-CK014)».
- Что: `shared/sdd/bdd-coverage.ts:296-306` теперь `severity = flowVersion === 'v2' ? 'error' : 'warn'`. Спека не изменена: `git diff --name-only e80d09fc..HEAD` не содержит ни одного файла из `specs/**`.
- Почему блокирующее: `B2-17` (spec-first) — объявленная предпосылка всей ветки `B2` и закрыта PR #31 именно ради того, чтобы код не расходился со спекой; правило брифа 4 требует «снять запись или явно пересмотреть её»; `L-16` определяет `accepted` как «спека дописывается под фактическое поведение» — а исполнитель поставил себе `accepted` и спеку не дописал.
- Воспроизведение: `sed -n '104p;218,222p' specs/cli/sdd-check/sdd-check.spec.md` и `sed -n '296,306p' shared/sdd/bdd-coverage.ts`.
- Правка (малая, механическая): в `sdd-check.spec.md` — новая запись `D-CK038` («`SDD_BDD_COVERAGE_ROW_UNPARSED` градуируется по `flowVersion`: v1 warn — сохранить baseline 140 строк `GAP-B-1`, v2 error — fail-closed после миграции; основание — `B2-22`, `A19`, строка доски `61:96`»), в `D-CK014` — `Superseded-by: D-CK038` в части severity, в `:104` заменить «warn, всегда» на «severity по `flowVersion`, см. D-CK038». Тем же куском — зафиксировать поддержанный синтаксис-subset (см. B-2), которого в спеке нет вообще.

### B-2 (блокирующее) — `describe.skip` остаётся «observed»: fail-closed по `A19` закрыт наполовину

- Где: `shared/sdd/bdd-coverage.ts:187-197` — модификаторы читаются только у `it`/`test`; `describe.skip`/`describe.todo` не учитываются. `@invariant` (`:181-184`) называет единственной известной дырой рантайм-`t.skip()`.
- Живой пример в дереве: `cli/cmd/lint/__tests__/dbc-contract.check.test.ts:18` — `describe.skip('DbcContractCheck', …)` с четырьмя `it(...)` внутри.
- Воспроизведение (клон, HEAD пачки):

```
$ node --import tsx -e "import {extractTestCaseNames} from './shared/sdd/bdd-coverage.ts'; …"
observed by HEAD extractor: ["should return no errors for valid content","should return LintError[] for content with missing contracts","should mutate file on disk when autofix is true","should preserve original filePath in errors"]
$ node --import tsx --test cli/cmd/lint/__tests__/dbc-contract.check.test.ts
# tests 0  # pass 0  # skipped 0  # todo 0
```

  Четыре имени «наблюдаются» при нуле запущенных тестов — ровно тот false closure, который запрещает `A19` («**неактивный тест** не закрывает сценарий») и который аксиома репозитория `ai/kit/axiom/process/ax-no-focused-or-skipped-tests-on-merge.xml:3` называет в одном ряду: «`it.only` / `describe.only` / `it.skip` / **`describe.skip`** / `it.todo`».
- Масштаб сегодня: нулевой — тикет `tasks/cli/lint/cli-lint.task-17.md` ссылается на этот файл (`:96` — «cov DbcContractCheck — 4 кейса → dbc-contract.check.test.ts (describe.skip — depends on tree-sitter)»), но он легаси и секции `TEST_COVERAGE` не имеет, поэтому `checkTicketBddCoverage` возвращает `[]`. Дефект **латентный и выстрелит ровно на миграции `E-14`**, то есть в той самой волне, ради которой пачка делалась.
- Правка — одна из двух, на выбор Lead: (а) учесть модификаторы `describe`/`suite` (отслеживать активный `describe.skip`-блок или как минимум не считать observed файл, где `describe.skip` присутствует) + тест на живом файле; (б) если (а) — отдельная задача, то поддержанный синтаксис-subset обязан назвать `describe.skip`/`describe.todo` **известной непокрытой дырой** (в спеке по B-1 и в `@invariant`), а дыра — уехать в `E-23` (adversarial-корпус) отдельной строкой доски. Молча оставлять нельзя: строка `B2-22` требует subset «зафиксировать», а зафиксированный subset сейчас умалчивает о случае, который в дереве реально есть.

### B-3 (решение оператора; исполнителем помечено `pending-operator`) — swift-неудовлетворимость не закрыта и не проверена

Подробности и воспроизведение — §3.3. Приёмочная колонка `61:96` и `A19` требуют swift-фикстуру; поставлен TS-тест, который неудовлетворимость не проверяет. Сам сценарий воспроизводится и сегодня красный. Вопрос Q1.

### N-1 (неблокирующее) — два токена `accepted` исполнитель присвоил себе сам

`R-B2-22 §4.1` («Токен: **`accepted`** (по факту зелёных гейтов и нулевой регрессии)») и `R-B2-10 §4.1` («Токен: **`accepted`** — файл не требовал правки»). По `L-16` (`02-LEAD-DECISIONS.md:22`) при записи ставится **`pending-operator`**, а `accepted` — вердикт оператора («принять как есть»), и гейт `SDD_DEVIATION_VERDICT_MISSING` считает открытым только `pending-operator`. Самоприсвоение выводит оба отклонения из-под гейта до того, как оператор их увидел, — и как раз первое из них несёт невыполненное следствие «спека дописывается» (B-1). Правка: переставить оба на `pending-operator`; по существу оба отклонения я подтверждаю (правка `sdd-check.cmd.ts:560` без вариантов необходима, `migration-plan.ts` действительно ни при чём) — оператору остаётся формальность.

### N-2 (неблокирующее) — четыре числа не воспроизводятся

| В отчёте | Факт (`# tests` до/после в клоне) |
|---|---|
| `R-B2-22 §1`: «+11 новых тестов» | 29 → 41 = **+12** |
| `R-BATCH-16` таблица: «+14 тестов» (`bdd-coverage.test.ts`) | **+12** |
| `R-B2-10 §1`/`R-BATCH-16`: «+5 … + 4» = «+9 тестов» (`anchor-inject.test.ts`) | 17 → 25 = **+8** |
| `R-B2-10 §3.1`/`R-BATCH-16`: «checked=91 files with injections» | **90** при множестве `git ls-files "tasks/**/*.md"`; число зависит от множества, а оно не названо |

Правило брифа 3. Правка — подставить вывод прогона и назвать множество файлов.

### N-3 (неблокирующее) — ссылка на диапазон строк в mermaid чуть шире функции

`R-B2-22` и `R-BATCH-16`: «`extractTestCaseNames` … `bdd-coverage.ts:187-202` (после правки)» — функция занимает `:187-197`. Остальные ссылки сверены и точны: `:184` (база), `:287` (база), `:296` (HEAD), `sdd-check.cmd.ts:560`, `anchor-inject.ts:17/18/22/132`, `sdd-migrate.cmd.ts:310`. Все стрелки «стало» на обеих mermaid — реальные вызовы: `sdd-check.cmd.ts:560 → checkUnparsedCoverageRows(…, ticketFlowVersion(…))` и `sdd-migrate.cmd.ts:310 → hasPhasesWithoutOverview(text)` присутствуют в коде дословно.

### N-4 (неблокирующее, унаследованное) — закрытый инвентарь спеки `sdd-migrate` не знает новую сущность

`specs/cli/sdd-migrate/sdd-migrate.spec.md` §3 Entity Inventory (closed-world) перечисляет `injectAnchors` и `scaffoldExecutionLog` из `shared/sdd/anchor-inject`, но не `hasPhasesWithoutOverview`. Дыра **не введена этой пачкой**: там же нет `scaffoldFirstRound`, `upgradeVerificationTable`, `parsePhasesOverview` (пачка 22 / `E-06`). Правку логично слить с B-1 одним касанием спек, но задачей пачки 16 не считаю.

## 9. Правки, которые нужно внести до пуша

1. **B-1:** `specs/cli/sdd-check/sdd-check.spec.md` — новая запись `D-CK038` + `Superseded-by` у `D-CK014` в части severity + правка перечня кодов `:104`; там же — раздел «поддержанный синтаксис-subset `extractTestCaseNames`» с явным перечнем (`it`/`test`, любая цепочка модификаторов, исключаются `skip`/`todo`; не покрыто: `describe.skip`/`describe.todo`, рантайм `t.skip()`, `.each`-формы).
2. **B-2:** либо код + тест на `describe.skip`, либо явная фиксация дыры (см. B-2, вариант «б») + строка на доске с маршрутом в `E-23`.
3. **Правило 2:** в `R-B2-22 §3` строка 3 и `R-B2-10 §3` строка 5 — заменить пересказ на вывод команды; для B2-22 указать честную мутацию (у `bdd-coverage.ts` нет нового экспорта, «module has no export» там невозможно). Готовые к вставке мутации и их вывод — §5 этого документа.
4. **N-1:** оба `accepted` → `pending-operator` в `R-B2-22 §4.1`, `R-B2-10 §4.1`, `R-BATCH-16` пп. 1 и 3.
5. **N-2/N-3:** подставить `+12` / `+8` / `+3`, `90` (с указанием множества), `:187-197`.
6. **Дополнить отчёты тем, чего в них нет, а стоило бы** (я это измерил, числа готовы): популяция дефекта `.skip` в корпусе = 1 тикет из 138 (закрывает «оценка не подтверждена измерением», `06 §6 п.6`); B2-10 не добавляет находок и при реальном `--write` (§4.3); `--write` целиком не является чистой вставкой — 386 удалений от `upgradeVerificationTable` (§4.2).

## 10. Остатки для доски

1. `B2-22` — **не закрывать как ВЫПОЛНЕНО**, пока не сделаны B-1 и B-2 и пока не решён Q1: приёмочная колонка строки `61:96` содержит swift-фикстуру, которой нет.
2. Новая строка (кандидат, после решения Q1): **`B2-23`** «`getTestFileIndex`/`source-extensions.ts`: индекс тест-файлов знает не только `.ts/.tsx/.js`; swift-тикет с корректной строкой покрытия закрывается» — размер S–M, критерий `A19`, предпосылка для `E-23`, основание `ISS-9.2` (`20-ISSUES-VERDICTS.md:98`).
3. Новая строка (кандидат): **`B2-24`** «`describe.skip`/`describe.todo` не дают observed-имён» — S, критерий `A19`, туда же `E-23`. Либо — правка объёма `E-23` с явным adversarial-кейсом `describe.skip`.
4. Уже названный исполнителем долг: **19 тикетов корпуса** (`tasks/cli/lint/*` ×8, `tasks/cli/alt-opinion/*` ×4, `tasks/dbc/dbc-linter/*` ×4, `tasks/vcs/vcs-client/*` ×2, `tasks/cli/vcs-draft-note/*` ×1) требуют ручного дописывания `Phases Overview` — отдельная строка, предпосылка для `E-14`; полный список воспроизводится `sdd-migrate anchors --all . | grep '^  WARN'`.
5. `31-TRACK-CHECK-LOG.md:636` и `06 §6 п.6` — снять оговорку «сколько строк корпуса опираются на `.skip`/`.todo`-покрытие — неизвестно»: измерено, **1 тикет** (§3.2).
6. `06 §5.2 п.36` (`B2-10`) — можно отмечать выполненным по существу (патч-инвариант доказан на 90 файлах, dry-run по умолчанию), но с оговоркой §4.2 про `--write` в целом.
7. `B2-13` — статус «ОТЛОЖЕНА до PR #38» подтверждён; в пачку 16 корректно не входила.

## 11. Вопросы оператору по двум токенам `pending-operator`

**Q1 (B2-22, swift / `source-extensions.ts`).** Приёмка `B2-22` на доске (`61:96`) и критерий `A19` требуют фикстуру «swift-тикет с корректной строкой покрытия не становится незакрываемым». Исполнитель поставил вместо неё TS-тест, который неудовлетворимость не проверяет; я воспроизвёл настоящий случай — DONE-тикет с существующим `FooTests.swift` и корректной строкой покрытия по-прежнему даёт `SDD_BDD_SCENARIO_UNTESTED`, потому что `sdd-check.cmd.ts:532` индексирует только `\.(test|spec)\.(ts|tsx|js)$`. Вопрос: **(а)** принять `B2-22` в текущем объёме, а расширение индекса вынести новой строкой `B2-23` (и тогда `A19` остаётся частично открытым до неё — что именно и записать в трек), **(б)** дополнить пачку 16 фиксом `source-extensions.ts` сейчас (объём S–M, задевает `sdd-check.cmd.ts` — файл вне списка «трогать» брифа), или **(в)** переписать приёмочную колонку `B2-22`, сняв swift-фикстуру и передав её целиком в `E-23`? Мой совет: **(а)** — фикс индекса меняет поведение на весь корпус и просится в отдельный замер по `GAP-B-1`, а вот приёмку `B2-22` без него закрывать нельзя, её надо переформулировать явно.

**Q2 (B2-10, форма отказа без `PHASES_OVERVIEW`).** Доска (`61:84`) говорит «явный **отказ**», трек (`31:632`) — «явный **отказ/предупреждение**». Реализовано предупреждение: тикет всё равно мигрируется и пишется, `MigrateOutcome.ok = true`, exit 0, WARN виден только человеку в тексте отчёта (воспроизведение — §4.5). Вопрос: **(а)** этого достаточно, трек — источник истины, строку доски привести к формулировке «явное предупреждение»; **(б)** нужен машиночитаемый сигнал без отказа от записи — ненулевой exit или счётчик `warnings` в `MigrateOutcome`, чтобы `E-14`/CI могли остановиться (объём S); или **(в)** нужен настоящий hard-отказ — такой тикет пропускается целиком, `--write` для него не срабатывает (объём S, но меняет смысл: 19 тикетов корпуса тогда не мигрируются вовсе до ручной правки). Мой совет: **(б)** — «тихо» перестало быть тихим только для человека, а весь смысл строки доски в том, чтобы миграция не проехала мимо автомата; при (в) 19 тикетов зависают до ручной работы, и это стоит решать отдельно от формы сигнала.

---

## Итог

**Блокирующее (до пуша и PR):**
1. **B-1** — `D-CK014` (active) словами запрещает то, что делает код; `specs/cli/sdd-check/sdd-check.spec.md:104,222` не тронуты. Нарушены spec-first (`B2-17`), правило брифа 4 и следствие токена `accepted` по `L-16`. Правка малая и механическая (§9-1).
2. **B-2** — `describe.skip` по-прежнему даёт «observed»: 4 имени при 0 запущенных тестах на живом файле `cli/cmd/lint/__tests__/dbc-contract.check.test.ts:18`. `A19` («неактивный тест не закрывает сценарий») закрыт наполовину, а «зафиксированный синтаксис-subset» об этом молчит. Либо фикс, либо явная фиксация дыры + маршрут в `E-23` (§9-2).

**Требует решения оператора (не правится верификатором):** **B-3**/Q1 (swift-фикстура и `source-extensions.ts`), Q2 (форма отказа без `PHASES_OVERVIEW`). Оба токена по существу заявлены исполнителем верно; переформулированы в §11 как решения с вариантами и советом.

**Неблокирующее:** N-1 (два `accepted` вместо `pending-operator`), N-2 (+11/+14/+9/91 → +12/+12/+8/90), N-3 (диапазон `:187-202` → `:187-197`), N-4 (закрытый инвентарь `sdd-migrate.spec.md` не знает `hasPhasesWithoutOverview` — унаследовано от пачки 22), плюс уже внесённый в правило 7 факт мутаций через `git stash`.

**Подтверждено (перепроверено мной, а не принято на слово):**
- `.skip`/`.todo`/`.skip.each` больше не «observed»; `.only`/`.concurrent` — остаются; мутация M1 роняет 6 тестов.
- Градация `SDD_BDD_COVERAGE_ROW_UNPARSED` по `flowVersion` правомерна против `L-3`/`GAP-B-1`/`D-39` (код не новый, образец в модуле есть, корпус целиком v1, warn 140→140) — но упирается в B-1.
- Ровно один тикет корпуса перестал закрываться ложно: `tasks/cli/vcs-context-resolver/vcs-context-resolver.task-68.md`, severity **warn** (scope `cli` = v1). Популяция дефекта измерена независимо: 1 из 138 тикетов, 9 inactive-only имён на 481 тест-файл.
- `sdd-check --all .`: error **192 → 192** (ноль новых), warn **434 → 435**, единственная новая находка — названная выше; `GAP-B-1`/`D-38` соблюдены; `gate:sdd-check-baseline` зелёный.
- `### Phase P<N>` распознаётся (17 тикетов корпуса), `Phases Overview` на `###` — тоже; 19 WARN = 17 + 2 уже заанкоренных; `dbc-linter.task-08.md` (без фаз) WARN не даёт.
- Патч-инвариант `injectAnchors`: **90 файлов корпуса, 0 нарушений**, только вставки строк-якорей, ни одного удаления или правки (собственный побайтовый тест, выборка из 5 названа).
- dry-run по умолчанию: 127 тикетов, `git status --short tasks/` → 0 строк.
- Числа `121 would / 6 skip / 19 WARN`, `127`, `140/71/76`, `3756/3748/8`, `41/25/19/83` — воспроизведены дословно.
- Все четыре замка держат на семантических мутациях (M1–M4), включая ту, где исполнитель доказывал красный лишь отсутствием экспорта.
- `merge-tree` чист со всеми девятью открытыми PR (#38, #40, #41, #42, #45, #46, #48, #49, #50) и с релизной веткой `550d50dc`; фактическое слияние с релизом в клоне: `test-topology check` в норме, 168/168 затронутых тестов зелёные.
- Сверх отчётов: B2-10 не добавляет ни одной находки `sdd-check` даже после реального `--write` по всему корпусу (446/525 с фиксом и без, 0 различий по (severity, code, file)).
- Гейты: `npm test` 3756/3748/0/8 exit 0; `npm run check` ALL PASS (5/5); `gate:sdd-check-baseline` OK; `npm run build` ✓; `node dist/gennady.js sdd-check --all .` exit 1 по числу error — ожидаемо по `D-39`, не регрессия.
- Дерево `rc-perf` после проверки не изменено (`git status --porcelain` пусто, HEAD `96276f54`), временные ref'ы `refs/v16/*` удалены.
