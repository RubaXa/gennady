V-BATCH-19 — независимая проверка пачки 19 «Авторинг спек соразмерен задаче и не переписывает историю»

Проверяющий: `plan-verifier` (свежие глаза). Дата: 2026-09-09.
Дерево: `.../scratchpad/rc-w3`, ветка `lead/spec-authoring`, база `c9b58636` (голова RC `origin/codex/sdd-v2-rc52-followup`).
Коммиты: `d0dde2cd` (T-B6-01), `068c7080` (T-B6-14), `1c4b746f` (V14-1). T-B6-10 — коммита нет (заявленный STOP).
Проверены отчёты: `R-T-B6-01.md`, `R-T-B6-14.md`, `R-V14-1.md`, `R-T-B6-10.md`, `R-BATCH-19-spec-authoring.md`.
Метод: каждое утверждение → команда/строка первоисточника → вердикт. Все команды-доказательства перезапущены мной, не переписаны из отчётов.

---

## A. Полнота и зона

`git diff --stat c9b58636..1c4b746f` → 6 файлов, 258 insertions(+), 33 deletions(-).

| Файл | В таблице сводного отчёта | В таблице пер-задачного | Вердикт |
|---|---|---|---|
| `ai/kit/templates/sdd-v2/scope.directive.hbs` | да | T-B6-01, T-B6-14, V14-1 | ПОДТВЕРЖДЕНО |
| `ai/kit/templates/sdd-v2/module.directive.hbs` | да | T-B6-01, T-B6-14, V14-1 | ПОДТВЕРЖДЕНО |
| `ai/directives/sdd-v2/scope.directive.xml` | да | все три | ПОДТВЕРЖДЕНО |
| `ai/directives/sdd-v2/module.directive.xml` | да | все три | ПОДТВЕРЖДЕНО |
| `ai/kit/lint-axioms.ts` | да (отклонение №2) | R-T-B6-01 §4 п.1 | ПОДТВЕРЖДЕНО |
| `ai/kit/__tests__/scale-express-authoring.test.ts` | да | R-V14-1 | ПОДТВЕРЖДЕНО |

Пер-коммитные счётчики (`git show --stat`): 5 / 4 / 5 файлов — совпадают со строками «`git diff --stat`: N файлов» в каждом отчёте. ПОДТВЕРЖДЕНО.

**Запретные зоны — чисто.** `git diff --name-only c9b58636..1c4b746f` не содержит ни `execute.directive.*`, ни `phase-execution-protocol*`, ни `audit-halt-activation.mjs` (зона PR #38), ни одного пути под `shared/**`, `cli/**`, `ai/flow-eval/**`. ПОДТВЕРЖДЕНО.

`ai/kit/lint-axioms.ts` — единственный выход за буквальную зону; правка добавляет два имени файла в существующую строку `KNOWN_DANGLING_AXIOM_REFS` для `AX_STALE_AFTER_PIVOT_VERIFICATION` (владелец T-B6-17), новой политики не заводит. Отклонение задекларировано в обоих отчётах. ПОДТВЕРЖДЕНО, возражений нет.

---

## B. Существо задач

### T-B6-01 — pivot объявляет решения замещёнными

- Режимы `greenfield · refine · pivot · rewrite` в Mission, ветвление в `STEP_0_CREATE` (`scope.directive.xml:71-80`), halts `H_REWRITE_WITH_DOWNSTREAM` (`:63`), `H_PIVOT_NO_INVALIDATION_LIST` (`:64`), оба процитированы из шагов (`:79`, `:118`) — ПОДТВЕРЖДЕНО, `audit:halts` зелен.
- «Prior Decision Log entry is preserved byte-for-byte» — `scope.directive.xml:77`; аксиома `AX_PIVOT_REQUIRES_SUPERSESSION` подключена (`:21`). ПОДТВЕРЖДЕНО.
- `AX_REFINE_MODULE_PRESERVES_CONTRACTS` собран в module (`module.directive.xml:19`), breaking change → `Risk accepted` + Pivot Invalidation List по consumer-модулям (`:101-107`). ПОДТВЕРЖДЕНО.
- **Согласованность с D-41/IDN.** Аксиома прямо запрещает миграцию идентичности: «The prior id may be a legacy `D-N` only when it already existed; never rewrite append-only history just to rename it» (`ai/kit/axiom/spec/ax-pivot-requires-supersession.xml`). D-41 «ID не мигрируются» — ПОДТВЕРЖДЕНО, конфликта нет.
- **Согласованность с L-16/D-49 (токены вердикта).** Формат `REWORK_DECISION_LOG_ENTRY_FORMAT` (`formats/pivot-formats.xml`) — однострочный, `supersedes: <prior-id>`, и явно «never add Status/Recorded fields». Токены `pending-operator`/`accepted`/`rework`/`rolled-back` живут на другой оси (запись отклонения, гейт `SDD_DEVIATION_VERDICT_MISSING`); пятого статуса «superseded» в набор не вносится. ПОДТВЕРЖДЕНО, коллизии нет.
- **ОПРОВЕРГНУТО (неблокирующее).** Таблица файлов `R-T-B6-01.md:16` и узел `Fmt` в mermaid «стало» утверждают, что scope цитирует формат через `READ_AND_USE_DIRECTIVE(".../pivot-formats.xml")`. Фактически `scope.directive.xml:117` содержит прозаическое «read `ai/directives/sdd-v2/formats/pivot-formats.xml`». Машинно-распознаваемую форму несут `module.directive.xml:105`, `infra.directive.xml:460`, `interface.directive.xml:266` и сам scope в другом месте (`:154`). Прозаическая форма не образует ребро в графе `READ_AND_USE_DIRECTIVE` (`ai/kit/delta-assembly.ts:73`, `ai/kit/audit-contract-activation.mjs:282`), поэтому аудит её не покрывает — гейт зелен, но покрытия нет. **Правка:** привести `scope.directive.hbs` к общей форме `READ_AND_USE_DIRECTIVE("ai/directives/sdd-v2/formats/pivot-formats.xml")`, перегенерировать, поправить строку таблицы и узел mermaid.
- **ОПРОВЕРГНУТО (неблокирующее).** Колонка «Чем доказано» называет `stateless-sdd-flow-contract.test.ts` («17 якорных regex»), а §3 п.5 — «новый kit-тест `deps.test.ts`/`stateless-sdd-flow-contract.test.ts` (регресс-замки на буквальный текст)». Ни один тест в коммите `d0dde2cd` не заведён и не изменён; `grep -n "supersed\|SUPERSESSION\|H_REWRITE\|Invalidation" ai/kit/__tests__/stateless-sdd-flow-contract.test.ts` не даёт ни одного попадания по новому тексту директив. **Регресс-замка у T-B6-01 нет вообще.** Правка: либо убрать слово «новый» и не выдавать существующий тест за доказательство, либо (лучше) добавить 3–4 ассерта в `scale-express-authoring.test.ts` или соседний файл: `H_REWRITE_WITH_DOWNSTREAM`, `H_PIVOT_NO_INVALIDATION_LIST`, «preserved byte-for-byte», Pivot Invalidation List в module.
- **ОПРОВЕРГНУТО (неблокирующее).** Mermaid «было»: `scope.directive.xml:33-36 (до правки)`. На базе `c9b58636` «replaced as a whole» — строка **45**, `STEP_0_CREATE` — **39**. Правка: заменить на `:39-45`.
- Эвал `fibonacci-library` не запускался; обоснование (`ai/flow-eval/**` вне зоны, готовой pivot-фикстуры нет) проверено — фикстуры действительно нет. НЕ ПРОВЕРЯЕМО в границах пачки; отклонение честно задекларировано в обоих отчётах.

### T-B6-14 — самопроверка стиля перед записью документа целиком

- `AX_ARTIFACT_STYLE_SELF_CHECK` определён и процитирован из шага в обоих владельцах: `scope.directive.xml:32` + `:112`, `module.directive.xml:31` + `:90`. ПОДТВЕРЖДЕНО.
- **Покрыты ли все `Write` целого документа.** В scope и module по одному `Write` целого файла, оба теперь под аксиомой. Литеральную формулировку «exactly ONE `Write`» во всём корпусе несут только эти два файла; ещё четыре владельца (`infra`, `interface`, `discover-from-code`, `recover-from-code`) уже несли аксиому до пачки. Множество «6 владельцев» ПОДТВЕРЖДЕНО.
- **ОПРОВЕРГНУТО (неблокирующее).** Mermaid «было»/«стало» в `R-T-B6-14.md` даёт якоря `infra.directive.xml:207`, `interface.directive.xml:140`, `recover-from-code.directive.xml:69`. Фактически — 212 / 145 / 74, причём и на базе `c9b58636`, и на голове ветки (эти файлы пачкой не тронуты). Верен только `discover-from-code.directive.xml:37`. Правка: три числа.
- **Неблокирующее.** У T-B6-14 нет регресс-замка: `grep -rn AX_ARTIFACT_STYLE_SELF_CHECK ai/kit/__tests__` — пусто. Приёмка доски («every whole-document Write carries style self-check») держится только на grep из отчёта. Правка: один ассерт в `scale-express-authoring.test.ts` (или переименовать файл в `authoring-invariants.test.ts`).

### V14-1 — масштаб и экспресс-ветвь доезжают до scope/module

- `deps=` расширены: `module.directive.xml:8` и `scope.directive.xml:10` (в scope строка сдвинулась на 2 из-за строки Modes в Mission — задание называло `:8`, фактически `:10`; содержательно ПОДТВЕРЖДЕНО в обоих).
- `grep -c AX_SCALE_PROPORTIONAL_DEPTH` → 2 и 2 (приёмка требует ≥1). ПОДТВЕРЖДЕНО.
- Тело аксиомы в scope/module не инлайнится — она объявлена как унаследованная и цитируется по имени. Это ровно образец `infra.directive.xml:316-323`, названный триажем: `infra` тоже только цитирует, единственное место определения — `router.directive.xml`, через который проходит любой вход. Отклонения от образца нет. ПОДТВЕРЖДЕНО.
- Все точки утверждения вне ветки: `</LogicSwitch>` scope `:100` < `STEP_3_CHECK` `:123` < `STEP_4_CLOSE` `:141`; module `:82` < `STEP_4_CHECK` `:119` < `STEP_5_CLOSE` `:135`. ПОДТВЕРЖДЕНО.
- Тест `ai/kit/__tests__/scale-express-authoring.test.ts` перезапущен мной: `# tests 7 / # pass 7 / # fail 0`. Тест содержательный, а не косметический: он проверяет и позицию шагов относительно `</LogicSwitch>`, и сохранение условия полноты брифа, и наличие обоих receipts (`draftReceipt`/`moduleReceipt`, `sdd-log ... authoring-complete`). ПОДТВЕРЖДЕНО.
- **ОПРОВЕРГНУТО (неблокирующее).** Mermaid «было» и текст отчёта: «scope/module не знают о SCALE, всегда полный интервью-путь». До пачки оба файла уже несли `<ScalePath when="scale=function|fix">` (`scope.directive.xml:135`, `module.directive.xml:130` — единственные два файла корпуса с этим элементом; на базе — `module:95`). Верна только половина утверждения (про `deps=`). Следствие по существу: теперь в одном файле два конструкта с одинаковым триггером `fix|function` — `ScalePath` и новый `LogicSwitch`. Это тот класс, на который смотрит `AX_PARALLEL_MECHANISM_REQUIRES_JUSTIFICATION`. Правка: поправить формулировку «было» и добавить одну строку — почему две конструкции сосуществуют (разные предметы: `ScalePath` — про число модулей, `LogicSwitch` — про глубину интервью); либо завести follow-up на объединение.

### Смысл директив сверх задач не изменён

`git diff c9b58636..1c4b746f -- ai/kit/templates ai/kit/axiom` прочитан целиком. Вне трёх заявленных тем изменений нет: ни один шаг не удалён, ни один `ToolCall`/receipt не тронут, ни одна аксиома-файл не отредактирована (подключены три уже существующих партиала). Единственная неучтённая в задачах правка — `ver="2.3"` → `ver="2.4"` в обоих шапках, что нормально. ПОДТВЕРЖДЕНО.

### T-B6-10 — правомерность STOP

- Строка доски `61-TASK-BOARD.md:155` (не `:153`, как пишет `R-T-B6-10.md:13` — правка) в колонке «Файлы» прямо содержит `.hbs` монолитов; трек `40-TRACK-DIRECTIVES-SKILLS.md:831` формулирует задачу как «`infra` (9592), `root` (7572), `migration-v1-v2` (6741), `router` (6094) **переведены в `lazy`**», файлы включают четыре `.hbs`, «Step-блоков достаточно: 9/5/9/3» (проверено: 9/5/9/3 — совпадает). Бриф `62-BATCH-QUEUE.md:270` сузил зону до двух файлов. Расхождение доски и брифа — реально. ПОДТВЕРЖДЕНО.
- L-12: «единый лимит 8000 для всех, монолиты тоже; **предусловие — lazy-split `infra` раньше включения гейта**». Q6 в треке (`:930-934`) — то же предупреждение. STOP согласуется с уже принятым решением. ПОДТВЕРЖДЕНО.
- Стоп-класс 2 процитирован верно: `70-ORCHESTRATION-PROTOCOL.md:105` — «красная задача не чинится втихую расширением baseline (изменение baseline — отдельное operator-approved решение)». ПОДТВЕРЖДЕНО.
- D-55 (карта изменений до старта брифа) подтверждает: расширение зоны на четыре чужих `.hbs` требует новой утверждённой оператором карты, а не решения исполнителя. ПОДТВЕРЖДЕНО.
- Механика измерена мной заново: `ai/kit/step-budget-gate.ts` — `if (!existsSync(stepsDir)) continue;`, лимиты `SKELETON_TOKEN_TARGET = 6000` / `SKELETON_TOKEN_LIMIT = 8000` (`:38`, `:40`). Пересчёт `countTokens` по всем 28 директивам: `infra` 9633, `root` 7572, `migration-v1-v2` 6741, `router` 6094 — цифры отчёта воспроизведены точь-в-точь. ПОДТВЕРЖДЕНО.
- **ОПРОВЕРГНУТО (правка обязательна до PR).** Формулировка «гейт не покрывает **4 монолитные директивы**» (`R-BATCH-19:18`, `:62`, `R-T-B6-10:29`) неверна: `assembly-manifest.json` объявляет `lazy` только три директивы (`audit`, `scaffold`, `phase-execution-protocol`), значит гейт **не измеряет 25 из 28**. Четыре — это лишь те, что названы задачей и превышают пороги. Читателю PR сейчас сообщается меньший масштаб, чем есть. Правка формулировки: «гейт измеряет 3 директивы из 28; из 25 неизмеряемых `infra` (9633) выше жёсткого лимита 8000, ещё три выше мягкой цели 6000, остальные в пределах».
- **Неблокирующее.** Число `9592` на доске и в брифе устарело — замерено `9633` (2026-09-09). Правка доски/трека/очереди.
- **Возражение по существу вариантов.** Отчёт предлагает два равноправных пути. Вариант 2 (baseline для `check:directive-budgets`) семантически совпадает с Q6(b) — «отдельный, более мягкий лимит для монолитов», который трек отклонил именно словами «легализует текущее состояние и снимает мотивацию разбивать», и который уже закрыт решением L-12 в пользу (a). Подавать его как равную альтернативу — значит скрыто переоткрывать L-12. Правка: пометить вариант 2 как «откат L-12», а не как альтернативу внутри действующего решения.

---

## C. Регрессии — перезапущено мной

| Команда | Мой результат | Заявлено | Вердикт |
|---|---|---|---|
| `npm test` | `# tests 3638 / # pass 3630 / # fail 0 / # skipped 8`, exit 0 | 3630/3638/0 | ПОДТВЕРЖДЕНО |
| `npm run check` (1-й прогон) | exit 1: `⛔ лестница остановлена на «test:coverage»`, 1 fail + 7 cancelled | ALL PASS 5/5 | флейк, см. ниже |
| `npm run check` (2-й прогон) | `✅ ALL PASS (5/5)`, exit 0 | ALL PASS 5/5 | ПОДТВЕРЖДЕНО |
| `npm run build:directives` | `Generated 55 directive(s).`, 35 dangling-axiom warnings, 0 undefined-ref errors, exit 0 | то же | ПОДТВЕРЖДЕНО |
| `npm run check:directives-fresh` | `✓ ai/directives/** matches a fresh rebuild.` | то же | ПОДТВЕРЖДЕНО |
| `git status --porcelain` после сборки | пусто | пусто | ПОДТВЕРЖДЕНО (регенерация детерминирована) |
| `npm run audit:sdd-templates` | fresh ✓; axioms clean 28; contracts clean 28+33; halts clean 33+33; budgets ✓; exit 0 | то же | ПОДТВЕРЖДЕНО |
| `npm run gate:sdd-check-baseline` | `OK — no error outside the baseline (baseline commit 227c03a8…, tag rc-baseline-1)` | то же | ПОДТВЕРЖДЕНО |
| `node --test ai/kit/__tests__/scale-express-authoring.test.ts` | 7/7 pass | 7/7 | ПОДТВЕРЖДЕНО |

**Флейки под нагрузкой — поимённо.** `load average 88.53` при 12 ядрах. В упавшем прогоне `test:coverage` (топология `coverage`, не `deterministic`) отвалились восемь файлов, все — CLI-интеграция с порождением подпроцессов: `cli/__tests__/tool-behavior/bootstrap-path.test.ts`, `clean-repo-composition.test.ts`, `sdd-task.test.ts`, `sdd-verify-repair-adapters.test.ts`, `sdd-verify.test.ts`, `testcov.test.ts`, `cli/cmd/lint/__tests__/lint.cmd.test.ts`, `cli/cmd/testcov/__tests__/testcov.cmd.test.ts`; плюс 7 отменённых по каскаду. Ни одного файла из `ai/kit/**` среди них — к содержанию пачки нестабильность отношения не имеет. Повторный прогон зелёный. Отчёты называют флейк, но не называют файлы — правка: перечислить восемь имён, иначе «флаки под нагрузкой» неотличимо от отговорки.

**Слияния.** `git merge-tree --write-tree` от `1c4b746f` — чисто со всеми: голова RC `c9b58636`, `lead/phase-agent-bounds` (#38), `lead/journal-round` (#40), `lead/verify-stacks`, `lead/eval-reproducible`. Конфликтов нет ни одного.
Про общий каталог с #38: пересечение по файлам — только `ai/kit/lint-axioms.ts`, и правки лежат в разных ганках одного массива (`:193` против `:207-215`), разные id аксиом — сливается автоматически. **Конфликтов в сгенерированных директивах нет и быть не может:** #38 регенерирует `execute`/`reconcile`/`scaffold`/`phase-execution-protocol`, пачка 19 — `scope`/`module`, множества не пересекаются; ни один из шести партиалов, включаемых в `scope`/`module`, не тронут веткой #38. Правило «перегенерировать после слияния» здесь избыточно, но `check:directives-fresh` после мержа стоит прогнать как дешёвый инвариант. `lead/verify-stacks` и `lead/eval-reproducible` вообще не трогают `ai/kit/**` и `ai/directives/**`.

---

## D. Сводный отчёт-черновик PR

- Понятность без кодов: раздел «Черновик PR (простым языком)» читается без словаря — ПОДТВЕРЖДЕНО, за одним исключением: четвёртый пункт занижает масштаб (см. правку про «4 монолита»).
- Соответствие D-57: описание словами ✓, таблица файлов ✓, mermaid было/стало ✓, команды-доказательства с результатом ✓, вердикт независимой проверки — отсутствует (появится после этой проверки), коды задач с расшифровкой — частично: `T-B6-01`/`T-B6-14`/`V14-1` в разделе «Команды пуша» и заголовке идут без расшифровки словами.
- Mermaid сводного отчёта сверен с кодом: узлы «стало» (`STEP_0_CREATE` ветвит режимы, `LogicSwitch` в `STEP_1_ORIENT`, `AX_ARTIFACT_STYLE_SELF_CHECK` перед `Write`, `STEP_3_CHECK`/`STEP_4_CLOSE` вне ветки) — все подтверждаются файлом. Узел `Budget2` («4 монолита по-прежнему не сканируются») — цифра неверна, см. выше. В mermaid сводного отчёта якорей `file:line` нет, поэтому проверять там нечего; ошибочные якоря — в пер-задачных отчётах (перечислены в §B).
- STOP T-B6-10 описан честно: не выдан за выполненную задачу, измерения приведены и воспроизводятся, ссылка на стоп-класс корректна. Единственная нечестность — подача варианта 2 как равноправного при действующем L-12.

---

## Итог

**Блокирующее (до PR):**
1. `R-BATCH-19:18`, `:62` и `R-T-B6-10:29` — «гейт не покрывает 4 монолитные директивы». Гейт измеряет 3 директивы из 28. Формулировку исправить в обоих отчётах и в тексте PR: это утверждение о состоянии продукта, а не о размере задачи.
2. `R-T-B6-10` — вариант 2 (baseline для `check:directive-budgets`) подать как откат L-12 / возврат к отклонённому Q6(b), а не как альтернативу внутри действующего решения.

**Неблокирующее (правки текста и один шов кода):**
3. `scope.directive.hbs` — перевести ссылку на `formats/pivot-formats.xml` в `READ_AND_USE_DIRECTIVE(...)`, как у module/infra/interface; перегенерировать. Поправить `R-T-B6-01.md:16` и узел `Fmt`.
4. `R-T-B6-01` — убрать `stateless-sdd-flow-contract.test.ts`/`deps.test.ts` из колонки «Чем доказано» и слово «новый»: тестов пачка для T-B6-01 не завела.
5. Регресс-замки для T-B6-01 и T-B6-14 (3–5 ассертов в `scale-express-authoring.test.ts`) — иначе два из четырёх пунктов приёмки держатся только на grep из отчёта.
6. Якоря mermaid: `R-T-B6-01` «было» `:33-36` → `:39-45`; `R-T-B6-14` `infra:207`→`212`, `interface:140`→`145`, `recover-from-code:69`→`74`.
7. `R-T-B6-10.md:13` — строка доски `61-TASK-BOARD.md:153` → `:155`.
8. `R-V14-1` — «scope/module не знают о SCALE» неверно: `<ScalePath when="scale=function|fix">` был там до пачки; добавить строку о сосуществовании двух конструкций.
9. Перечислить восемь имён флейкнувших файлов вместо «флаки под нагрузкой».
10. Обновить `9592` → `9633` на доске (`:155`), в треке (`:831`, `:933`) и в очереди (`:270` описание пачки).

**Подтверждено:** полнота таблиц файлов (6/6 и 5/4/5), запретные зоны не тронуты, все девять команд-доказательств воспроизведены с заявленным результатом, детерминированность регенерации, содержательность нового теста, отсутствие изменений сверх задач, чистое слияние со всеми пятью ветками, согласованность pivot-механики с D-41 и L-16, правомерность STOP T-B6-10 по строке доски, L-12, D-55 и стоп-классу 2.

**Рекомендация: PR после правок 1–2 (обязательно) и 3–10 (в том же проходе — все дешёвые).** Возвращать пачку не нужно: код три задачи закрывает по существу, дефекты — в текстах отчётов и в одном шве директивы.

---

## Формулировки для T-B6-10 (к разговору с оператором, D-63)

Предлагаю расщепить, а не расширять зону пачки 19:

- **T-B6-10a — «Четыре монолитные директивы разбиты на ленивую сборку».** Размер **M**. Зона: `ai/kit/templates/sdd-v2/{infra,root,migration-v1-v2,router}.directive.hbs`, `ai/kit/assembly-manifest.json`. Приёмка: `assembly-manifest.json` содержит семь `lazy`-записей; у каждой из четырёх появилась папка `<name>/steps/` (9/5/9/3 пакета); skeleton каждой ≤ 6000 токенов (мягкая цель); `check:directives-fresh` и `audit:sdd-templates` зелены. Предпосылка: утверждённая оператором карта изменений по D-55.
- **T-B6-10b — «Гейт бюджетов измеряет все директивы, монолит выше лимита роняет сборку».** Размер **S**. Зона: `ai/kit/step-budget-gate.ts`, `ai/kit/__tests__/skeleton-package-binding.guard.test.ts` (трек прямо просит расширить именно его). Приёмка: скан больше не пропускает директивы без `steps/`; тест «директива на 8001 токен даёт finding уровня error и exit 1»; `check:directive-budgets` зелен на реальном дереве. Идёт строго после T-B6-10a.

Что спросить у оператора одним разговором:
1. **L-12 в силе?** Если да — заводим T-B6-10a/T-B6-10b, а строку T-B6-10 на доске помечаем «расщеплена». Пачка 19 уходит в PR как есть, с честным открытым стопом.
2. **Если ресурса на разбиение сейчас нет** — допустим ли промежуточный шаг по образцу L-3 («warn сейчас, error после инвентаризации долга»): гейт **начинает измерять все 28 директив немедленно**, но для четырёх названных монолитов уровень находки — warning с печатью числа, а переход в error привязан к закрытию T-B6-10a. Это не baseline-allowlist: измерение включено, регресс виден, мотивация разбивать не снимается. Требует явного решения оператора, потому что смягчает букву приёмки «монолит выше лимита роняет гейт».
3. **Baseline по образцу GAP-B-1** (зафиксировать 9633/7572/6741/6094 как точку отсчёта, валить только новое превышение) — технически возможен и однострочен, но по существу равен отклонённому Q6(b) и отменяет L-12. Спрашивать его следует именно так: «откатываем ли L-12», а не «какой из двух вариантов». Если оператор его выбирает — строка приёмки доски «увеличение бюджета не является улучшением без измерения» требует зафиксировать метрику (task success / missed dependency / tokens), иначе baseline становится тихой легализацией.
4. Попутно: подтвердить обновление числа `9592` → `9633` в доске, треке и очереди.
