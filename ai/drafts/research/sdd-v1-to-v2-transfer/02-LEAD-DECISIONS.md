# 02 — Технические решения Lead (в рамках полномочий 5.2)

> Полномочия: техвопросы, ответ на которые следует из документов плана и уже принятых решений оператора, закрывает Lead; архитектура, смена скоупа, удаления, политика — оператор (`70-ORCHESTRATION-PROTOCOL.md` §3). Каждое решение ниже можно отменить решением оператора.

| # | Трек / вопрос | Решение Lead | Обоснование |
|---|---|---|---|
| L-1 | 31 D1: токен `ver` при живом receipt | **Вариант 1**: `ver` возвращается в словарь как человеческая заметка; закрывать фазу может только receipt | Это уже позиция v2 (`execute.directive.xml:42` «hand-written `ver` line is not evidence»); ничего не переписывается |
| L-2 | 31 D4: маркер `PHASE_RECEIPTS:v1` | **Вариант 3**: тикеты v2-имени `*.task.<ID>.md` проверяются всегда; legacy — по маркеру; предусловие — правка `firstRoundPhaseBlockCounts` (B2-16) | Согласуется с D-4 (ERROR после мигратора и самомиграции): после самомиграции весь корпус — v2-имена |
| L-3 | 31 D5: строгость новых кодов | **Вариант 2**: warn сейчас, error после инвентаризации долга (B2-15 + golden `DA-lazy-asm`) | Прямое следствие D-4 |
| L-4 | 32 D-4: связка `sync-skills` → полный `sync` | **Вариант 3**: директивный проход opt-in (`--with-directives`), по умолчанию off (SO-9, первая волна) | Узкая команда не должна выполнять самое широкое разрушение; S-задача |
| L-5 | 33 Q3: версия схемы файла правила (snake_case в инструкции пакета vs CamelCase в файлах) | **Вариант (b)** + исправить инструкцию пакета `ax-rules-load-from-phase-block.xml:3` на CamelCase | Дешевле всего; расхождение внутри пакета чинится на источнике |
| L-6 | 33 Q4: где живёт проверка контракта правила | **Вариант (c)**: один модуль в `shared/sdd/`, два входа (`ai/kit`-аудит и `sdd-check --rules`) | Потребитель и мейнтейнер получают одну и ту же проверку |
| L-7 | 33 Q6: категории `architecture`/`quality` | **(b)+(c)**: `quality` убрать (каталога нет нигде), `architecture` оставить и наполнить правилом `ports-adapters`, на которое уже ссылается шаблон; согласовать все три списка категорий | Три расходящихся списка в пакете — дефект, не выбор |
| L-8 | 33 Q7: baseline-хуки зовут несуществующий `verify --wip` | **Вариант (b)**: при порте переписать `<VerificationHooks>` под `sdd-verify --task --phase` сразу | В v2 команды `verify --wip` нет и не будет (D-13: публичный verify только `--plan --json`) |
| L-9 | 40 Q1: несобранные аксиомы библиотеки | **Вариант (c)**: 20 цитируемых + названные в §1 — собрать; остальные `status="draft"` с зафиксированным критерием | Ничего не удаляется (v1 заморожен, тексты сохраняются) |
| L-10 | 40 Q2: `lint-axioms` referenced-but-undefined | **Вариант (b)**: error с сокращающимся `KNOWN_DANGLING`-allowlist | Приём уже применён в `audit-halt-activation.mjs` |
| L-11 | 40 Q4: 7 «выживших» правил `sdd-infra-golang` | **Вариант (b)**: два стек-агностичных (`AX_FAIL_VS_ENV_FAIL`, `AX_GATES_NEVER_MUTATE`) → `ai/kit/axiom/infra/`, пять go-специфичных → rule-файл golang | Инварианты не должны прятаться в файле одного стека |
| L-12 | 40 Q6: бюджеты директив | **Вариант (a)**: единый лимит 8000 токенов для всех, монолиты тоже; предусловие — lazy-split `infra` (T-B6-10) раньше включения гейта | Иначе гейт измеряет три малые директивы и молчит про `infra` 9592 |
| L-13 | 50 Q3: где хранить фикстуры реальных репозиториев | **Вариант (a)**: вне репозитория, `EVAL_FIXTURES_ROOT` вместо абсолютных путей, воспроизводимость из SHA (E-16), проверка на файлы класса секретов (в `fixture-detmig` найден `Tools/Artifactory/.netrc`) | Безопасность и воспроизводимость; **факт про `.netrc` доведён до оператора** |
| L-14 | 50 Q4: модель судьи | **Вариант (a)**: одна дешёвая модель `llm-proxy`, судья — диагностика (следствие 50 Q1) | Только семейство `llm-proxy` (решение оператора в RC-сессии) |
| L-15 | 50 Q5: порядок «полнота мигратора» ↔ «бар исполнимости» | **Вариант (b)**: сначала бар red-first (E-07, S: коды в `MIGRATION_CRITICAL_CODES`), затем полнота мигратора (E-06) | Дисциплина red-first уже принята в `session-metrics.py gate`; бар дешёвый |
| L-16 | Токены вердикта отложенного решения (D-49, `_raw/V14-2-AUTONOMOUS-EXECUTE.md`) | Четыре закрытых токена: `pending-operator` (по умолчанию при записи) · `accepted` = «принять как есть», следствие — спека дописывается под фактическое поведение (v2 «add to the spec») · `rework` = «переделать по спеке» · `rolled-back` = «откатить». «Add to the spec» отдельным токеном не вводится — это следствие `accepted`. Гейт `SDD_DEVIATION_VERDICT_MISSING` считает открытым только `pending-operator`. |
| L-17 | Гейт baseline при переименовании файла (V-BATCH-04) | Ребейзлайн ради переименования не делается (D-38 — только решением оператора и не по косметике). Правило: задача, которой нужно переименовать файл с находками в baseline, оставляет имя файла и меняет содержимое (Meta Task-ID), либо переименование откладывается до очередного авторизованного ребейзлайна. Вариант «rename detection в гейте» отклонён: маскирует регрессию на переименованном и испорченном файле. |
| L-18 | Стековые ветки пачек | Пачки, перегенерирующие директивы, ведутся стеком: PR #28 → PR #30 (kit-lint) → пачка 4 (specs-match-code, ребейз на kit-lint с правилом «структура пачки 5 + содержание пачки 4») → пачка 6 (на PR #28). Сгенерированные файлы при конфликте всегда перегенерируются, не сливаются руками. Порядок мержа оператором: #28, затем #29 (независим), затем #30, затем пачка 4. |
| L-19 | Стратегия мержа PR пачек (вопрос ревьюера) | Для стековых PR требуется **merge commit**, не squash: только тогда после мержа нижнего PR дифф верхнего сокращается сам. Если оператор всё же сквошит — Lead перебазирует верхний PR (`git rebase --onto` на новую голову, отбрасывая уже влитые коммиты) и перепушивает ветку; описание PR не меняется. Мерж #29 (независимый) — в любой момент; при мерже #29 до #31 Lead перебазирует #31 (общий файл `sdd-check.cmd.ts`, слияние проверено чистым в V-BATCH-04). |
| L-20 | Замороженная копия критериев для ревью | Тег `plan-baseline-1` = `e87fec62` на ветке плана: критерии A1–A24, решения D-1..D-59, гид 71. Ревью PR #28–#31 ведётся против этого тега; изменения критериев после него — отдельным PR в план с пометкой в журнале. |
| L-21 | Гейт `yagni` при переносе примитивов без вызовов (V-02) | Примитивы verify переносятся из v1 первыми, вызовы приходят в V-03..V-09; гейт `yagni` (pre-commit) требует ≥2 использований. Решение: **новая спека v2 `specs/cli/verify/verify.spec.md`** с разделом `Usage Waiver` по существующему механизму (8 прецедентов), запись на символ с владельцем-задачей; waiver снимается в той задаче, что приносит вызов. Это не правка v1-данных (D-39 соблюдён). Отклонено: `--no-verify`, отключение гейта, слияние V-02 в один коммит с V-03 (теряется трассируемость коммит → задача). |
