# Результаты и состояние eval-ов

eval — это проверка: даём модели задачу и смотрим, пройдёт ли она шаг разработки сама, в одноразовой
песочнице. Ниже — какие проверки есть, в каком они состоянии и с какими числами (время, число действий
модели, израсходованные токены).

## Постоянные результаты (GAP-E-6, D-62)

С этого решения каждый прогон `npm run sdd-flow-eval` пишет ОДНУ постоянную запись на сценарий —
`ai/flow-eval/results/<дата>-<сценарий>[-N]/summary.json` (+ `judge.md`, когда судья писал вердикт).
Это не gitignore'ится: сырые данные каждого прогона живут в репозитории. Таблица ниже — не ручная, а
СГЕНЕРИРОВАНА из этих файлов скриптом `ai/flow-eval/scripts/results-table.ts`; регенерация без диффа —
тест свежести (см. `ai/flow-eval/scripts/__tests__/results-table.test.ts`). Не редактируй текст между
маркерами руками — следующий прогон скрипта его перезапишет.

<!-- GAP-E-6:GENERATED:BEGIN — do not hand-edit; run `results:table` -->

**Постоянные результаты** — числа ниже посчитаны СКРИПТОМ из `ai/flow-eval/results/<дата>-<сценарий>/summary.json` (медиана по всем прогонам этого сценария; действий = число вызовов инструментов на последнем наблюдении). Регенерация: `node --import tsx ai/flow-eval/scripts/results-table.ts`.

| Сценарий | Прогонов | Действий (медиана) | Время (медиана) | Токенов (медиана) | Состояние |
| -------- | -------: | ------------------: | ---------------- | -----------------: | --------- |
| `infra-log-summary` | 3 | 11 | ~2 мин | ~15 000 | Проходит (3/3) |

<!-- GAP-E-6:GENERATED:END -->

## Исторические данные (восстановлены из журнала OpenCode, до GAP-E-6)

Числа в этом и следующих разделах — **не из `ai/flow-eval/results/`** (этого каталога тогда не
существовало): они восстановлены задним числом из журнала OpenCode по реальным прогонам, вручную, PR
#32. «Типичный прогон» там = медиана по всем прогонам этого eval за то время. Раздел заморожен как
исторический — не путать с генерируемой таблицей выше, которая копится по новым прогонам.

### Учебные проверки

Гоняются на выдуманных задачах (фикстуры создаются с нуля).

| Проверка              | Что модель должна сделать                       | Прогонов | Действий | Время    |  Токенов | Состояние                            |
| --------------------- | ----------------------------------------------- | -------: | -------: | -------- | -------: | ------------------------------------ |
| `fibonacci-library`   | По короткому описанию написать спецификации     |       36 |       74 | ~15 мин  | ~191 000 | Проходит: спеки без ошибок структуры |
| `tic-tac-toe`         | По готовым спекам составить список задач        |        7 |       71 | ~17 мин  | ~233 000 | Проходит                             |
| `slugify-toolchain`   | Взять готовую задачу и выполнить: код + тесты   |        4 |      114 | ~7 мин   | ~180 000 | Проходит до конца, тесты зелёные     |
| `broken-specs-repair` | Найти и починить одну ошибку, внесённую в спеку |        4 |       15 | ~2 мин   |  ~44 000 | Проходит                             |
| `infra-log-summary`   | Написать bash-скрипт: сводка по логам           |        1 |       10 | ~2 мин   |  ~32 000 | Один прогон                          |
| `infra-rotate-logs`   | Написать bash-скрипт: ротация логов             |        1 |        8 | ~1.5 мин |  ~14 000 | Один прогон                          |
| `infra-makefile`      | Написать Makefile по описанию                   |        1 |        7 | ~1 мин   |  ~11 000 | Один прогон                          |

«Действий» — сколько раз модель вызвала инструмент (запуск команды, чтение файла, правка). «Токенов» —
сколько израсходовала за прогон.

**Полный проход насквозь** (написать спеки → составить задачи → выполнить) прогонялся на `fibonacci`
как цепочка: спеки ~15 мин / 191 000 токенов → задачи ~13 мин / 281 000 → выполнение ~12 мин / 256 000.
Проходит до конца — это главный положительный результат по учебным проверкам.

Кроме перечисленного, было ещё ~90 прогонов-вариантов (другие промпты и фикстуры) — это проверки
гипотез, а не отдельные eval-ы; они разобраны в [`EXPERIMENTS-LOG.md`](./EXPERIMENTS-LOG.md).

### Проверки на реальном проекте cloud-ios

Здесь числа сохранены по каждому прогону.

#### Миграция спецификаций инфраструктуры cloud-ios в формат SDD v2

Между прогонами автопроверку **усиливали**: первые два прогона смотрели только на структуру спек,
дальше — на полноту переноса. Поэтому смысл столбца «Автопроверка» по ходу меняется.

| Время (UTC) | Автопроверка                  | Действий | Время  | Токенов |
| ----------- | ----------------------------- | -------: | ------ | ------: |
| 05.09 11:33 | структура спек — не прошла    |      107 | 21 мин | 292 027 |
| 05.09 12:19 | структура спек — не прошла    |      122 | 35 мин | 483 903 |
| 05.09 16:53 | полнота переноса — не прошла  |      144 | 54 мин | 623 701 |
| 05.09 17:57 | полнота переноса — не прошла  |      180 | 59 мин | 831 583 |
| 06.09 09:29 | полнота переноса — **прошла** |      105 | 38 мин | 442 442 |
| 06.09 10:13 | полнота переноса — **прошла** |      116 | 42 мин | 431 844 |
| 06.09 10:37 | полнота переноса — **прошла** |       83 | 13 мин | 196 547 |

Автопроверка к концу стала проходить — по мере того как спека дозревала.

#### Восстановление удалённого скрипта в iOS-проекте cloud-ios

В настоящем iOS-проекте cloud-ios есть рабочий скрипт `Tools/check-swiftlint-exceptions.sh` — он следит
за исключениями из правил линтера SwiftLint (какие нарушения стиля в коде официально разрешены). Для
проверки этот скрипт **удалили** и попросили модель воссоздать его заново из спецификации (тикет
`IB-script`). Смотрим, восстановит ли флоу рабочий файл по одному описанию.

Во всех прогонах модель **писала** скрипт, но задачу до конца не доводила (не закрывала тикет).

Как вообще проверяется, что bash-гард восстановлен **правильно** (у него нет компиляции, как у кода) —
разобрано отдельно: [`guard-verification.md`](./guard-verification.md).

| Время (UTC) | Действий | Время  | Токенов |
| ----------- | -------: | ------ | ------: |
| 12:37       |       32 | 14 мин | 160 798 |
| 14:52       |       66 | 20 мин | 207 540 |
| 15:28       |       79 | 24 мин | 189 695 |
| 16:22       |       58 | 7 мин  | 119 883 |
| 16:50       |       52 | 15 мин | 158 921 |
| 17:14       |       37 | 6 мин  |  98 504 |
| 17:38       |       74 | 20 мин | 247 934 |

#### Как читать эти 14 прогонов

Кроме автопроверок, каждый прогон оценивал ещё и **автоматический проверяющий на базе модели**. Он не
принял ни один из 14 прогонов cloud-ios. Поэтому числа выше показывают **не «успех», а как шёл процесс**:
сколько модель работала, сколько действий и токенов потратила. Польза этих прогонов — в том, что каждый
провал указал на конкретный дефект, который потом починили.

### Что дали провалы на cloud-ios

| Нашли на прогонах                                        | Что сделали в ответ                                            |
| -------------------------------------------------------- | -------------------------------------------------------------- |
| Оценка миграции пропускала недоделанные задачи           | Добавили отдельную проверку полноты переноса                   |
| «Скрипт написан» считалось «задача сделана»              | Добавили проверку, что задача доведена до конца (тикет закрыт) |
| Песочница держала старую сборку между прогонами          | Сделали, чтобы в песочницу всегда клалась свежая сборка        |
| Утверждение оператора между фазами оставалось незакрытым | Написали скрипт, который его корректно имитирует               |

Подробный пофазный разбор «как флоу стал проходить» жил в `PROGRESS-REPORT.md` — чистом историческом
снимке, удалённом при унификации доков (GAP-E-5, D-46): его выводы уже перечислены в таблице выше,
отдельного документа для них больше нет.
