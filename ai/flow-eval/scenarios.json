[
  {
    "id": "fibonacci-library",
    "fixture": "fibonacci-library",
    "phase": "spec-authoring",
    "mode": "full-spec-to-approval-1",
    "scale": "function",
    "intent": "Author the complete canonical Fibonacci specification from inputs/brief.md, including behavior, edge cases, and acceptance criteria.",
    "acceptance": "Specifications only. Public API nth(n: number): number is pure. Integer n from 0 through 77 is accepted; F(0)=0 and F(1)=1. Negative, non-integer, and greater-than-77 inputs are rejected with explicit errors. Scope and one cohesive module spec exist, include Requirement IDs and negative scenarios, pass the mechanical check, receive one fresh semantic review, and leave Approval #1 pending."
  },
  {
    "id": "tic-tac-toe",
    "fixture": "tic-tac-toe",
    "phase": "scaffold",
    "mode": "actual-tickets-to-approval-2",
    "intent": "Materialize actual implementation tickets from the approved tic-tac-toe specification, with dependencies and executable acceptance criteria.",
    "acceptance": "The approved specs assign every runtime, package-manager, and configuration prerequisite to exactly one Bootstrap Requirements owner. Actual tickets and indexes exist and pass sdd-check --task --authoring plus sdd-check --all before review and Approval #2. A red ticket check returns to decomposition instead of being patched locally."
  },
  {
    "id": "slugify-toolchain",
    "fixture": "slugify-toolchain",
    "phase": "execute",
    "mode": "canonical-execute",
    "intent": "Execute the canonical slugify ticket against the canonical slugify specification, including tests and the declared toolchain."
  },
  {
    "id": "broken-specs-repair",
    "fixture": "broken-specs",
    "phase": "repair",
    "mode": "fix-to-clean",
    "intent": "The workspace specs are structurally complete but `npx gennady sdd-check --all .` reports errors. Fix every reported error in its owning spec file until the check is clean. Do not disable or weaken any check.",
    "acceptance": "`npx gennady sdd-check --all .` reports 0 errors; the invalid mermaid diagram is corrected to valid syntax, following the checker error message."
  },
  {
    "id": "infra-log-summary",
    "fixture": "infra-log-summary",
    "phase": "task",
    "mode": "brief-to-artifact",
    "intent": "Complete inputs/brief.md: write bin/log-summary.sh to the exact output format.",
    "acceptance": "golden/verify.sh exits 0."
  },
  {
    "id": "infra-rotate-logs",
    "fixture": "infra-rotate-logs",
    "phase": "task",
    "mode": "brief-to-artifact",
    "intent": "Complete inputs/brief.md: write bin/rotate-logs.sh.",
    "acceptance": "golden/verify.sh exits 0."
  },
  {
    "id": "infra-makefile",
    "fixture": "infra-makefile",
    "phase": "task",
    "mode": "brief-to-artifact",
    "intent": "Complete inputs/brief.md: write a Makefile with build/test/clean/help.",
    "acceptance": "golden/verify.sh exits 0."
  }
]
