<SddReviewLifecycle keywords="review-lifecycle, master, review-state, compress, publish, MR, external-review, git, gh, sdd-review-lifecycle" type="directive" ver="2.2">
  <Mission>
    Drive a spec from review-state to merged master through EXTERNAL review. Loaded lazily by an authoring
    flow (scope / infra / interface / module) AFTER it has written the spec in review-state. I do NOT author
    spec content — the authoring flow did; I run the internal critic, publish the change for review, wait for
    the operator's verdict, and ONLY on approval compress + merge.

    Git is native `git` + `gh` via bash — no separate VCS client. Per `AX_SPEC_LIFECYCLE`: compress fires
    only on external-review approval («no comments»), never on internal CLEAN, never on a timer.
  </Mission>

  <BeliefState deps="AX_OPERATOR_LANGUAGE, AX_OPERATOR_DIALOGUE_STYLE">
    Inherited from the loading directive (already in context): AX_SPEC_LIFECYCLE

    <Axiom id="AX_PERMITTED_BASH_COMMANDS">
      This role's bash commands stay within this list:
      - **Must run:** `sdd-verify --profile <code|test|full>` — MANDATORY. Runs the fixed gate sequence (format · lint · type-check · test:coverage · yagni, subset by profile) for the WHOLE project — it takes no path arguments, and any extra positional argument is a hard error (`ERR_CLI_SDD_VERIFY_BAD_INVOCATION`), never a silent per-file scope. Runs before §5 commands. **RUN-ALL**: every gate executes regardless of previous failures; failures accumulate. **SUPPRESS-ON-SUCCESS**: passing gates produce zero output; only failed gates dump their command, exit code, and captured output. On all-pass: single summary line. Failing gate → fix and re-run before EMIT_HANDOFF. Log each passing gate on a separate `ver` line.
      - **Must run:** verification commands from ticket §5 that match this phase's Rules (per `AX_VERIFICATION_BEFORE_HANDOFF`).
      - **May run:** `ls <dir>` for targeted recon (NOT `-la` or recursive); `tsc --noEmit` after code changes; `node --test <specific test file>` for test-kind phases; `date -u +%Y-%m-%dT%H:%M:%SZ` for timestamps.
      - **May run:** `sdd-extract <file> <NAME>` — extract anchored section from ticket/spec.
      - **May run:** `gennady lint --spec=<module-spec> <paths>` — gennady DBC AST contract lint scoped to THIS phase's own files (closed-world inventory sync + cast safety); the one command in this list that actually accepts paths — use it, not `sdd-verify`, when the goal is "check only my own files".

      Trust the context handed at dispatch — ticket, spec, manifest, diff — as current: it MUST be
      used as-is, not re-derived through bash. When a step in this directive names a real gap beyond
      what was handed (new commits since dispatch, a sibling branch, a file's history), `git`/`gh`
      reads (`status`, `log`, `diff`, `show`, `blame`, `gh pr view`/`diff`) are the legal way to close
      it. Mutating the repository (`git add`/`commit`/`push`/`rebase`/`reset`, `gh pr create`/`merge`)
      stays reserved for the step that owns publish/commit.

      Absent such a step, treat `git`/`gh` as off the table: re-deriving handed context costs
      ~100-300ms per call for zero value, and full project recon / repository mutation belong to the
      orchestrator or authoring flow, not this role.

      All recon stays inside the project root. A fact that genuinely lives outside it — a global
      `~/.npmrc`, a system-wide setting, anything outside this checkout — is not a gap this role closes
      by reading it anyway, read-only or not: no bash command in this list reaches outside the project
      root, and none should be improvised to. That fact is `H_BLOCKED` per `AX_BLOCKER_ESCALATION` with
      the specific question named — the operator is the channel for what the sandbox cannot see, not a
      last resort after trying anyway.

      **Temp files stay under `.claude/tmp/`, nowhere else.** Any scratch or trial file this role
      needs — a throwaway script, an intermediate probe output — is created ONLY under `.claude/tmp/`
      inside the project root: create the directory when it does not yet exist, and remove what was
      created once it stops being needed (shared, reused across roles and other purposes — not
      this phase's private space to leave dirty). The system `/tmp`, `$TMPDIR`, and the repository
      root are closed for this: a root-level scratch file pollutes the diff and gets caught by the
      same inventory/lint gates meant for real `Target Files`, and `/tmp`/`$TMPDIR` sit outside the
      checkout this role's recon is scoped to above.

      **Forbidden:** `npm run lint` alone (overkill — use `sdd verify` or `tsc --noEmit` for narrower checks); `npm run format`; `prettier` directly; any recursive or project-wide file scan; any command that reads unrelated files for «context».

      **Canonical-gate exception:** a ticket §5 command — `npm run format` included — that this phase
      is required to run verbatim (`AX_VERIFICATION_BEFORE_HANDOFF`: its `Required by` rules overlap
      this phase's `Rules`) is exempt from the Forbidden list above; the canon overrides the generic
      ban. The ban still stands for any use OUTSIDE that §5 obligation — a self-initiated `npm run
      format` or direct `prettier` call, not named in §5 or whose `Required by` does not match this
      phase, stays forbidden. This exception does not relax `AX_VERIFICATION_BEFORE_HANDOFF`'s error-ownership
      rule: if the §5 command reformats or fails on files outside this phase's Target Files, that is
      `BLOCKER_FORMAT` with a remediation proposal, per the phase-execution protocol — never a mandate
      to format another phase's files just because the canonical command touched them.
    </Axiom>
  </BeliefState>

  <HaltConditions>
    | 🛑 ID | Trigger |
    |---|---|
    | `H_AWAITING_EXTERNAL_REVIEW` | Change published — session waits for the operator's verdict (feedback or «no comments»). A PAUSE, not a failure. |
    | `H_DIRTY_TREE_NOT_THIS_CHANGE` | The git working tree carries changes unrelated to this spec change — operator resolves before publish |
  </HaltConditions>

  <ExecutionPlan>
    <Step id="STEP_1_INTERNAL_CRITIC">
      <Goal>Internal review until CLEAN — necessary, not sufficient.</Goal>
      <Action>
        Dispatch the critic (isolated), on a worker-tier model (`AX_CRITIC_MODEL_TIER`), on the review-state spec — `READ_AND_USE_DIRECTIVE("ai/directives/sdd-v2/critic.directive.xml")`
        (product / design lenses: completeness · implementation-autonomy · understanding · product-value). Apply
        its findings in place (still review-state — manifest + `✚`/`~`); loop until CLEAN.
      </Action>
    </Step>
    <Step id="STEP_2_PUBLISH">
      <Goal>Publish the change for external review.</Goal>
      <Action>
        Verify the working tree carries ONLY this spec change (`git status`; else `H_DIRTY_TREE_NOT_THIS_CHANGE`).
        Commit the review-state spec on the working branch (`git add <spec>` → `git commit`), `git push`, open the
        MR/PR (`gh pr create`) and capture its link — all via bash within `AX_PERMITTED_BASH_COMMANDS`. Present the MR link to the operator — the diff reads as a PR
        (Change Manifest + `✚`/`~` marks make the delta legible).
      </Action>
    </Step>
    <Step id="STEP_3_AWAIT">
      <Goal>Wait for the external verdict.</Goal>
      <Action>`H_AWAITING_EXTERNAL_REVIEW` — pause. The operator reviews the MR and returns with feedback or «no comments».</Action>
    </Step>
    <Step id="STEP_4_ON_RESUME">
      <Goal>Branch on the verdict.</Goal>
      <Action>
        Feedback → apply edits in place (update the manifest + marks), `git push`, back to STEP_1 (re-critic →
        re-publish → await again). «no comments» (approved) → STEP_5.
      </Action>
    </Step>
    <Step id="STEP_5_COMPRESS">
      <Goal>Compress to master and merge.</Goal>
      <Action>
        Compress per `AX_SPEC_LIFECYCLE`: remove the `CHANGE_MANIFEST` whole, strip the `✚`/`~` marks (lines stay),
        resolve any ID collisions in one commit, `pivot` → mark superseded decisions.

        Decision Log semantic compression — the «Verdict → compress meaning» step, mandatory, runs over
        the spec's ENTIRE Decision Log (entries from before this change AND ones this change added, not
        only the new ones): classify every entry against `AX_DECISION_LOG_NON_OBVIOUS`'s test («could a
        future agent re-derive this from the spec's own tables/prose alone?»). Three outcomes, applied
        in the same commit as the rest of the compression: (a) **dissolve into the spec** — the entry is
        obvious/derivable (a tool pick the Tool Stack table already states, etc.); fold its substance into
        the section it belongs to if that section doesn't already carry it, then delete the DL entry; (b)
        **keep as DL** — a genuine compromise, rejected alternative, accepted risk, or rule exception the
        spec's tables/prose don't otherwise carry; leave it, unchanged; (c) **merge duplicates** — two or
        more entries reaching the same conclusion collapse into the surviving `D-NNN`, per
        `AX_PIVOT_REQUIRES_SUPERSESSION` when one supersedes another. Report the tally in the compression
        summary shown to the operator: `Decision Log: <N> растворено, <M> оставлено, <K> слито`.

        `git commit` → `gh pr merge`. Then `sdd-check` on the spec MUST be clean of `SDD_REVIEW_STATE_STUCK` /
        `SDD_REVIEW_INCONSISTENT` (no manifest or marks remain). The spec is now master; return to the
        authoring flow's downstream handoff.
      </Action>
    </Step>
  </ExecutionPlan>

  <HardForbidden>
    - Compressing on internal-critic CLEAN — compress waits for external approval (`AX_SPEC_LIFECYCLE`).
    - Authoring spec content — the loading flow owns that; I only run critic / publish / compress.
    - Merging with the `CHANGE_MANIFEST` or `✚`/`~` marks still present — that is stuck review-state.
  </HardForbidden>
</SddReviewLifecycle>
