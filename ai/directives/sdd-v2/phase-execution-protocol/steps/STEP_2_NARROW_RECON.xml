0.8.4

<Step id="STEP_2_NARROW_RECON">
      <Goal>Verify ground truth before writing — log only on divergence.</Goal>
      <Action>
        Per `AX_NARROW_RECON`: log a recon line ONLY on divergence; when state matches the plan, stay
        silent. On divergence write exact `discovery <fact>` to the phase log payload, then run
        eventLog ToolCall below;
        if it breaks the phase contract → `H_RECON_DIVERGENCE`, else proceed.
        <ToolCall owner="phase-worker" result="eventLog">npx gennady sdd-log <ticket> line --content-file .claude/tmp/<task-id>-<phase-id>-log.txt --phase <PhaseID></ToolCall> Inputs <ticket>, <task-id>, <phase-id>, <PhaseID>: file-write of literal divergence observed here. Use eventLog.
      </Action>
    </Step>

<Axiom id="AX_NARROW_RECON">
      Before any Edit / Write: one quick recon scoped to THIS phase only.
      1. Read prior-phase Handoff lines from this Round's Execution Log to align with what was produced.
      2. Do not list every Target File parent. Run one bounded `ls <nearest existing declared ancestor>`
         only when a Target File declares a new parent directory or STEP_1/Handoff evidence says its parent
         is absent. When the parent is already established by the current phase context or Handoff, no `ls`
         is needed. Never use `ls` to discover targets or widen the manifest.

      Divergence (file expected absent but exists / expected present but absent) → record an Insight line; if it blocks the phase contract → `H_RECON_DIVERGENCE`. Do not «fix» silently.

      Forbidden: `git status`, `git branch`, `git log`, `git diff`, or any other git operation. The phase agent does not commit, does not inspect repository state, does not work with git. Full project recon is the orchestrator's job.
    </Axiom>