0.8.4
<!-- Step packages regenerate via: npm run build:directives -- --assembly=lazy. A missing file at a path below means the build or sync is stale — rebuild rather than guess. -->

<SddScaffold keywords="task-scaffolding, dependency-graph, dag, bdd, rules-cascade, rule-activation, tickets, modes, extend-dag, isolated, cheap-model, sdd-scaffold" type="directive" ver="2.0">
  <Mission>
    Decompose the approved spec tree into a DAG of compact, self-contained task tickets ready for
    autonomous execution by a cheaper model in isolation. A separate skill (`/sdd-scaffold`), run in
    its own process; the operator sees only two gates — the breakdown and the test plan — everything
    else is mechanical.

    Input — `specs/README.md` Scope Graph (+ each scope / module spec). Output — per-module ticket
    files + the task-index hierarchy (`<module>.3-tasks.md`, scope, project). Next — the execute
    orchestrator consumes one ticket at a time.

    Out of scope: writing code, running tests — that is execute. Spec authoring — that is the `/sdd`
    flow. This directive does not interview; it converges existing specs into tickets.

    **Glossary.** Modes: initial · extend-dag. Cascade = the set of rule files effective for a scope,
    computed from the Scope Graph (root → scope → module → phase).
  </Mission>

  <BeliefState>
    Inherited from the loading directive (already in context): AX_NO_PROCESS_NARRATION, AX_OPERATOR_DIALOGUE_STYLE, AX_OPERATOR_LANGUAGE, AX_OPERATOR_OUTPUT_LIVE_TEXT, AX_PREFLIGHT_BLAST_RADIUS_SCOPED, AX_PROGRESSIVE_DISCLOSURE, AX_STATELESS_ARTIFACT, AX_TOOL_INVOCATION, MESSAGE_LAYOUT, QUESTION_RULE_SLIM, SESSION_FILE_FORMAT













































    <Axiom id="AX_CAP_5">Five critic results is the hard automatic cap. At the fifth result — regardless
      of sensor verdict — the operator must dispose the cycle before readiness or another dispatch. If that
      round made no edits, the choices are `CLEAN`, `CONTINUE THROUGH ROUND N` with an explicit higher cap,
      or `RESTART: reason`; if it made edits, CLEAN is unavailable because the changed bytes have not been
      reviewed, so only CONTINUE or RESTART are offered. A raised cap behaves identically when reached. The
    sensor cannot authorize continuation, and continuation is never readiness.</Axiom>
  </BeliefState>

  <SessionState>

  </SessionState>

  <HaltConditions>
    | 🛑 ID | Trigger |
    |---|---|
    | `H_NO_SPECS` | `specs/README.md` absent, or no approved scope spec to scaffold |
    | `H_SCOPE_NOT_DECOMPOSED` | Scope type is missing/ambiguous; product/library Module Map is not a non-empty exact manifest of its canonical module specs; or an interface is used as a task owner. Continue through `/sdd` module decomposition and integrated review first; infrastructure is the sole flat-scope exception. |
    | `H_AMBIGUOUS_MODE` | Tasks exist + mode verb absent (per `AX_MODE_AUTO_DETECT_OR_HALT`) |
    | `H_MISSING_RULE_FILE` | A rule reference or `<DependsOn>` entry does not resolve to an existing file (`AX_RULES_RESOLUTION_HARD_FAIL`) |
    | `H_RULES_CYCLE` | `<DependsOn>` closure cycles during per-phase rule activation (STEP_3) |
    | `H_VERIFICATION_COMMAND_MISSING` | A `<RequiresVerification>` alias is absent from infra-spec Verification Commands (alias, ticket ID) |
    | `H_BOOTSTRAP_REQUIREMENTS_MISSING` | The target scope being scaffolded (the intake target) lacks a Bootstrap Requirements section — checked ONLY for that scope, not for every ✅ scope in the Portal (did not pass the scope flow) |
    | `H_ID_COLLISION` | A generated Task-ID / slug collides with an existing one |
    | `H_TYPING_SCENARIO_MISSING` | A DbC contract in Spec References has no `contract`-level typing scenario mapped to a test |
    | `H_SCAFFOLD_NOT_EXECUTABLE` | The feasibility critic cannot reach an operator-disposed state within its bounded loop, or a required fresh fallback cannot receive the full latest target-set plus retained context/history |
    | `H_OPERATOR_REJECT` | Operator rejected the breakdown or the test plan |
  </HaltConditions>

  <ExecutionPlan>
    - **STEP_0_INTAKE** — Read the Portal; detect scopes, graph, mode. (auto) Before executing this step, READ_AND_USE_DIRECTIVE("ai/directives/sdd-v2/scaffold/steps/STEP_0_INTAKE.xml").
    - **STEP_0B_PREFLIGHT** — Clear the FLOW_VERSION/READINESS preflight gate before scaffolding. Before executing this step, READ_AND_USE_DIRECTIVE("ai/directives/sdd-v2/scaffold/steps/STEP_0B_PREFLIGHT.xml").
    - **STEP_1_CASCADE** — Resolve the rules cascade. (auto) Before executing this step, READ_AND_USE_DIRECTIVE("ai/directives/sdd-v2/scaffold/steps/STEP_1_CASCADE.xml").
    - **STEP_2_DAG** — Build the acyclic task DAG. — GATE 1: operator approves the BREAKDOWN. Before executing this step, READ_AND_USE_DIRECTIVE("ai/directives/sdd-v2/scaffold/steps/STEP_2_DAG.xml").
    - **STEP_3_TASK_GENERATION** — Generate tickets + test plan. (auto) Before executing this step, READ_AND_USE_DIRECTIVE("ai/directives/sdd-v2/scaffold/steps/STEP_3_TASK_GENERATION.xml").
    - **STEP_3B_FEASIBILITY_CRITIC** — Reject structurally valid but operationally impossible tickets before execute. (auto) Before executing this step, READ_AND_USE_DIRECTIVE("ai/directives/sdd-v2/scaffold/steps/STEP_3B_FEASIBILITY_CRITIC.xml").
    - **STEP_4_TEST_PLAN_REVIEW** — GATE 2: operator approves the TEST PLAN. Before executing this step, READ_AND_USE_DIRECTIVE("ai/directives/sdd-v2/scaffold/steps/STEP_4_TEST_PLAN_REVIEW.xml").
    - **STEP_5_FINALIZE** — Write the index hierarchy; hand off. (auto) Before executing this step, READ_AND_USE_DIRECTIVE("ai/directives/sdd-v2/scaffold/steps/STEP_5_FINALIZE.xml").
  </ExecutionPlan>

  <ChatProtocol>
  </ChatProtocol>

  <ChatOutput>
    Service line — dim, internal, kept silent (per `AX_NO_PROCESS_NARRATION`):
    `📋 TS_R<N> · <scope> · <mode> · conf=<LOW|MEDIUM|HIGH>`. Breadcrumb milestones (per
    `BREADCRUMB_FORMAT`): Intake · Cascade · DAG · Tickets · Tests · Done. Only two milestones are
    operator gates — DAG (breakdown) and Tests (test plan).

    <Contract id="BREADCRUMB_FORMAT">
      One line, its own paragraph below the service line — a blank line separates the two (per
      `MESSAGE_LAYOUT`); a single `\n` with no blank line is a contract defect, markdown renders it
      fused onto the service line as one run-on. This session's milestones, each rendered `<Name>
      <status>`, with a status marker chosen for legibility on BOTH light and dark themes — no
      half-tone shading characters (`░ ▒ ▓`) anywhere, they vanish depending on theme. Status set: `✅`
      done · `👷` active (the step running now) · `◻️` not yet reached. Delta only; never re-list
      everything already settled. This is the «where are we», and it replaces a verbose run-on of
      finished steps.

      `Vision ✅ · Scope Graph 👷 · Infra ◻️ · Portal ◻️`
    </Contract>

  </ChatOutput>

  <ArtifactOutput>
    <!-- ticket / module-index / scope+project trackers load lazily at STEP_3 / STEP_5 — see ExecutionPlan. -->

  </ArtifactOutput>
</SddScaffold>
