0.8.4

<Step id="STEP_4_TEST_PLAN_REVIEW">
      <Goal>GATE 2: operator approves the TEST PLAN.</Goal>
      <Action>
        Present the test plan as a decision card: each BDD scenario as a use-case, tagged
        `[<ACRONYM>-REQ-N]`, traced up to the scope Vision / goal it protects, plus coverage (every
        contract has a typing scenario, every scenario maps to a test). The operator approves **what is
        tested, which use-cases are covered, and that this guarantees the vision** — the one gate on
        substance. Include every STEP_3 coverage-owner/toolchain/e2e alternative in this SAME card;
        its one approval resolves all. Out-of-scope proposals the subagent surfaced are raised here.
        Return `TerminalDecision: pause` and preserve the session while stopped for that decision.

        On approval, materialize only the selected coverage/toolchain/e2e choices in affected tickets,
        then record `gate2-choice {choices,changedTickets}` through feasibilityGate2Event and follow its
        `NEXT=`. Empty changedTickets permits `NEXT=FINALIZE`. Non-empty changedTickets requires
        `NEXT=REFRESH_TARGETS`: run gate2ContextRefresh, record `target-refreshed`, and return to
        STEP_3B's same retained critic. Do not regenerate the DAG, rerun STEP_3, or replay choices
        already approved.

        A critic `NEW_FORK` is presented as one delta-only card with its affected use-cases; its Gate 2
        choice must name changed tickets and be reviewed before finalize. Every subsequent response is
        first recorded through STEP_3B, so result 5 or a raised cap returns
        `NEXT=ASK_OPERATOR_CAP` before CLEAN or a fork can advance. At any operator decision return
        `TerminalDecision: pause`; the CLI-owned journal is the resume state.
        <ToolCall owner="this-step" result="gate2ContextRefresh">npx gennady sdd-check --scaffold-feasibility</ToolCall> Use gate2ContextRefresh after Gate 2 changes and before critic re-dispatch.
        <ToolCall owner="this-step" result="feasibilityGate2Event">npx gennady sdd-session feasibility --content-file .claude/tmp/sdd-scaffold-feasibility-event.json</ToolCall> Payload: current gate2-choice or target-refreshed event. Use feasibilityGate2Event before acting on its NEXT instruction.
      </Action>
    </Step>

After completing this step, and only then, READ_AND_USE_DIRECTIVE("ai/directives/sdd-v2/scaffold/steps/STEP_5_FINALIZE.xml"). Do not preload later step packages.