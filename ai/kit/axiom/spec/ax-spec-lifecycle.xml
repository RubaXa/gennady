<!-- source: stateless SDD v2 specification lifecycle -->
<Axiom id="AX_SPEC_LIFECYCLE">
  A V2 specification has one durable format. Greenfield authoring creates that format directly;
  refine/pivot edits it in place. Git diff shows changes for semantic judgment. Temporary Change Manifest,
  per-line review marks, Critic Rounds, publication state, and migration between V2 subformats are
  not part of the specification.

  Before approval #1, run structural checks and one fresh independent semantic review of the
  complete target set. Resolve findings in the owning specs. Any semantic edit invalidates the old
  verdict, resets the human-readable approval marker to pending, and requires a fresh bounded review.
  The operator then approves the current artifacts and the specs record approval #1 per
  `ARTIFACT_APPROVAL_MARKER`. Implementation follows only after approval #1. Marker mechanics never
  use a content hash; the model judges staleness from the artifacts and Git evidence.

  An editorial correction may use its exact mechanical verifier only when it changes no requirement,
  behavior, contract, inventory, architecture, dependency, BDD acceptance, risk, or Decision Log
  meaning. Any such delta is semantic and returns to the lifecycle above.
</Axiom>
