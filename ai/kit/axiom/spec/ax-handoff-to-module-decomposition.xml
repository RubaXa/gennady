<Axiom id="AX_HANDOFF_TO_MODULE_DECOMPOSITION">
  Module decomposition accepts a structurally complete `product` or `library` scope draft as the
  next authoring step before approval #1. It creates/updates the module specs needed to make the
  complete spec set reviewable. For an already-approved scope, add/refine module work is also valid.
  Neither case depends on a session working set, temporary manifest, or bare approval label.
  Infrastructure remains the sole no-module scope type.

  The resulting complete scope+module spec set runs one independent semantic review and operator
  approval #1 before scaffold. A later semantic change repeats that boundary.
</Axiom>
