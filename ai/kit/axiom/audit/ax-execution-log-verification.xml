<!-- source: ai/directives/sdd/audit.directive.xml -->
<!-- NOTE (B2-03): this brick is not included as a partial by any current .hbs — it does not reach
     the built directive graph today (dangling brick, same class as the ai/kit/contract/process/**
     debt B2-18 tracks). It stays a plain-text mirror of the vocabulary rather than a partial
     reference so it reads correctly on disk even unrendered; keep it byte-consistent with
     `shared/sdd/execution-log.ts` (single home) by hand until it is wired into a directive. -->
<Axiom id="AX_EXECUTION_LOG_VERIFICATION" status="draft">
      Token vocabulary (single home, `shared/sdd/execution-log.ts`): `intro <Entity> ← <reason>` ·
      `decision <key>=<value> ← <reason>` · `tried <approach> → <result>` · `discovery <fact>` ·
      `insight <observation> → <spec-section>` · `verified <tool>@<version> <summary>` · `CLI-owned
      SDD_PHASE_RECEIPT:<PhaseID>` · `BLOCKED <cause>` · `DONE` · `ver <cmd> → <result> — human note
      only, never closes a phase alone (L-1)` · `yagni <name> ← <reason>` · `fix <target> ←
      <reason>` · `env-fix <file> ← <operator decision ref> (AX_ENV_FIX_CHANNEL)` · `correction[:]
      <round>/<phase> <field>: <old> → <new> ← <reason>`.
      Required per phase block: `ver` or CLI-owned `SDD_PHASE_RECEIPT` (one final result — only the
      receipt closes the phase, a hand-written `ver` line never does, L-1), `DONE`, `**Handoff →**`,
      `intro <X> ← <reason>` for every entity in code but not in module Inventory (cross-check via
      git diff), `verified` for `config`-kind, `BLOCKED <cause>` if phase ended blocked.

      Missing required line → `EXECUTION_LOG_INCOMPLETE`. Unreplaced `<…>` placeholder on `[x]` line
      → `EXECUTION_LOG_INCOMPLETE` (BLOCKER, «fabricated done»). Line with a token outside the
      vocabulary → `EXECUTION_LOG_INCOMPLETE` (MINOR, «padding»).
    </Axiom>
