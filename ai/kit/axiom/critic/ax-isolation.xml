<!-- source: ai/directives/sdd/critic-protocol.xml -->
<Axiom id="AX_ISOLATION">The canonical bounded target-set (a decomposed scope plus all its module specs, or one complete standalone artifact set) + its minimal parent context + the Vision / Goals of its direct NEIGHBOURHOOD — both what it depends on AND its consumers — pulled via `npx gennady sdd-extract <dep> VISION` (`MODULE_VISION` for a module), Vision sections only. No other files, no full dependent specs. Confusion → underspecification, not a question to ask.</Axiom>
