<!-- source: ai/directives/infra/nodejs-npm-setup.xml -->
<Axiom id="AX_NPM_AGENT_SANDBOX_REGISTRY">
  **The lock file decides the registry. A committed `package-lock.json` already records which
  registry every dependency resolved from — install with that registry, unchanged. Force the public
  registry only when no lock file exists yet.**

  **Lock file present → install as-is, no registry override:**
  ```bash
  npm ci
  npm install
  ```
  Changing the registry a lock resolved from means re-resolving that lock — a decision at
  infrastructure-scope-discovery scope, never something this role decides per invocation. `npm ci`/`npm
  install` read the registry the lock (and any committed `.npmrc`) already declares; do not prefix them
  with a different `npm_config_registry`.

  **No lock file yet (fresh install, first dependency) → force the public registry:**
  ```bash
  npm_config_registry=https://registry.npmjs.org/ npm install
  npm_config_registry=https://registry.npmjs.org/ npm view <pkg>
  ```
  The problem this solves: operator's `~/.npmrc` typically points at a corporate registry/proxy
  reachable only from inside corp network; a sandboxed agent doesn't have access → `npm install` hangs
  on `ENOTFOUND` / `EAI_AGAIN` or fails. Forcing `registry.npmjs.org` inline — not by editing
  `~/.npmrc` — resolves the first lock against a registry the sandbox can actually reach.

  Why inline prefix, not a config edit: bypasses corp `~/.npmrc` without modifying it (operator's local
  setup stays intact); no persistent state in shell env — nothing leaks between commands;
  self-documenting in logs — operator sees exactly which registry produced the lock; sandbox-friendly —
  no dependency on inherited env from launch context. Mechanism: npm config priority — CLI flags >
  `npm_config_*` env vars > project `.npmrc` > `~/.npmrc` > `/etc/npmrc` > defaults. POSIX `VAR=value
  command` makes the variable visible to the spawned process only.

  **Incompatibility — the project genuinely needs a private corp package, and the public registry 403s
  on it:** this is not a registry choice to negotiate from inside the sandbox. Both directions are
  closed to the worker — corp registry is unreachable, public registry lacks the package — so there is
  no per-invocation flag that resolves it. Halt `H_BLOCKED` per `AX_BLOCKER_ESCALATION` with the facts
  (package name, both registries' response, whether a lock already exists) for the operator to decide:
  resolve the lock in a full-network shell, or grant the sandbox explicit network access. Do not search
  for a registry, mirror, or proxy that might work around the 403 — that is exactly the improvisation
  `AX_BLOCKER_ESCALATION` exists to stop.

  **Forbidden:** overriding the registry on a command that already has a lock to read (`npm ci`/`npm
  install` against an existing `package-lock.json`); modifying operator's `~/.npmrc` (breaks operator's
  other work); committing project `.npmrc` with a corp registry URL; silent failure / partial install
  when a registry is unreachable (operator must see the explicit error).
</Axiom>
