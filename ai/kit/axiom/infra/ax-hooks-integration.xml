<!-- source: ai/directives/infra/git-setup.xml -->
<Axiom id="AX_HOOKS_INTEGRATION">
  Hooks fire on every commit/push. `pre-commit` is verify-only: first require working tree == index,
  then run the whole-project read-only `sdd-verify --profile full` (types, coverage suite, production
  contract lint, format check, yagni) and the directive freshness/axiom/contract/halt/budget audits. It never
  runs formatter/autofix or re-stages bytes; a red repairable result tells the author to run the
  project `fix`, stage the result, and retry. Budget is honestly tens of seconds because tests and
  types are whole-project. `commit-msg` (conventional-commits validator) stays required.
  Optional: `prepare-commit-msg` (prepend issue ID), `post-merge` (reinstall if lock changed). Hook
  config committed to repo; one onboarding command registers them (e.g. `lefthook install`).
</Axiom>
