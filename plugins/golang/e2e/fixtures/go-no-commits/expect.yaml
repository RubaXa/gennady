notes: >
  A git repo without a single commit cannot measure guarded drift, so a drift gate is ENV_FAIL
  rather than a code finding: there is no HEAD baseline.
requires: [go]
noCommit: true
argv: ['--all']
exitCode: 1
treeUnchanged: false
gates:
  golang:needs-replica: { status: env-fail }
