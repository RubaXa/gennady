notes: >
  requires must not break the normal path: every precondition holds, so the gate command runs
  and passes. Preconditions resolve cwd in the guarded repository tree, which `test -f` proves.
requires: [go]
argv: ['--all']
exitCode: 0
gates:
  golang:e2e: { status: pass }
